---
title: Live playground
sidebar_position: 0
---
import sandpack1IndexTs from "../sandpack/examples/live-playground/main/index.ts?raw";
import sandpack1ConstantsTs from "../sandpack/examples/live-playground/main/constants.ts?raw";

This page is a minimal interactive playground.

- The editor and game preview are visible at the same time.
- Use the **Run** button in the editor to apply changes.
- Hot reload is disabled, so the game does not rebuild on every keystroke.
- The canvas is hard-limited to preview width to avoid scrolling.

export const PREVIEW_HEIGHT = 300;

<SandpackExample
	files={{
		"/index.ts": sandpack1IndexTs,
		"/constants.ts": sandpack1ConstantsTs,
	}}
	visibleFiles={["/index.ts", "/constants.ts"]}
	hiddenFiles={["/index.html", "/styles.css", "/package.json"]}
	activeFile="/index.ts"
	showRunButton
	editorHeight={PREVIEW_HEIGHT}
	options={{
		autoReload: false,
		showNavigator: true,
		showRefreshButton: true,
		showTabs: true,
		showLineNumbers: true,
		wrapContent: false,
	}}
	customSetup={{
		dependencies: {
			"sliver-engine": "0.0.1-alpha-5",
		},
	}}
/>
