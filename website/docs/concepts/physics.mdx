---
title: Physics
sidebar_position: 6
---
import sandpack1IndexTs from "../sandpack/concepts/physics/live-example-physics-options/index.ts?raw";
import sandpack1EnclosureDimensionsTs from "../sandpack/concepts/physics/live-example-physics-options/enclosureDimensions.ts?raw";
import sandpack1CreateBoundsTs from "../sandpack/concepts/physics/live-example-physics-options/createBounds.ts?raw";
import sandpack1CreateColorPhysicsOptionsTs from "../sandpack/concepts/physics/live-example-physics-options/createColorPhysicsOptions.ts?raw";
import sandpack1CreateExtraSquaresTs from "../sandpack/concepts/physics/live-example-physics-options/createExtraSquares.ts?raw";
import sandpack1MainTs from "../sandpack/concepts/physics/live-example-physics-options/main.ts?raw";
import sandpack2IndexTs from "../sandpack/concepts/physics/live-example-scene-gravity/index.ts?raw";
import sandpack2EnclosureDimensionsTs from "../sandpack/concepts/physics/live-example-scene-gravity/enclosureDimensions.ts?raw";
import sandpack2CreateGravityTs from "../sandpack/concepts/physics/live-example-scene-gravity/createGravity.ts?raw";
import sandpack2CreateBoundsTs from "../sandpack/concepts/physics/live-example-scene-gravity/createBounds.ts?raw";
import sandpack2GravityBoxTs from "../sandpack/concepts/physics/live-example-scene-gravity/GravityBox.ts?raw";
import sandpack2MainTs from "../sandpack/concepts/physics/live-example-scene-gravity/main.ts?raw";
import sandpack3IndexTs from "../sandpack/concepts/physics/live-example-detached-hitboxes/index.ts?raw";
import sandpack3EnclosureDimensionsTs from "../sandpack/concepts/physics/live-example-detached-hitboxes/enclosureDimensions.ts?raw";
import sandpack3CreateHitboxConfigsTs from "../sandpack/concepts/physics/live-example-detached-hitboxes/createHitboxConfigs.ts?raw";
import sandpack3CreateHitboxObjectsTs from "../sandpack/concepts/physics/live-example-detached-hitboxes/createHitboxObjects.ts?raw";
import sandpack3CreateBoundsTs from "../sandpack/concepts/physics/live-example-detached-hitboxes/createBounds.ts?raw";
import sandpack3MainTs from "../sandpack/concepts/physics/live-example-detached-hitboxes/main.ts?raw";
import sandpack4IndexTs from "../sandpack/concepts/physics/live-example-non-solid-trigger-hitbox/index.ts?raw";
import sandpack4EnclosureDimensionsTs from "../sandpack/concepts/physics/live-example-non-solid-trigger-hitbox/enclosureDimensions.ts?raw";
import sandpack4CreateBoundsTs from "../sandpack/concepts/physics/live-example-non-solid-trigger-hitbox/createBounds.ts?raw";
import sandpack4TriggerZoneTs from "../sandpack/concepts/physics/live-example-non-solid-trigger-hitbox/TriggerZone.ts?raw";
import sandpack4MovingBoxBaseTs from "../sandpack/concepts/physics/live-example-non-solid-trigger-hitbox/MovingBoxBase.ts?raw";
import sandpack4TeleportingBoxTs from "../sandpack/concepts/physics/live-example-non-solid-trigger-hitbox/TeleportingBox.ts?raw";
import sandpack4MainTs from "../sandpack/concepts/physics/live-example-non-solid-trigger-hitbox/main.ts?raw";

Sliver’s physics is intentionally lightweight and **tick-based**:

- Movement is integrated once per tick (`position += speed`, `rotation += angularVelocity`).
- Gravity is a per-tick vector stored on the `Scene`.
- Collisions are resolved from `Hitbox` overlaps after all objects tick.

This makes physics deterministic with respect to ticks, and keeps the engine simple. It also means you should think in “per tick” units unless you convert from seconds using the tick rate.

## Physics properties on GameObject

Every `GameObject` has a `phisics` descriptor (note the spelling):

```ts
gameObject.setPhisics({
  immovable: false,
  affectedByGravity: true,
  restitution: 0.2,
  friction: 0.4,
  mass: 2,
});
```

- `immovable`: if `true`, collision resolution won’t apply linear/angular impulse updates to the object and its inverse mass is treated as `0`.
- `affectedByGravity`: if `true`, the scene gravity vector is added to `speed` each tick (only if not immovable and not being dragged).
- `restitution`: bounciness used for collision impulse resolution (`0` = no bounce, `1` = perfectly elastic). Values are clamped to `[0, 1]`.
- `friction`: tangential impulse clamp (`0` = no friction). Values are clamped to `[0, 1]`.
- `mass`: used to scale linear and angular collision response. If `<= 0` or invalid, it falls back to `1`.

## Live example: physics options

This sandbox builds a closed box (left wall, right wall, ceiling, floor) plus 8 grabbable square bodies.

- `createBounds.ts` and `createExtraSquares.ts` keep positions/hitboxes hardcoded.
- 4 cyan squares and 4 rose squares are spawned, with different sizes.
- Edit only `createColorPhysicsOptions.ts` to tune physics options for each color.
- Press **Run** to apply changes.

<SandpackExample
	files={{
		"/index.ts": {
			code: sandpack1IndexTs,
			readOnly: true,
		},
		"/enclosureDimensions.ts": {
			code: sandpack1EnclosureDimensionsTs,
			readOnly: true,
		},
		"/createBounds.ts": {
			code: sandpack1CreateBoundsTs,
			readOnly: true,
		},
		"/createColorPhysicsOptions.ts": sandpack1CreateColorPhysicsOptionsTs,
		"/createExtraSquares.ts": {
			code: sandpack1CreateExtraSquaresTs,
			readOnly: true,
		},
		"/main.ts": {
			code: sandpack1MainTs,
readOnly: true,
},
}}
visibleFiles={["/createColorPhysicsOptions.ts"]}
activeFile="/createColorPhysicsOptions.ts"
editorHeight={320}
showRunButton
hiddenFiles={[
"/index.html",
"/styles.css",
"/package.json",
"/index.ts",
"/enclosureDimensions.ts",
"/main.ts",
"/createBounds.ts",
"/createExtraSquares.ts",
]}
options={{
		autoReload: false,
		showNavigator: true,
		showRefreshButton: true,
		showTabs: true,
		showLineNumbers: true,
		wrapContent: false,
	}}
customSetup={{
		dependencies: {
			"sliver-engine": "0.0.1-alpha-5",
		},
	}}
/>

## Motion: speed and angular velocity

The engine integrates motion in `GameObject.tick()`:

- `speed` (`Vector`) is applied to `position` once per tick.
- `angularVelocity` (`number`) is applied to `rotation` once per tick.

Example: constant velocity:

```ts
import { Vector } from "sliver-engine";

gameObject.speed = new Vector(2, 0); // +2 px per tick
```

If you prefer “pixels per second”, compute a per-tick delta using the tick rate:

```ts
const dt = 1 / this.getContext()!.getTickRate();
this.speed = new Vector(120 * dt, 0); // 120 px/s converted to px/tick
```

## Gravity (Scene)

Scenes store a gravity vector:

```ts
import { Vector } from "sliver-engine";

scene.setGravity(new Vector(0, 0.4));
```

Gravity is applied by `GameObject.tick()` when:

- `phisics.affectedByGravity === true`
- `phisics.immovable === false`
- `beingGrabbed === false` (dragging temporarily disables gravity; see [`Input & events`](./events.md) for the drag/grab decorators and patterns)

Gravity is added directly to `speed` once per tick, so treat it as “acceleration per tick”.

_Note: You can use negative values and zero to disable gravity._

### Live example: scene gravity

This sandbox creates a closed box (ceiling, floor, left wall, right wall) and one gravity-affected box.

- Edit only `createGravity.ts` to change the gravity vector.
- Press **Run** to apply changes.

<SandpackExample
	files={{
		"/index.ts": {
			code: sandpack2IndexTs,
			readOnly: true,
		},
		"/enclosureDimensions.ts": {
			code: sandpack2EnclosureDimensionsTs,
			readOnly: true,
		},
		"/createGravity.ts": sandpack2CreateGravityTs,
		"/createBounds.ts": {
			code: sandpack2CreateBoundsTs,
			readOnly: true,
		},
		"/GravityBox.ts": {
			code: sandpack2GravityBoxTs,
			readOnly: true,
		},
		"/main.ts": {
			code: sandpack2MainTs,
readOnly: true,
},
}}
visibleFiles={["/createGravity.ts"]}
activeFile="/createGravity.ts"
editorHeight={320}
showRunButton
hiddenFiles={[
"/index.html",
"/styles.css",
"/package.json",
"/index.ts",
"/enclosureDimensions.ts",
"/main.ts",
"/createBounds.ts",
"/GravityBox.ts",
]}
options={{
		autoReload: false,
		showNavigator: true,
		showRefreshButton: true,
		showTabs: true,
		showLineNumbers: true,
		wrapContent: false,
	}}
customSetup={{
		dependencies: {
			"sliver-engine": "0.0.1-alpha-5",
		},
	}}
/>

## Hitboxes (Square and Circle)

Collisions use `Hitbox`es attached to `GameObject`s:

- `SquareHitbox(offset, size, gameObject, options?)`
- `CircleHitbox(offset, radius, gameObject, options?)`

Attach hitboxes with `addHitbox`:

```ts
import { GameObject, SquareHitbox, Vector } from "sliver-engine";

const box = new GameObject("crate", new Vector(100, 100));
box.setPhisics({ immovable: false });
box.addHitbox(new SquareHitbox(Vector.zero(), new Vector(32, 32), box));
```

### Live example: detached hitboxes

This sandbox has two grabbable objects with detached hitboxes inside an enclosed box.

- Edit only `createHitboxConfigs.ts` to change the hitboxes list (type, size and offset) for each object.
- Press **Run** to apply changes.

<SandpackExample
	files={{
		"/index.ts": {
			code: sandpack3IndexTs,
			readOnly: true,
		},
		"/enclosureDimensions.ts": {
			code: sandpack3EnclosureDimensionsTs,
			readOnly: true,
		},
		"/createHitboxConfigs.ts": sandpack3CreateHitboxConfigsTs,
		"/createHitboxObjects.ts": {
			code: sandpack3CreateHitboxObjectsTs,
			readOnly: true,
		},
		"/createBounds.ts": {
			code: sandpack3CreateBoundsTs,
			readOnly: true,
		},
		"/main.ts": {
			code: sandpack3MainTs,
			readOnly: true,
		},
	}}
	visibleFiles={["/createHitboxConfigs.ts"]}
	activeFile="/createHitboxConfigs.ts"
	editorHeight={320}
	showRunButton
	hiddenFiles={[
	"/index.html",
	"/styles.css",
	"/package.json",
	"/index.ts",
	"/enclosureDimensions.ts",
	"/main.ts",
	"/createBounds.ts",
	"/createHitboxObjects.ts",
	]}
	options={{
		autoReload: false,
		showNavigator: true,
		showRefreshButton: true,
		showTabs: true,
		showLineNumbers: true,
		wrapContent: false,
	}}
	customSetup={{
		dependencies: {
			"sliver-engine": "0.0.1-alpha-5",
		},
	}}
/>

### Hitbox options: `solid` and `debug`

```ts
box.addHitbox(
  new SquareHitbox(Vector.zero(), new Vector(32, 32), box, {
    solid: true,
    debug: false,
  }),
);
```

- `solid: false` means “detect overlap, but don’t resolve collisions/impulses”.
  - This is useful for UI hit-testing or trigger volumes.
- `debug: true` renders the hitbox (via the object’s debug rendering path).

Tip: `GameObject.addHitbox` warns when you add 5+ hitboxes; many hitboxes can get expensive because collisions are pairwise.

### Live example: non-solid trigger hitbox

This sandbox has a moving square and a non-solid trigger area on the right side of the same enclosure.

- The trigger uses `solid: false`, so it detects overlap without pushing the square.
- Edit only `TeleportingBox.ts` and customize the square’s `onColision` behavior.
- Press **Run** to apply changes.

<SandpackExample
	files={{
		"/index.ts": {
			code: sandpack4IndexTs,
			readOnly: true,
		},
		"/enclosureDimensions.ts": {
			code: sandpack4EnclosureDimensionsTs,
			readOnly: true,
		},
		"/createBounds.ts": {
			code: sandpack4CreateBoundsTs,
			readOnly: true,
		},
		"/TriggerZone.ts": {
			code: sandpack4TriggerZoneTs,
			readOnly: true,
		},
		"/MovingBoxBase.ts": {
			code: sandpack4MovingBoxBaseTs,
			readOnly: true,
		},
		"/TeleportingBox.ts": sandpack4TeleportingBoxTs,
		"/main.ts": {
			code: sandpack4MainTs,
			readOnly: true,
		},
	}}
	visibleFiles={["/TeleportingBox.ts"]}
	activeFile="/TeleportingBox.ts"
	editorHeight={320}
	showRunButton
	hiddenFiles={[
	"/index.html",
	"/styles.css",
	"/package.json",
	"/index.ts",
	"/enclosureDimensions.ts",
	"/main.ts",
	"/createBounds.ts",
	"/TriggerZone.ts",
	"/MovingBoxBase.ts",
	]}
	options={{
		autoReload: false,
		showNavigator: true,
		showRefreshButton: true,
		showTabs: true,
		showLineNumbers: true,
		wrapContent: false,
	}}
	customSetup={{
		dependencies: {
			"sliver-engine": "0.0.1-alpha-5",
		},
	}}
/>

## Collision pipeline (what happens each tick)

After all objects tick, `Scene` runs collision handling:

1. It filters to **active** objects that have hitboxes.
2. It runs a broad-phase AABB check per object pair, then checks hitbox intersections.
3. For each overlapping object pair:
   - calls `a.beforeColision(b)` and `b.beforeColision(a)` once per pair per tick
   - calls `a.onColision(b, penetration)` / `b.onColision(a, -penetration)` once per pair per tick
   - stores every overlapping **solid** hitbox pair as a contact
4. It runs a small projected Gauss-Seidel velocity solver over those contacts:
   - reads the bodies’ **current** `speed` / `angularVelocity` on each pass
   - applies normal impulses immediately, so stacks converge over multiple iterations
   - uses a Baumgarte bias to correct penetration drift without calling `translate()`
   - zeroes restitution for near-resting contacts to avoid perpetual micro-bounces
   - clamps tangential friction against the accumulated normal impulse
5. Bodies that remain very slow while touching something for several frames are put to sleep (`speed = 0`, `angularVelocity = 0`).

Important details:

- If either object’s `beforeColision` returns `false`, the pair is ignored for the remainder of that tick.
- If either hitbox is `solid: false`, the engine will still notify `onColision` (overlap), but will skip resolution/impulses.
- Collision handling is velocity-based; the engine does not snap bodies apart with `translate()` during resolution.

### Using `beforeColision` for triggers and filters

Example: ignore collisions with your own bullets, but still react to others:

```ts
override beforeColision(other: GameObject): boolean {
  return other.name !== "bullet";
}
```

### Using `onColision` for gameplay reactions

```ts
import { GameObject, Vector } from "sliver-engine";

override onColision(other: GameObject, penetration: Vector): void {
  if (other.name === "spikes") {
    this.sendMessage("player:died", null);
  }
}
```

`penetration` is the overlap vector (minimum translation direction/amount) computed from hitboxes, which is often useful for “which side did we hit?” style logic.

## Common patterns

### Static ground + dynamic player

- Ground: `immovable: true` (default) with a solid hitbox
- Player: `immovable: false`, `affectedByGravity: true`, a solid hitbox

### Non-solid triggers

Use a non-solid hitbox and handle the overlap in `onColision`:

```ts
trigger.addHitbox(
  new SquareHitbox(Vector.zero(), new Vector(64, 64), trigger, {
    solid: false,
  }),
);
```

### “Pause” without physics advancing

Scenes are all ticked while active. If you push an overlay scene (pause menu) but want gameplay physics to stop, gate your gameplay objects’ `tick()` logic (or switch scenes with replace). See [`Game loop`](./game-loop.md) and [`Scenes`](./scenes.md).
