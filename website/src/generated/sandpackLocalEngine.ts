// Auto-generated by website/scripts/generateSandpackLocalEngine.cjs
// Do not edit by hand.

const LOCAL_ENGINE_PACKAGE_JSON = "{\n  \"name\": \"sliver-engine\",\n  \"version\": \"0.0.1-alpha-7\",\n  \"main\": \"./index.js\",\n  \"exports\": {\n    \".\": \"./index.js\",\n    \"./package.json\": \"./package.json\",\n    \"./*\": \"./*\"\n  }\n}\n";

export const SANDPACK_LOCAL_ENGINE_FILES = {
	"/node_modules/sliver-engine/package.json": {
		code: LOCAL_ENGINE_PACKAGE_JSON,
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Assets/Shapes/Arrow.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get renderArrow () {\n        return renderArrow;\n    },\n    get renderChevron () {\n        return renderChevron;\n    },\n    get shapeWithParams () {\n        return shapeWithParams;\n    }\n});\nconst renderArrow = (context, position, size)=>{\n    const centerX = position.x + size.x / 2;\n    const centerY = position.y + size.y / 2;\n    context.beginPath();\n    context.moveTo(centerX - size.x / 2, centerY);\n    context.lineTo(centerX + size.x / 2, centerY);\n    context.lineTo(centerX + size.x / 2 - size.x * 0.1, centerY - size.y * 0.1);\n    context.moveTo(centerX + size.x / 2, centerY);\n    context.lineTo(centerX + size.x / 2 - size.x * 0.1, centerY + size.y * 0.1);\n    context.stroke();\n    context.closePath();\n};\nconst renderChevron = (context, position, size)=>{\n    const centerX = position.x + size.x / 2;\n    const centerY = position.y + size.y / 2;\n    context.beginPath();\n    context.moveTo(centerX - size.x / 2, centerY - size.y / 2);\n    context.lineTo(centerX, centerY);\n    context.lineTo(centerX - size.x / 2, centerY + size.y / 2);\n    context.stroke();\n    context.closePath();\n};\nconst shapeWithParams = (shape, options = {})=>(context, position, size)=>{\n        context.save();\n        context.lineWidth = options.strokeWidth || 2;\n        context.strokeStyle = options.color || \"black\";\n        context.lineCap = options.lineCap || \"round\";\n        context.lineJoin = options.lineJoin || \"round\";\n        shape(context, position, size);\n        context.restore();\n    };",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Assets/Shapes/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./Arrow.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/CanvasController/SpriteLibrary.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"SpriteLibrary\", {\n    enumerable: true,\n    get: function() {\n        return SpriteLibrary;\n    }\n});\nconst _index = require(\"../Lib/Vector/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nconst isDrawable2DContext = (context)=>!!context && \"drawImage\" in context;\nclass SpriteLibrary {\n    async loadSprite(name, url) {\n        return new Promise((resolve, reject)=>{\n            const img = new Image();\n            img.onload = ()=>{\n                this.sprites.set(name, img);\n                resolve();\n            };\n            img.onerror = reject;\n            img.src = url.toString();\n        });\n    }\n    async loadSvgSprite(name, url) {\n        return new Promise((resolve, reject)=>{\n            const img = new Image();\n            img.onload = ()=>{\n                this.sprites.set(name, img);\n                resolve();\n            };\n            img.onerror = reject;\n            fetch(url).then((response)=>response.text()).then((svgText)=>{\n                const svgBlob = new Blob([\n                    svgText\n                ], {\n                    type: \"image/svg+xml\"\n                });\n                const url = URL.createObjectURL(svgBlob);\n                img.src = url;\n            }).catch(reject);\n        });\n    }\n    /**\n     * Load a fixed size sprite sheet\n     */ async loadSpriteSheet(name, url, frameWidth, frameHeight) {\n        return new Promise((resolve, reject)=>{\n            const img = new Image();\n            img.onload = ()=>{\n                const columns = Math.floor(img.width / frameWidth);\n                this.spriteSheets.set(name, {\n                    image: img,\n                    frameWidth,\n                    frameHeight,\n                    columns\n                });\n                resolve();\n            };\n            img.onerror = reject;\n            img.src = url.toString();\n        });\n    }\n    getSprite(name) {\n        return this.sprites.get(name);\n    }\n    async loadSpriteDefinitionsFromJson(url) {\n        const response = await fetch(url.toString());\n        if (!response.ok) {\n            throw new Error(`Failed to load sprite manifest \"${url.toString()}\": ${response.status} ${response.statusText}`);\n        }\n        const payload = await response.json();\n        this.loadSpriteDefinitions(payload);\n    }\n    loadSpriteDefinitions(payload) {\n        const manifest = this.validateSpriteManifest(payload);\n        Object.entries(manifest.sprites).forEach(([name, entry])=>{\n            this.loadSpriteManifestEntry(name, entry);\n        });\n    }\n    defineSpriteFromSheet(name, sheetName, topLeft, bottomRight) {\n        this.ensureSpriteNameAvailable(name);\n        this.spriteDefinitions.set(name, {\n            kind: \"rect\",\n            sheetName,\n            topLeft: topLeft.clone(),\n            bottomRight: bottomRight.clone()\n        });\n    }\n    defineSpriteFromSheetIndexes(name, sheetName, indexes) {\n        this.ensureSpriteNameAvailable(name);\n        const clonedIndexes = indexes.map((row)=>row.slice());\n        this.spriteDefinitions.set(name, {\n            kind: \"indexes\",\n            sheetName,\n            indexes: clonedIndexes\n        });\n        const sheet = this.spriteSheets.get(sheetName);\n        if (sheet) {\n            this.bakeSpriteDefinitionBitmap(name, {\n                kind: \"indexes\",\n                sheetName,\n                indexes: clonedIndexes\n            }, sheet);\n        }\n    }\n    getSpriteDefinition(name) {\n        const definition = this.spriteDefinitions.get(name);\n        if (!definition) return null;\n        if (definition.kind === \"rect\") {\n            return {\n                kind: \"rect\",\n                sheetName: definition.sheetName,\n                topLeft: definition.topLeft.clone(),\n                bottomRight: definition.bottomRight.clone()\n            };\n        }\n        return {\n            kind: \"indexes\",\n            sheetName: definition.sheetName,\n            indexes: definition.indexes.map((row)=>row.slice())\n        };\n    }\n    getSpriteSheetFrameSize(sheetName) {\n        const sheet = this.spriteSheets.get(sheetName);\n        if (!sheet) return null;\n        return {\n            frameWidth: sheet.frameWidth,\n            frameHeight: sheet.frameHeight\n        };\n    }\n    /**\n     * Render a named sprite defined from a sprite sheet selection.\n     */ drawSpriteDefinition(ctx, spriteName, position, scale = 1, mirrorHorizontal = false, mirrorVertical = false) {\n        const definition = this.spriteDefinitions.get(spriteName);\n        if (!definition) return;\n        const sheet = this.spriteSheets.get(definition.sheetName);\n        if (!sheet) return;\n        const { image, frameWidth, frameHeight } = sheet;\n        if (definition.kind === \"rect\") {\n            const startCol = Math.min(definition.topLeft.x, definition.bottomRight.x);\n            const startRow = Math.min(definition.topLeft.y, definition.bottomRight.y);\n            const endCol = Math.max(definition.topLeft.x, definition.bottomRight.x);\n            const endRow = Math.max(definition.topLeft.y, definition.bottomRight.y);\n            const spriteWidth = (endCol - startCol + 1) * frameWidth;\n            const spriteHeight = (endRow - startRow + 1) * frameHeight;\n            const sx = startCol * frameWidth;\n            const sy = startRow * frameHeight;\n            ctx.imageSmoothingEnabled = false;\n            ctx.save();\n            if (mirrorHorizontal || mirrorVertical) {\n                const centerX = position.x + spriteWidth * scale / 2;\n                const centerY = position.y + spriteHeight * scale / 2;\n                ctx.translate(centerX, centerY);\n                if (mirrorHorizontal) {\n                    ctx.scale(-1, 1);\n                }\n                if (mirrorVertical) {\n                    ctx.scale(1, -1);\n                }\n                ctx.translate(-centerX, -centerY);\n            }\n            ctx.drawImage(image, sx, sy, spriteWidth, spriteHeight, position.x, position.y, spriteWidth * scale, spriteHeight * scale);\n            ctx.restore();\n            return;\n        }\n        const rowCount = definition.indexes.length;\n        const columnCount = Math.max(0, ...definition.indexes.map((row)=>row.length));\n        const spriteWidth = columnCount * frameWidth;\n        const spriteHeight = rowCount * frameHeight;\n        ctx.imageSmoothingEnabled = false;\n        ctx.save();\n        if (mirrorHorizontal || mirrorVertical) {\n            const centerX = position.x + spriteWidth * scale / 2;\n            const centerY = position.y + spriteHeight * scale / 2;\n            ctx.translate(centerX, centerY);\n            if (mirrorHorizontal) {\n                ctx.scale(-1, 1);\n            }\n            if (mirrorVertical) {\n                ctx.scale(1, -1);\n            }\n            ctx.translate(-centerX, -centerY);\n        }\n        const baked = this.spriteDefinitionBitmaps.get(spriteName);\n        if (baked) {\n            ctx.drawImage(baked.bitmap, position.x, position.y, baked.size.x * scale, baked.size.y * scale);\n            ctx.restore();\n            return;\n        }\n        this.bakeSpriteDefinitionBitmap(spriteName, definition, sheet);\n        const bakedAfter = this.spriteDefinitionBitmaps.get(spriteName);\n        if (bakedAfter) {\n            ctx.drawImage(bakedAfter.bitmap, position.x, position.y, bakedAfter.size.x * scale, bakedAfter.size.y * scale);\n            ctx.restore();\n            return;\n        }\n        definition.indexes.forEach((row, rowIndex)=>{\n            row.forEach((index, colIndex)=>{\n                if (index === null || index < 0) return;\n                const sx = index % sheet.columns * frameWidth;\n                const sy = Math.floor(index / sheet.columns) * frameHeight;\n                ctx.drawImage(image, sx, sy, frameWidth, frameHeight, position.x + colIndex * frameWidth * scale, position.y + rowIndex * frameHeight * scale, frameWidth * scale, frameHeight * scale);\n            });\n        });\n        ctx.restore();\n    }\n    ensureSpriteNameAvailable(name) {\n        if (this.sprites.has(name) || this.spriteDefinitions.has(name)) {\n            throw new Error(`Sprite name \"${name}\" is already in use.`);\n        }\n    }\n    bakeSpriteDefinitionBitmap(name, definition, sheet) {\n        if (this.spriteDefinitionBitmaps.has(name)) return;\n        const rowCount = definition.indexes.length;\n        const columnCount = Math.max(0, ...definition.indexes.map((row)=>row.length));\n        if (rowCount <= 1 && columnCount <= 1) return;\n        const width = columnCount * sheet.frameWidth;\n        const height = rowCount * sheet.frameHeight;\n        const canvas = typeof OffscreenCanvas !== \"undefined\" ? new OffscreenCanvas(width, height) : document.createElement(\"canvas\");\n        if (!(canvas instanceof OffscreenCanvas)) {\n            canvas.width = width;\n            canvas.height = height;\n        }\n        const context = canvas.getContext(\"2d\");\n        if (!isDrawable2DContext(context)) return;\n        context.imageSmoothingEnabled = false;\n        definition.indexes.forEach((row, rowIndex)=>{\n            row.forEach((index, colIndex)=>{\n                if (index === null || index < 0) return;\n                const sx = index % sheet.columns * sheet.frameWidth;\n                const sy = Math.floor(index / sheet.columns) * sheet.frameHeight;\n                context.drawImage(sheet.image, sx, sy, sheet.frameWidth, sheet.frameHeight, colIndex * sheet.frameWidth, rowIndex * sheet.frameHeight, sheet.frameWidth, sheet.frameHeight);\n            });\n        });\n        if (canvas instanceof OffscreenCanvas && \"transferToImageBitmap\" in canvas) {\n            const bitmap = canvas.transferToImageBitmap();\n            this.spriteDefinitionBitmaps.set(name, {\n                bitmap,\n                size: new _index.Vector(width, height)\n            });\n            return;\n        }\n        this.spriteDefinitionBitmaps.set(name, {\n            bitmap: canvas,\n            size: new _index.Vector(width, height)\n        });\n    }\n    validateSpriteManifest(payload) {\n        if (!payload || typeof payload !== \"object\" || Array.isArray(payload)) {\n            throw new Error(\"Sprite manifest must be an object.\");\n        }\n        const sprites = payload.sprites;\n        if (!sprites || typeof sprites !== \"object\" || Array.isArray(sprites)) {\n            throw new Error(\"Sprite manifest must include a 'sprites' object.\");\n        }\n        return {\n            sprites\n        };\n    }\n    loadSpriteManifestEntry(name, entry) {\n        if (!entry || typeof entry !== \"object\") {\n            throw new Error(`Sprite \"${name}\" must be an object.`);\n        }\n        if (!entry.sheet || typeof entry.sheet !== \"string\") {\n            throw new Error(`Sprite \"${name}\" must define a sheet name.`);\n        }\n        const hasRect = !!entry.rect;\n        const hasGrid = !!entry.grid;\n        if (hasRect === hasGrid) {\n            throw new Error(`Sprite \"${name}\" must define exactly one of rect or grid.`);\n        }\n        if (entry.rect) {\n            var _entry_rect_mode;\n            const mode = (_entry_rect_mode = entry.rect.mode) !== null && _entry_rect_mode !== void 0 ? _entry_rect_mode : \"grid\";\n            if (mode !== \"grid\" && mode !== \"id\") {\n                throw new Error(`Sprite \"${name}\" rect mode must be \"grid\" or \"id\".`);\n            }\n            if (mode === \"grid\") {\n                const from = this.parseGridPoint(entry.rect.from, name, \"rect.from\");\n                const to = this.parseGridPoint(entry.rect.to, name, \"rect.to\");\n                this.defineSpriteFromSheet(name, entry.sheet, new _index.Vector(from[0], from[1]), new _index.Vector(to[0], to[1]));\n                return;\n            }\n            const fromId = this.parseTileId(entry.rect.from, name, \"rect.from\");\n            const toId = this.parseTileId(entry.rect.to, name, \"rect.to\");\n            const sheet = this.requireSpriteSheet(entry.sheet, name);\n            const from = this.tileIdToGrid(sheet.columns, fromId);\n            const to = this.tileIdToGrid(sheet.columns, toId);\n            this.defineSpriteFromSheet(name, entry.sheet, new _index.Vector(from[0], from[1]), new _index.Vector(to[0], to[1]));\n            return;\n        }\n        if (!entry.grid) return;\n        var _entry_grid_mode;\n        const mode = (_entry_grid_mode = entry.grid.mode) !== null && _entry_grid_mode !== void 0 ? _entry_grid_mode : \"id\";\n        if (mode !== \"grid\" && mode !== \"id\") {\n            throw new Error(`Sprite \"${name}\" grid mode must be \"grid\" or \"id\".`);\n        }\n        if (!Array.isArray(entry.grid.tiles)) {\n            throw new Error(`Sprite \"${name}\" grid tiles must be a 2D array.`);\n        }\n        if (mode === \"id\") {\n            const tiles = entry.grid.tiles.map((row, rowIndex)=>{\n                if (!Array.isArray(row)) {\n                    throw new Error(`Sprite \"${name}\" grid row ${rowIndex} must be an array.`);\n                }\n                return row.map((tile, colIndex)=>this.parseTileIdOrNull(tile, name, `grid.tiles[${rowIndex}][${colIndex}]`));\n            });\n            this.defineSpriteFromSheetIndexes(name, entry.sheet, tiles);\n            return;\n        }\n        const sheet = this.requireSpriteSheet(entry.sheet, name);\n        const tiles = entry.grid.tiles.map((row, rowIndex)=>{\n            if (!Array.isArray(row)) {\n                throw new Error(`Sprite \"${name}\" grid row ${rowIndex} must be an array.`);\n            }\n            return row.map((tile, colIndex)=>{\n                if (tile === null) return null;\n                const point = this.parseGridPoint(tile, name, `grid.tiles[${rowIndex}][${colIndex}]`);\n                return this.gridToTileId(sheet.columns, point[0], point[1]);\n            });\n        });\n        this.defineSpriteFromSheetIndexes(name, entry.sheet, tiles);\n    }\n    requireSpriteSheet(sheetName, spriteName) {\n        const sheet = this.spriteSheets.get(sheetName);\n        if (!sheet) {\n            throw new Error(`Sprite \"${spriteName}\" references missing spritesheet \"${sheetName}\".`);\n        }\n        return sheet;\n    }\n    parseGridPoint(value, spriteName, fieldName) {\n        if (Array.isArray(value) && value.length === 2 && typeof value[0] === \"number\" && typeof value[1] === \"number\") {\n            return [\n                value[0],\n                value[1]\n            ];\n        }\n        throw new Error(`Sprite \"${spriteName}\" ${fieldName} must be [col, row].`);\n    }\n    parseTileId(value, spriteName, fieldName) {\n        if (typeof value === \"number\") {\n            return value;\n        }\n        throw new Error(`Sprite \"${spriteName}\" ${fieldName} must be a tile id.`);\n    }\n    parseTileIdOrNull(value, spriteName, fieldName) {\n        if (value === null) return null;\n        if (typeof value === \"number\") return value;\n        throw new Error(`Sprite \"${spriteName}\" ${fieldName} must be a tile id.`);\n    }\n    tileIdToGrid(columns, id) {\n        return [\n            id % columns,\n            Math.floor(id / columns)\n        ];\n    }\n    gridToTileId(columns, col, row) {\n        return row * columns + col;\n    }\n    /**\n     * Render the sprite frame by its index in the sheet\n     * Indexes grow from left to right, top to bottom\n     */ drawSpriteFrame(ctx, sheetName, index, position, scale = 1, mirrorHorizontal = false, mirrorVertical = false) {\n        const sheet = this.spriteSheets.get(sheetName);\n        if (!sheet) return;\n        const { image, frameWidth, frameHeight, columns } = sheet;\n        const sx = index % columns * frameWidth;\n        const sy = Math.floor(index / columns) * frameHeight;\n        ctx.imageSmoothingEnabled = false; // nearest neighbor\n        // Save the current context state\n        ctx.save();\n        // Apply transformations if needed\n        if (mirrorHorizontal || mirrorVertical) {\n            // Calculate the center of the sprite\n            const centerX = position.x + frameWidth * scale / 2;\n            const centerY = position.y + frameHeight * scale / 2;\n            // Translate to the center of the sprite\n            ctx.translate(centerX, centerY);\n            // Apply mirroring\n            if (mirrorHorizontal) {\n                ctx.scale(-1, 1);\n            }\n            if (mirrorVertical) {\n                ctx.scale(1, -1);\n            }\n            // Translate back to the original position\n            ctx.translate(-centerX, -centerY);\n        }\n        // Draw the image with the applied transformations\n        ctx.drawImage(image, sx, sy, frameWidth, frameHeight, position.x, position.y, frameWidth * scale, frameHeight * scale);\n        // Restore the context state\n        ctx.restore();\n    }\n    /**\n     * Render a specific cell from the sprite sheet grid\n     */ drawSpriteGrid(ctx, sheetName, col, row, position, scale = 1) {\n        const sheet = this.spriteSheets.get(sheetName);\n        if (!sheet) return;\n        const { image, frameWidth, frameHeight } = sheet;\n        const sx = col * frameWidth;\n        const sy = row * frameHeight;\n        ctx.imageSmoothingEnabled = false;\n        ctx.drawImage(image, sx, sy, frameWidth, frameHeight, position.x, position.y, frameWidth * scale, frameHeight * scale);\n    }\n    constructor(){\n        _define_property(this, \"sprites\", new Map());\n        _define_property(this, \"spriteSheets\", new Map());\n        _define_property(this, \"spriteDefinitions\", new Map());\n        _define_property(this, \"spriteDefinitionBitmaps\", new Map());\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/CanvasController/Transformations.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get withRotation () {\n        return withRotation;\n    },\n    get withTranslation () {\n        return withTranslation;\n    }\n});\nconst withRotation = (angle, shape)=>{\n    return (ctx, position, size)=>{\n        ctx.save();\n        ctx.translate(position.x + size.x / 2, position.y + size.y / 2);\n        ctx.rotate(angle);\n        ctx.translate(-(position.x + size.x / 2), -(position.y + size.y / 2));\n        shape(ctx, position, size);\n        ctx.restore();\n    };\n};\nconst withTranslation = (translationVector, shape)=>{\n    return (ctx, position, size)=>{\n        ctx.save();\n        ctx.translate(translationVector.x, translationVector.y);\n        shape(ctx, position, size);\n        ctx.restore();\n    };\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/CanvasController/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get CanvasController () {\n        return CanvasController;\n    },\n    get ShapeDrawer () {\n        return ShapeDrawer;\n    },\n    get SpriteLibrary () {\n        return _SpriteLibrary.SpriteLibrary;\n    }\n});\nconst _SpriteLibrary = require(\"./SpriteLibrary.js\");\n_export_star(require(\"./Transformations.js\"), exports);\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}\nclass ShapeDrawer {\n    /**\n     * Executes drawing instructions with a temporary alpha multiplier.\n     */ withOpacity(opacity, draw) {\n        if (opacity >= 1) {\n            draw();\n            return;\n        }\n        this.context.save();\n        this.context.globalAlpha *= opacity;\n        draw();\n        this.context.restore();\n    }\n    /**\n     * Draws a rectangle on the canvas with configurable fill/stroke behavior.\n     *\n     * @param x - The x-coordinate of the rectangle's top-left corner\n     * @param y - The y-coordinate of the rectangle's top-left corner\n     * @param width - The width of the rectangle\n     * @param height - The height of the rectangle\n     * @param color - The color to use for filling or stroking (default: \"black\")\n     * @param filled - If true, fills the rectangle; if false, strokes the outline (default: true)\n     * @param opacity - Alpha multiplier applied while drawing (default: 1)\n     *\n     * @example\n     * // Draw a filled red rectangle\n     * shapeDrawer.drawRectangle(10, 10, 100, 50, \"red\", true);\n     *\n     * @example\n     * // Draw a blue rectangle outline\n     * shapeDrawer.drawRectangle(10, 10, 100, 50, \"blue\", false);\n     */ drawRectangle(x, y, width, height, color = \"black\", filled = true, opacity = 1) {\n        this.withOpacity(opacity, ()=>{\n            this.context.fillStyle = color;\n            if (filled) {\n                this.context.fillRect(x, y, width, height);\n            } else {\n                this.context.strokeStyle = color;\n                this.context.strokeRect(x, y, width, height);\n            }\n        });\n    }\n    /**\n     * Sets the default font family used for text rendering.\n     *\n     * @param font - The font family name to use as default (e.g., \"Arial\", \"Verdana\")\n     *\n     * @example\n     * // Set default font to Arial\n     * shapeDrawer.setDefaultFont(\"Arial\");\n     */ setDefaultFont(font) {\n        this.defaultFont = font;\n    }\n    getDefaultFont() {\n        return this.defaultFont;\n    }\n    /**\n     * Draws a straight line between two points with customizable appearance.\n     *\n     * @param start - The starting point of the line as a Vector\n     * @param end - The ending point of the line as a Vector\n     * @param width - The thickness of the line in pixels\n     * @param color - The color of the line (default: \"red\")\n     * @param lineCap - The style of the line endings (default: \"round\")\n     *\n     * @example\n     * // Draw a thick blue line with square caps\n     * shapeDrawer.drawLine(new Vector(10, 10), new Vector(100, 50), 5, \"blue\", \"square\");\n     *\n     * @example\n     * // Draw a thin red line with default round caps\n     * shapeDrawer.drawLine(new Vector(0, 0), new Vector(100, 100), 1);\n     */ drawLine(start, end, width, color = \"red\", lineCap = \"round\") {\n        this.context.beginPath();\n        this.context.lineWidth = width;\n        this.context.strokeStyle = color;\n        this.context.lineCap = lineCap;\n        this.context.moveTo(start.x, start.y);\n        this.context.lineTo(end.x, end.y);\n        this.context.stroke();\n        this.context.closePath();\n    }\n    /**\n     * Draws a circle with customizable appearance and optional center dot.\n     *\n     * @param x - The x-coordinate of the circle's center\n     * @param y - The y-coordinate of the circle's center\n     * @param radius - The radius of the circle in pixels\n     * @param color - The color to use for filling or stroking the circle (default: \"black\")\n     * @param filled - If true, fills the circle; if false, strokes the outline (default: true)\n     * @param centerDot - If true, draws a small dot at the center of the circle (default: true)\n     * @param centerDotColor - The color of the center dot (default: \"red\")\n     * @param centerDotRadius - The radius of the center dot in pixels (default: 2)\n     *\n     * @example\n     * // Draw a filled blue circle with default center dot\n     * shapeDrawer.drawCircle(100, 100, 50, \"blue\", true);\n     *\n     * @example\n     * // Draw a red circle outline with no center dot\n     * shapeDrawer.drawCircle(100, 100, 50, \"red\");\n     *\n     * @example\n     * // Draw a green circle with custom center dot\n     * shapeDrawer.drawCircle(100, 100, 50, \"green\", true, true, \"yellow\", 3);\n     */ drawCircle(x, y, radius, color = \"black\", filled = true, centerDot = true, centerDotColor = \"red\", centerDotRadius = 2) {\n        this.context.beginPath();\n        this.context.arc(x, y, radius, 0, Math.PI * 2);\n        if (filled) {\n            this.context.fillStyle = color;\n            this.context.fill();\n        } else {\n            this.context.strokeStyle = color;\n            this.context.stroke();\n        }\n        if (centerDot) {\n            this.context.beginPath();\n            this.context.arc(x, y, centerDotRadius, 0, Math.PI * 2);\n            this.context.fillStyle = centerDotColor;\n            this.context.fill();\n        }\n        this.context.closePath();\n    }\n    /**\n     * Renders text at the specified position with customizable styling.\n     *\n     * @param text - The text string to render\n     * @param x - The x-coordinate of the text position\n     * @param y - The y-coordinate of the text position\n     * @param color - The color of the text (default: \"black\")\n     * @param size - The font size in pixels (default: \"16px\")\n     * @param align - The text alignment (default: \"center\")\n     * @param font - Optional font family to override the default font\n     * @param options - Optional rendering options (e.g. text wrapping)\n     *\n     * @example\n     * // Draw centered black text with default font\n     * shapeDrawer.drawText(\"Hello\", 100, 50);\n     *\n     * @example\n     * // Draw right-aligned red text with custom font\n     * shapeDrawer.drawText(\"World\", 100, 50, \"red\", \"20px\", \"right\", \"Verdana\");\n     *\n     * @example\n     * // Draw left-aligned text with default font but custom size\n     * shapeDrawer.drawText(\"Welcome\", 100, 50, \"black\", \"12px\", \"left\");\n     */ drawText(text, x, y, color = \"black\", size = \"16px\", align = \"center\", font, options) {\n        const fontToUse = font !== null && font !== void 0 ? font : this.defaultFont;\n        const previousBaseline = this.context.textBaseline;\n        this.context.fillStyle = color;\n        this.context.font = `${size} ${fontToUse}`;\n        this.context.textAlign = align;\n        if (options === null || options === void 0 ? void 0 : options.baseline) {\n            this.context.textBaseline = options.baseline;\n        }\n        const maxWidth = options === null || options === void 0 ? void 0 : options.wrapText;\n        if (maxWidth === undefined) {\n            this.context.fillText(text, x, y);\n            if (options === null || options === void 0 ? void 0 : options.baseline) this.context.textBaseline = previousBaseline;\n            return;\n        }\n        const lines = this.wrapText(this.context, text, maxWidth);\n        var _options_lineHeight;\n        const lineHeight = (_options_lineHeight = options === null || options === void 0 ? void 0 : options.lineHeight) !== null && _options_lineHeight !== void 0 ? _options_lineHeight : this.defaultLineHeightPx(size);\n        var _options_maxLines;\n        const maxLines = (_options_maxLines = options === null || options === void 0 ? void 0 : options.maxLines) !== null && _options_maxLines !== void 0 ? _options_maxLines : Number.POSITIVE_INFINITY;\n        for(let i = 0; i < Math.min(lines.length, maxLines); i += 1){\n            var _lines_i;\n            this.context.fillText((_lines_i = lines[i]) !== null && _lines_i !== void 0 ? _lines_i : \"\", x, y + i * lineHeight);\n        }\n        if (options === null || options === void 0 ? void 0 : options.baseline) this.context.textBaseline = previousBaseline;\n    }\n    defaultLineHeightPx(size) {\n        const match = /(-?\\d+(\\.\\d+)?)px/.exec(size);\n        const parsed = match ? Number(match[1]) : 16;\n        const fontPx = Number.isFinite(parsed) ? parsed : 16;\n        return Math.ceil(fontPx * 1.3);\n    }\n    wrapText(ctx, text, maxWidth) {\n        if (maxWidth <= 0) return [];\n        const hardLines = text.split(\"\\n\");\n        const out = [];\n        for (const hardLine of hardLines){\n            const line = hardLine.trimEnd();\n            if (line.trim().length === 0) {\n                out.push(\"\");\n                continue;\n            }\n            const words = line.split(/\\s+/g);\n            let current = \"\";\n            for (const word of words){\n                const candidate = current.length === 0 ? word : `${current} ${word}`;\n                if (ctx.measureText(candidate).width <= maxWidth) {\n                    current = candidate;\n                    continue;\n                }\n                if (current.length > 0) {\n                    out.push(current);\n                    current = word;\n                    continue;\n                }\n                // Word is longer than maxWidth; hard-break it.\n                let chunk = \"\";\n                for (const char of word){\n                    const next = `${chunk}${char}`;\n                    if (ctx.measureText(next).width <= maxWidth) {\n                        chunk = next;\n                        continue;\n                    }\n                    if (chunk.length > 0) {\n                        out.push(chunk);\n                        chunk = char;\n                    }\n                }\n                if (chunk.length > 0) out.push(chunk);\n                current = \"\";\n            }\n            if (current.length > 0) out.push(current);\n        }\n        return out;\n    }\n    /**\n     * Clears the canvas and fills it with a solid background color.\n     *\n     * @param color - The color to fill the canvas with (default: \"white\")\n     *\n     * @example\n     * // Clear canvas with default white background\n     * shapeDrawer.drawBackground();\n     *\n     * @example\n     * // Clear canvas with custom gray background\n     * shapeDrawer.drawBackground(\"#f0f0f0\");\n     */ drawBackground(color = \"white\") {\n        this.context.fillStyle = color;\n        this.context.fillRect(0, 0, this.context.canvas.width, this.context.canvas.height);\n    }\n    /**\n     * Executes drawing operations within a clipped rectangular region.\n     *\n     * This method saves the current canvas state, creates a clipping region,\n     * executes the provided drawing function, and then restores the original state.\n     *\n     * @param x - The x-coordinate of the clipping rectangle's top-left corner\n     * @param y - The y-coordinate of the clipping rectangle's top-left corner\n     * @param width - The width of the clipping rectangle\n     * @param height - The height of the clipping rectangle\n     * @param draw - A function containing the drawing operations to perform within the clipped region\n     *\n     * @example\n     * // Draw shapes only within a specific region\n     * shapeDrawer.withClippingRect(50, 50, 200, 200, () => {\n     *   shapeDrawer.drawCircle(100, 100, 50, \"blue\");\n     *   shapeDrawer.drawText(\"Clipped\", 100, 100);\n     * });\n     */ withClippingRect(x, y, width, height, draw) {\n        this.context.save();\n        this.context.beginPath();\n        this.context.rect(x, y, width, height);\n        this.context.clip();\n        draw();\n        this.context.restore();\n    }\n    /**\n     * Executes drawing operations with a temporary rotation around a pivot point.\n     *\n     * This method saves the current canvas state, applies the rotation transform,\n     * executes the provided drawing function, and then restores the original state.\n     *\n     * @param x - The x-coordinate of the rotation pivot\n     * @param y - The y-coordinate of the rotation pivot\n     * @param angle - Rotation angle in radians\n     * @param draw - A function containing the drawing operations to perform while rotated\n     */ withRotation(x, y, angle, draw) {\n        if (angle === 0) {\n            draw();\n            return;\n        }\n        this.context.save();\n        this.context.translate(x, y);\n        this.context.rotate(angle);\n        this.context.translate(-x, -y);\n        draw();\n        this.context.restore();\n    }\n    /**\n     * Draws a sprite from the sprite library onto the canvas.\n     *\n     * @param spriteName - The name of the sprite to draw (must be loaded in the SpriteLibrary)\n     * @param x - The x-coordinate of the top-left corner where the sprite will be drawn\n     * @param y - The y-coordinate of the top-left corner where the sprite will be drawn\n     * @param width - The width to scale the sprite to\n     * @param height - The height to scale the sprite to\n     *\n     * @example\n     * // Draw a sprite named \"player\" at position (100, 100) with size 50x50\n     * shapeDrawer.drawSprite(\"player\", 100, 100, 50, 50);\n     *\n     * @example\n     * // Draw a sprite with different dimensions\n     * shapeDrawer.drawSprite(\"enemy\", 200, 200, 30, 40);\n     */ drawSprite(spriteName, x, y, width, height) {\n        const sprite = this.spriteLibrary.getSprite(spriteName);\n        if (sprite) {\n            this.context.drawImage(sprite, x, y, width, height);\n        }\n    }\n    /**\n     * Loads a font (local URL or Google Fonts stylesheet) and registers it for use.\n     *\n     * This method handles both local font files and Google Fonts URLs. For Google Fonts,\n     * it injects the stylesheet and waits for the font to be available. For local fonts,\n     * it creates a new FontFace object and adds it to the document's font collection.\n     *\n     * @param fontName - The name of the font to load (must match the font family name)\n     * @param url - The URL of the font file or Google Fonts stylesheet\n     * @returns A Promise that resolves when the font is successfully loaded and available\n     *\n     * @example\n     * // Load a Google Font\n     * await shapeDrawer.loadFont(\"Roboto\", \"https://fonts.googleapis.com/css2?family=Roboto\");\n     *\n     * @example\n     * // Load a local font file\n     * await shapeDrawer.loadFont(\"CustomFont\", \"/fonts/custom-font.woff2\");\n     *\n     * @throws Will throw an error if the font fails to load\n     */ async loadFont(fontName, url) {\n        // Google Fonts endpoints return CSS, so inject the stylesheet first and wait for the font to be available\n        if (url.includes(\"fonts.googleapis.com\")) {\n            await new Promise((resolve, reject)=>{\n                const link = document.createElement(\"link\");\n                link.rel = \"stylesheet\";\n                link.href = url;\n                link.onload = ()=>{\n                    document.fonts.load(`1em ${fontName}`).then(()=>resolve());\n                };\n                link.onerror = ()=>reject(new Error(`Failed to load font stylesheet: ${fontName}`));\n                document.head.appendChild(link);\n            });\n            return;\n        }\n        const newFont = new FontFace(fontName, `url(${url})`);\n        const loadedFont = await newFont.load();\n        document.fonts.add(loadedFont);\n        await document.fonts.load(`1em ${fontName}`);\n    }\n    constructor(context, spriteLibrary){\n        _define_property(this, \"context\", void 0);\n        _define_property(this, \"spriteLibrary\", void 0);\n        _define_property(this, \"defaultFont\", \"Arial\");\n        this.context = context;\n        this.spriteLibrary = spriteLibrary;\n    }\n}\nclass CanvasController {\n    clearCanvas() {\n        this.context.clearRect(0, 0, this.canvas.width, this.canvas.height);\n    }\n    getCanvas() {\n        return this.canvas;\n    }\n    getContext() {\n        return this.context;\n    }\n    getShapeDrawer() {\n        return this.shapeDrawer;\n    }\n    getSpriteLibrary() {\n        return this.spriteLibrary;\n    }\n    applyTranslation(translationVector) {\n        this.context.translate(translationVector.x, translationVector.y);\n    }\n    reset() {\n        this.context.setTransform(1, 0, 0, 1, 0, 0);\n    }\n    applyRotation(angle) {\n        this.context.rotate(angle);\n    }\n    constructor(width, height){\n        _define_property(this, \"canvas\", void 0);\n        _define_property(this, \"context\", void 0);\n        _define_property(this, \"spriteLibrary\", new _SpriteLibrary.SpriteLibrary());\n        _define_property(this, \"shapeDrawer\", void 0);\n        this.canvas = document.createElement(\"canvas\");\n        this.canvas.width = width;\n        this.canvas.height = height;\n        this.context = this.canvas.getContext(\"2d\");\n        this.shapeDrawer = new ShapeDrawer(this.context, this.spriteLibrary);\n        const canvasContainer = document.getElementById(\"canvas-container\");\n        if (canvasContainer) {\n            canvasContainer.appendChild(this.canvas);\n        } else {\n            document.body.appendChild(this.canvas);\n        }\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Context/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get GameContext () {\n        return GameContext;\n    },\n    get MessageBus () {\n        return MessageBus;\n    }\n});\nconst _keyAccumulator = require(\"../Events/keyAccumulator.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass MessageBus {\n    subscribe(channel, handler) {\n        var _this_channels_get;\n        const handlers = (_this_channels_get = this.channels.get(channel)) !== null && _this_channels_get !== void 0 ? _this_channels_get : new Set();\n        handlers.add(handler);\n        this.channels.set(channel, handlers);\n        return ()=>{\n            this.unsubscribe(channel, handler);\n        };\n    }\n    unsubscribe(channel, handler) {\n        const handlers = this.channels.get(channel);\n        if (!handlers) {\n            return;\n        }\n        handlers.delete(handler);\n        if (handlers.size === 0) {\n            this.channels.delete(channel);\n        }\n    }\n    publish(channel, payload, sender) {\n        const handlers = this.channels.get(channel);\n        handlers === null || handlers === void 0 ? void 0 : handlers.forEach((handler)=>handler(payload, sender !== null && sender !== void 0 ? sender : null));\n    }\n    constructor(){\n        _define_property(this, \"channels\", new Map());\n    }\n}\nclass GameContext {\n    getGame() {\n        return this.options.game;\n    }\n    incrementFrameCount() {\n        this.frameCount++;\n    }\n    getFrameCount() {\n        return this.frameCount;\n    }\n    getTickCount() {\n        return this.tickCount;\n    }\n    getTickRate() {\n        return this.options.game.getTickRate();\n    }\n    incrementTickCount() {\n        this.tickCount++;\n    }\n    getCanvas() {\n        return this.options.canvas;\n    }\n    getSceneManager() {\n        return this.options.sceneManager;\n    }\n    getSoundManager() {\n        return this.options.soundManager;\n    }\n    getKeyAccumulator() {\n        return this.options.keyAccumulator;\n    }\n    getCurrentScene() {\n        return this.options.sceneManager.getCurrentScene();\n    }\n    getActiveScenes() {\n        return this.options.sceneManager.getActiveScenes();\n    }\n    getAllScenes() {\n        return this.options.sceneManager.getAllScenes();\n    }\n    setCurrentScene(name) {\n        this.options.sceneManager.setSceneAsCurrent(name);\n    }\n    pushScene(name) {\n        const scene = this.options.sceneManager.getScene(name);\n        if (scene) {\n            this.options.sceneManager.pushSceneToActive(scene);\n        }\n    }\n    popScene() {\n        return this.options.sceneManager.popSceneFromActive();\n    }\n    transitionToScene(name, transition, mode = \"replace\") {\n        return this.options.sceneManager.transitionToScene(name, transition, mode);\n    }\n    sendMessage(channel, payload, sender) {\n        this.messageBus.publish(channel, payload, sender);\n    }\n    getPressedKeys() {\n        return this.options.keyAccumulator.getPressedKeys();\n    }\n    isKeyPressed(key) {\n        return this.options.keyAccumulator.isPressed(key);\n    }\n    subscribeToMessage(channel, handler) {\n        return this.messageBus.subscribe(channel, handler);\n    }\n    constructor(options){\n        _define_property(this, \"options\", void 0);\n        _define_property(this, \"messageBus\", new MessageBus());\n        _define_property(this, \"frameCount\", 0);\n        _define_property(this, \"tickCount\", 0);\n        this.options = options;\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Events/decorators.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get KEY_TICK_HANDLERS () {\n        return KEY_TICK_HANDLERS;\n    },\n    get composeMethodDecorators () {\n        return composeMethodDecorators;\n    },\n    get grabbable () {\n        return grabbable;\n    },\n    get onChildrenEvents () {\n        return onChildrenEvents;\n    },\n    get onClick () {\n        return onClick;\n    },\n    get onClickAnywhere () {\n        return onClickAnywhere;\n    },\n    get onHover () {\n        return onHover;\n    },\n    get onKeyComboHold () {\n        return onKeyComboHold;\n    },\n    get onKeyComboPressed () {\n        return onKeyComboPressed;\n    },\n    get onKeyHold () {\n        return onKeyHold;\n    },\n    get onKeyPressed () {\n        return onKeyPressed;\n    },\n    get onMouseMoved () {\n        return onMouseMoved;\n    },\n    get onMouseRelease () {\n        return onMouseRelease;\n    },\n    get onMouseWheel () {\n        return onMouseWheel;\n    },\n    get onMouseWheelOverHitbox () {\n        return onMouseWheelOverHitbox;\n    },\n    get onStopHovering () {\n        return onStopHovering;\n    },\n    get solidTo () {\n        return solidTo;\n    }\n});\nconst _index = require(\"../Lib/Vector/index.js\");\nconst KEY_TICK_HANDLERS = Symbol.for(\"sliver-engine.keyTickHandlers\");\nfunction registerKeyTickHandler(target, handler) {\n    const record = target;\n    const existing = record[KEY_TICK_HANDLERS];\n    if (existing) {\n        existing.push(handler);\n    } else {\n        record[KEY_TICK_HANDLERS] = [\n            handler\n        ];\n    }\n}\nconst matchesKeyState = (gameObject, keys)=>{\n    var _gameObject_getContext;\n    const context = (_gameObject_getContext = gameObject.getContext) === null || _gameObject_getContext === void 0 ? void 0 : _gameObject_getContext.call(gameObject);\n    if (!context) return false;\n    return keys.every((key)=>context.isKeyPressed(key));\n};\nconst composeMethodDecorators = (...decorators)=>{\n    return (target, propertyKey, descriptor)=>{\n        return decorators.reduceRight((desc, dec)=>{\n            const out = dec(target, propertyKey, desc);\n            return out !== null && out !== void 0 ? out : desc;\n        }, descriptor);\n    };\n};\nconst solidTo = (predicate)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(other) {\n            if (!predicate(this, other)) {\n                return false;\n            }\n            return original.call(this, other);\n        };\n        return descriptor;\n    };\n};\nconst onClick = (handler)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(event, ...args) {\n            if (event.type === \"mouseButtonPressed\") {\n                const hitboxes = this.getHitboxes();\n                if (hitboxes.some((hitbox)=>hitbox.intersectsWithPoint(new _index.Vector(event.x, event.y)))) {\n                    handler(this, event);\n                }\n            }\n            return original.call(this, event, ...args);\n        };\n        return descriptor;\n    };\n};\nconst onClickAnywhere = (handler)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(event, ...args) {\n            if (event.type === \"mouseButtonPressed\") {\n                handler(this, event);\n            }\n            return original.call(this, event, ...args);\n        };\n        return descriptor;\n    };\n};\nconst onMouseRelease = (handler)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(event, ...args) {\n            if (event.type === \"mouseButtonReleased\") {\n                const hitboxes = this.getHitboxes();\n                if (hitboxes.some((hitbox)=>hitbox.intersectsWithPoint(new _index.Vector(event.x, event.y)))) {\n                    handler(this, event);\n                }\n            }\n            return original.call(this, event, ...args);\n        };\n        return descriptor;\n    };\n};\nconst onMouseWheel = (handler)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(event, ...args) {\n            if (event.type === \"mouseWheelScrolled\") {\n                handler(this, event);\n            }\n            return original.call(this, event, ...args);\n        };\n        return descriptor;\n    };\n};\nconst grabbable = ()=>{\n    const STATE = Symbol.for(\"sliver-engine.grabbableState\");\n    const getState = (obj)=>{\n        const record = obj;\n        let state = record[STATE];\n        if (!state) {\n            state = {\n                offset: _index.Vector.zero(),\n                samples: [],\n                lastTickPosition: null,\n                stationaryTicks: 0,\n                throwSpeed: _index.Vector.zero(),\n                lastMouseMoveTimeMs: null\n            };\n            record[STATE] = state;\n        }\n        return state;\n    };\n    const maxSamples = 10;\n    const maxThrowSpeed = 30;\n    const stationaryEpsilonSq = 1e-6;\n    const stationaryTicksToZero = 2;\n    return function(target, _propertyKey, descriptor) {\n        registerKeyTickHandler(target, (obj)=>{\n            if (!obj.beingGrabbed) return;\n            const state = getState(obj);\n            const current = obj.getScenePosition();\n            if (!state.lastTickPosition) {\n                state.lastTickPosition = current.clone();\n                state.samples = [];\n                state.stationaryTicks = 0;\n                state.throwSpeed = _index.Vector.zero();\n                return;\n            }\n            const delta = current.toSubtracted(state.lastTickPosition);\n            state.lastTickPosition = current.clone();\n            if (delta.squaredMagnitude() <= stationaryEpsilonSq) {\n                state.stationaryTicks++;\n                if (state.stationaryTicks >= stationaryTicksToZero) {\n                    state.samples = [];\n                    state.throwSpeed = _index.Vector.zero();\n                }\n                return;\n            }\n            state.stationaryTicks = 0;\n            state.samples.unshift(delta);\n            state.samples.length = maxSamples;\n            const avg = state.samples.reduce((acc, v)=>acc.add(v), _index.Vector.zero());\n            if (state.samples.length > 0) {\n                avg.multiply(1 / state.samples.length);\n            }\n            if (avg.magnitude() > maxThrowSpeed) {\n                avg.normalize().multiply(maxThrowSpeed);\n            }\n            state.throwSpeed = avg;\n        });\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(event, ...args) {\n            var _this_scene;\n            const state = getState(this);\n            var _this_scene_getVisualOffset;\n            const sceneOffset = (_this_scene_getVisualOffset = (_this_scene = this.scene) === null || _this_scene === void 0 ? void 0 : _this_scene.getVisualOffset()) !== null && _this_scene_getVisualOffset !== void 0 ? _this_scene_getVisualOffset : _index.Vector.zero();\n            const mouseToScene = (x, y)=>new _index.Vector(x, y).toSubtracted(sceneOffset);\n            const pointerInside = (x, y)=>{\n                const hitboxes = this.getHitboxes();\n                return hitboxes.some((hitbox)=>hitbox.intersectsWithPoint(new _index.Vector(x, y)));\n            };\n            if (event.type === \"mouseButtonPressed\") {\n                if (pointerInside(event.x, event.y)) {\n                    this.beingGrabbed = true;\n                    this.speed = _index.Vector.zero();\n                    this.angularVelocity = 0;\n                    const mouseScenePos = mouseToScene(event.x, event.y);\n                    state.offset = this.getScenePosition().toSubtracted(mouseScenePos);\n                    state.samples = [];\n                    state.lastTickPosition = this.getScenePosition();\n                    state.stationaryTicks = 0;\n                    state.throwSpeed = _index.Vector.zero();\n                    state.lastMouseMoveTimeMs = performance.now();\n                    event.stopPropagation = true;\n                }\n            }\n            if (event.type === \"mouseMoved\") {\n                if (this.beingGrabbed) {\n                    state.lastMouseMoveTimeMs = performance.now();\n                    const mouseScenePos = mouseToScene(event.x, event.y);\n                    this.setPosition(mouseScenePos.toAdded(state.offset));\n                    this.speed = _index.Vector.zero();\n                    event.stopPropagation = true;\n                } else if (pointerInside(event.x, event.y)) {\n                    this.hovering = true;\n                } else {\n                    this.hovering = false;\n                }\n            }\n            if (event.type === \"mouseButtonReleased\") {\n                if (this.beingGrabbed) {\n                    var _this_getContext;\n                    this.beingGrabbed = false;\n                    var _this_getContext_getTickRate;\n                    const tickRate = (_this_getContext_getTickRate = (_this_getContext = this.getContext()) === null || _this_getContext === void 0 ? void 0 : _this_getContext.getTickRate()) !== null && _this_getContext_getTickRate !== void 0 ? _this_getContext_getTickRate : 60;\n                    const tickIntervalMs = 1000 / tickRate;\n                    const now = performance.now();\n                    const timeSinceMove = state.lastMouseMoveTimeMs === null ? Number.POSITIVE_INFINITY : now - state.lastMouseMoveTimeMs;\n                    // If the pointer has been still for at least ~1 tick before release,\n                    // drop the throw velocity to avoid \"stale\" inertia.\n                    // state.throwSpeed stores recent drag displacement per tick, so\n                    // convert it into velocity units before handing it to physics.\n                    this.speed = timeSinceMove >= tickIntervalMs ? _index.Vector.zero() : state.throwSpeed.clone().multiply(tickRate);\n                    event.stopPropagation = true;\n                }\n            }\n            return original.call(this, event, ...args);\n        };\n        return descriptor;\n    };\n};\nconst onMouseMoved = (handler)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(event, ...args) {\n            if (event.type === \"mouseMoved\") {\n                const hitboxes = this.getHitboxes();\n                if (hitboxes.some((hitbox)=>hitbox.intersectsWithPoint(new _index.Vector(event.x, event.y)))) {\n                    handler(this, event);\n                }\n            }\n            return original.call(this, event, ...args);\n        };\n        return descriptor;\n    };\n};\nconst onMouseWheelOverHitbox = (handler)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(event, ...args) {\n            if (event.type === \"mouseWheelScrolled\") {\n                const hitboxes = this.getHitboxes();\n                if (hitboxes.some((hitbox)=>hitbox.intersectsWithPoint(new _index.Vector(event.x, event.y)))) {\n                    handler(this, event);\n                }\n            }\n            return original.call(this, event, ...args);\n        };\n        return descriptor;\n    };\n};\nfunction onKeyPressed(key, handler) {\n    return onKeyComboPressed([\n        key\n    ], handler);\n}\nfunction onKeyHold(key, handler) {\n    return onKeyComboHold([\n        key\n    ], handler);\n}\nfunction onKeyComboPressed(keys, handler) {\n    const wasPressedMap = new WeakMap();\n    return function(target, _propertyKey, descriptor) {\n        // Register tick handler\n        registerKeyTickHandler(target, (gameObj)=>{\n            const pressed = matchesKeyState(gameObj, keys);\n            var _wasPressedMap_get;\n            const wasPressed = (_wasPressedMap_get = wasPressedMap.get(gameObj)) !== null && _wasPressedMap_get !== void 0 ? _wasPressedMap_get : false;\n            if (pressed && !wasPressed) {\n                var _keys_;\n                const lastKey = (_keys_ = keys[keys.length - 1]) !== null && _keys_ !== void 0 ? _keys_ : \"\";\n                handler(gameObj, {\n                    type: \"keyPressed\",\n                    key: lastKey\n                });\n            }\n            wasPressedMap.set(gameObj, pressed);\n        });\n        // Wrap handleEvent so it still receives keyPressed while the tick handler handles the held state\n        const original = descriptor.value;\n        if (original) {\n            descriptor.value = function(event, ...args) {\n                return original.call(this, event, ...args);\n            };\n        }\n        return descriptor;\n    };\n}\nfunction onKeyComboHold(keys, handler) {\n    return function(target, _propertyKey, descriptor) {\n        // Register tick handler\n        registerKeyTickHandler(target, (gameObj)=>{\n            if (matchesKeyState(gameObj, keys)) {\n                var _keys_;\n                const lastKey = (_keys_ = keys[keys.length - 1]) !== null && _keys_ !== void 0 ? _keys_ : \"\";\n                handler(gameObj, {\n                    type: \"keyPressed\",\n                    key: lastKey\n                });\n            }\n        });\n        // Wrap handleEvent so it still receives keyPressed while the tick handler handles the held state\n        const original = descriptor.value;\n        if (original) {\n            descriptor.value = function(event, ...args) {\n                return original.call(this, event, ...args);\n            };\n        }\n        return descriptor;\n    };\n}\nfunction onChildrenEvents() {\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(event) {\n            const result = original.call(this, event);\n            if (this.children) {\n                for (const child of this.children){\n                    if (typeof child.handleEvent === \"function\") {\n                        child.handleEvent(event);\n                    }\n                }\n            }\n            return result;\n        };\n        return descriptor;\n    };\n}\nconst onHover = (handler)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(event, ...args) {\n            if (event.type === \"mouseMoved\") {\n                const hitboxes = this.getHitboxes();\n                const isHovering = hitboxes.some((hitbox)=>hitbox.intersectsWithPoint(new _index.Vector(event.x, event.y)));\n                const gameObject = this;\n                const objectWasHovering = !!gameObject.hovering;\n                if (isHovering && !objectWasHovering) {\n                    gameObject.hovering = true;\n                    handler(this, event);\n                }\n            }\n            return original.call(this, event, ...args);\n        };\n        return descriptor;\n    };\n};\nconst onStopHovering = (handler)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(event, ...args) {\n            if (event.type === \"mouseMoved\") {\n                const hitboxes = this.getHitboxes();\n                const isHovering = hitboxes.some((hitbox)=>hitbox.intersectsWithPoint(new _index.Vector(event.x, event.y)));\n                const gameObject = this;\n                const objectWasHovering = !!gameObject.hovering;\n                if (!isHovering && objectWasHovering) {\n                    gameObject.hovering = false;\n                    handler(this, event);\n                }\n            }\n            return original.call(this, event, ...args);\n        };\n        return descriptor;\n    };\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Events/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get GameEventsdispatcher () {\n        return GameEventsdispatcher;\n    },\n    get KeyAccumulator () {\n        return _keyAccumulator.KeyAccumulator;\n    }\n});\nconst _keyAccumulator = require(\"./keyAccumulator.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass GameEventsdispatcher {\n    dispatchEvent(event) {\n        this.game.handleEvent(event);\n    }\n    constructor(game, keyAccumulator){\n        _define_property(this, \"game\", void 0);\n        _define_property(this, \"keyAccumulator\", void 0);\n        _define_property(this, \"canvasElement\", void 0);\n        this.game = game;\n        this.keyAccumulator = keyAccumulator;\n        this.canvasElement = game.canvas.getCanvas();\n        // Keyboard events\n        window.addEventListener(\"keydown\", (e)=>{\n            const event = {\n                type: \"keyPressed\",\n                key: e.key\n            };\n            this.keyAccumulator.handleKeyDown(e.key);\n            this.dispatchEvent(event);\n        });\n        window.addEventListener(\"keyup\", (e)=>{\n            const event = {\n                type: \"keyReleased\",\n                key: e.key\n            };\n            this.keyAccumulator.handleKeyUp(e.key);\n            this.dispatchEvent(event);\n        });\n        // Mouse events\n        this.canvasElement.addEventListener(\"mousemove\", (e)=>{\n            const rect = this.canvasElement.getBoundingClientRect();\n            const x = e.clientX - rect.left;\n            const y = e.clientY - rect.top;\n            const event = {\n                type: \"mouseMoved\",\n                x,\n                y\n            };\n            this.dispatchEvent(event);\n        });\n        this.canvasElement.addEventListener(\"mousedown\", (e)=>{\n            const rect = this.canvasElement.getBoundingClientRect();\n            const x = e.clientX - rect.left;\n            const y = e.clientY - rect.top;\n            const event = {\n                type: \"mouseButtonPressed\",\n                button: e.button,\n                x,\n                y\n            };\n            this.dispatchEvent(event);\n        });\n        this.canvasElement.addEventListener(\"mouseup\", (e)=>{\n            const rect = this.canvasElement.getBoundingClientRect();\n            const x = e.clientX - rect.left;\n            const y = e.clientY - rect.top;\n            const event = {\n                type: \"mouseButtonReleased\",\n                button: e.button,\n                x,\n                y\n            };\n            this.dispatchEvent(event);\n        });\n        this.canvasElement.addEventListener(\"wheel\", (e)=>{\n            e.preventDefault();\n            const rect = this.canvasElement.getBoundingClientRect();\n            const x = e.clientX - rect.left;\n            const y = e.clientY - rect.top;\n            const event = {\n                type: \"mouseWheelScrolled\",\n                deltaX: e.deltaX,\n                deltaY: e.deltaY,\n                x,\n                y\n            };\n            this.dispatchEvent(event);\n        }, {\n            passive: false\n        });\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Events/keyAccumulator.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"KeyAccumulator\", {\n    enumerable: true,\n    get: function() {\n        return KeyAccumulator;\n    }\n});\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass KeyAccumulator {\n    handleKeyDown(key) {\n        this.pressedKeys.add(key);\n    }\n    handleKeyUp(key) {\n        this.pressedKeys.delete(key);\n    }\n    getPressedKeys() {\n        return Array.from(this.pressedKeys);\n    }\n    isPressed(key) {\n        return this.pressedKeys.has(key);\n    }\n    constructor(){\n        _define_property(this, \"pressedKeys\", new Set());\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Game/Saves/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"GameSaves\", {\n    enumerable: true,\n    get: function() {\n        return GameSaves;\n    }\n});\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nconst getLocalStorage = ()=>{\n    if (typeof window === \"undefined\") {\n        return null;\n    }\n    try {\n        var _window_localStorage;\n        return (_window_localStorage = window.localStorage) !== null && _window_localStorage !== void 0 ? _window_localStorage : null;\n    } catch  {\n        return null;\n    }\n};\nconst encodeUtf8ToBase64 = (value)=>{\n    // Prefer Node/Bun (tests, tooling) if available.\n    // eslint-disable-next-line @typescript-eslint/no-explicit-any\n    const maybeBuffer = globalThis.Buffer;\n    if (maybeBuffer) {\n        return maybeBuffer.from(value, \"utf-8\").toString(\"base64\");\n    }\n    if (typeof TextEncoder !== \"undefined\" && typeof btoa !== \"undefined\") {\n        const bytes = new TextEncoder().encode(value);\n        let binary = \"\";\n        const chunkSize = 0x8000;\n        for(let i = 0; i < bytes.length; i += chunkSize){\n            const chunk = bytes.subarray(i, i + chunkSize);\n            binary += String.fromCharCode(...chunk);\n        }\n        return btoa(binary);\n    }\n    if (typeof btoa !== \"undefined\") {\n        return btoa(unescape(encodeURIComponent(value)));\n    }\n    throw new Error(\"Base64 encoding is not available in this environment.\");\n};\nconst decodeBase64ToUtf8 = (base64)=>{\n    // Prefer Node/Bun (tests, tooling) if available.\n    // eslint-disable-next-line @typescript-eslint/no-explicit-any\n    const maybeBuffer = globalThis.Buffer;\n    if (maybeBuffer) {\n        return maybeBuffer.from(base64, \"base64\").toString(\"utf-8\");\n    }\n    if (typeof atob !== \"undefined\") {\n        const binary = atob(base64);\n        if (typeof TextDecoder !== \"undefined\") {\n            const bytes = new Uint8Array(binary.length);\n            for(let i = 0; i < binary.length; i++){\n                bytes[i] = binary.charCodeAt(i);\n            }\n            return new TextDecoder().decode(bytes);\n        }\n        return decodeURIComponent(escape(binary));\n    }\n    throw new Error(\"Base64 decoding is not available in this environment.\");\n};\nconst encodeJsonToBase64 = (data)=>{\n    return encodeUtf8ToBase64(JSON.stringify(data));\n};\nconst decodeBase64ToJson = (base64)=>{\n    return JSON.parse(decodeBase64ToUtf8(base64));\n};\nconst generateSaveId = ()=>{\n    if (typeof crypto !== \"undefined\" && \"randomUUID\" in crypto) {\n        return crypto.randomUUID();\n    }\n    return `${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 10)}`;\n};\nclass GameSaves {\n    onLoadSaveFile(callback) {\n        this.onLoadSaveFileCallback = callback;\n        return this;\n    }\n    create(data, options) {\n        var _options_id;\n        const id = (_options_id = options === null || options === void 0 ? void 0 : options.id) !== null && _options_id !== void 0 ? _options_id : generateSaveId();\n        this.write(id, data, {\n            label: options === null || options === void 0 ? void 0 : options.label,\n            createIfMissing: true\n        });\n        return id;\n    }\n    write(id, data, options) {\n        const storage = getLocalStorage();\n        if (!storage) {\n            return;\n        }\n        const now = Date.now();\n        const index = this.readIndex(storage);\n        const existing = index.find((s)=>s.id === id);\n        if (!existing && (options === null || options === void 0 ? void 0 : options.createIfMissing) === false) {\n            return;\n        }\n        var _existing_createdAt;\n        const createdAt = (_existing_createdAt = existing === null || existing === void 0 ? void 0 : existing.createdAt) !== null && _existing_createdAt !== void 0 ? _existing_createdAt : now;\n        var _options_label;\n        const entry = {\n            id,\n            createdAt,\n            updatedAt: now,\n            label: (_options_label = options === null || options === void 0 ? void 0 : options.label) !== null && _options_label !== void 0 ? _options_label : existing === null || existing === void 0 ? void 0 : existing.label\n        };\n        const payload = encodeJsonToBase64(data);\n        storage.setItem(this.saveKey(id), payload);\n        const nextIndex = [\n            ...index.filter((s)=>s.id !== id),\n            entry\n        ].sort((a, b)=>b.updatedAt - a.updatedAt);\n        this.writeIndex(storage, nextIndex);\n    }\n    read(id) {\n        const storage = getLocalStorage();\n        if (!storage) {\n            return null;\n        }\n        const index = this.readIndex(storage);\n        const entry = index.find((s)=>s.id === id);\n        if (!entry) {\n            return null;\n        }\n        const encoded = storage.getItem(this.saveKey(id));\n        if (!encoded) {\n            return null;\n        }\n        try {\n            const data = decodeBase64ToJson(encoded);\n            return {\n                ...entry,\n                data\n            };\n        } catch  {\n            return null;\n        }\n    }\n    list() {\n        const storage = getLocalStorage();\n        if (!storage) {\n            return [];\n        }\n        return this.readIndex(storage);\n    }\n    loadAll() {\n        const storage = getLocalStorage();\n        if (!storage) {\n            return [];\n        }\n        const index = this.readIndex(storage);\n        const loaded = [];\n        for (const entry of index){\n            const encoded = storage.getItem(this.saveKey(entry.id));\n            if (!encoded) continue;\n            try {\n                var _this_onLoadSaveFileCallback, _this;\n                const data = decodeBase64ToJson(encoded);\n                const save = {\n                    ...entry,\n                    data\n                };\n                loaded.push(save);\n                (_this_onLoadSaveFileCallback = (_this = this).onLoadSaveFileCallback) === null || _this_onLoadSaveFileCallback === void 0 ? void 0 : _this_onLoadSaveFileCallback.call(_this, save);\n            } catch  {\n                continue;\n            }\n        }\n        return loaded;\n    }\n    delete(id) {\n        const storage = getLocalStorage();\n        if (!storage) {\n            return;\n        }\n        storage.removeItem(this.saveKey(id));\n        const index = this.readIndex(storage).filter((s)=>s.id !== id);\n        this.writeIndex(storage, index);\n    }\n    clear() {\n        const storage = getLocalStorage();\n        if (!storage) {\n            return;\n        }\n        const index = this.readIndex(storage);\n        for (const entry of index){\n            storage.removeItem(this.saveKey(entry.id));\n        }\n        storage.removeItem(this.indexKey());\n    }\n    indexKey() {\n        return `${this.namespace}:saves:index`;\n    }\n    saveKey(id) {\n        return `${this.namespace}:saves:${id}`;\n    }\n    readIndex(storage) {\n        const raw = storage.getItem(this.indexKey());\n        if (!raw) {\n            return [];\n        }\n        try {\n            const parsed = JSON.parse(raw);\n            if (!Array.isArray(parsed)) {\n                return [];\n            }\n            return parsed.filter((s)=>{\n                return !!s && typeof s.id === \"string\" && typeof s.createdAt === \"number\" && typeof s.updatedAt === \"number\";\n            }).sort((a, b)=>b.updatedAt - a.updatedAt);\n        } catch  {\n            return [];\n        }\n    }\n    writeIndex(storage, index) {\n        storage.setItem(this.indexKey(), JSON.stringify(index));\n    }\n    constructor(options){\n        _define_property(this, \"namespace\", void 0);\n        _define_property(this, \"onLoadSaveFileCallback\", null);\n        var _options_namespace;\n        this.namespace = (_options_namespace = options === null || options === void 0 ? void 0 : options.namespace) !== null && _options_namespace !== void 0 ? _options_namespace : \"sliver-engine\";\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Game/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get Game () {\n        return Game;\n    },\n    get GameSaves () {\n        return _index3.GameSaves;\n    }\n});\nconst _index = require(\"../Context/index.js\");\nconst _index1 = require(\"../Events/index.js\");\nconst _index2 = require(\"../SoundManager/index.js\");\nconst _index3 = require(\"./Saves/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass Game {\n    setup() {\n        if (this.didSetup) {\n            return;\n        }\n        this.didSetup = true;\n        this.saves.loadAll();\n        this.onSetup();\n        console.log(\"Game setup complete.\");\n    }\n    onLoadSaveFile(callback) {\n        this.saves.onLoadSaveFile(callback);\n        return this;\n    }\n    saveGame(data, options) {\n        return this.saves.create(data, options);\n    }\n    // Meant to be overridden by game-specific implementations to inject logic\n    // that should run once during game setup (after save files are loaded).\n    // eslint-disable-next-line @typescript-eslint/no-empty-function\n    onSetup() {}\n    start() {\n        this.setup();\n        console.log(\"Game started.\");\n        // Tick interval\n        setInterval(()=>{\n            const now = performance.now();\n            const tickInterval = 1000 / this.ticksPerSecond;\n            const lastTickTime = this.lastTickTime || 0;\n            if (now - lastTickTime >= tickInterval) {\n                this.onTick();\n                this.scenes.tick();\n                this.lastTickTime = now;\n            }\n        });\n        // Render loop\n        const renderLoop = ()=>{\n            this.canvas.clearCanvas();\n            this.render();\n            requestAnimationFrame(renderLoop);\n        };\n        requestAnimationFrame(renderLoop);\n    }\n    render() {\n        this.scenes.getActiveScenes().forEach((scene)=>{\n            scene.render(this.canvas);\n        });\n    }\n    // Meant to be overridden by game-specific implementations to inject logic\n    // that should run once per tick, before the scenes tick.\n    // eslint-disable-next-line @typescript-eslint/no-empty-function\n    onTick() {}\n    handleEvent(event) {\n        this.scenes.getActiveScenes().toReversed().forEach((scene)=>{\n            scene.handleEvent(event);\n        });\n    }\n    getContext() {\n        return this.context;\n    }\n    getTickRate() {\n        return this.ticksPerSecond;\n    }\n    getKeyAccumulator() {\n        return this.keyAccumulator;\n    }\n    constructor({ canvas, scenes, ticksPerSecond, soundManager, saveNamespace }){\n        _define_property(this, \"canvas\", void 0);\n        _define_property(this, \"scenes\", void 0);\n        _define_property(this, \"ticksPerSecond\", 60);\n        _define_property(this, \"lastTickTime\", 0);\n        _define_property(this, \"didSetup\", false);\n        _define_property(this, \"context\", void 0);\n        _define_property(this, \"keyAccumulator\", new _index1.KeyAccumulator());\n        _define_property(this, \"eventsDispatcher\", void 0);\n        _define_property(this, \"saves\", void 0);\n        this.canvas = canvas;\n        this.scenes = scenes;\n        this.ticksPerSecond = ticksPerSecond || this.ticksPerSecond;\n        this.context = new _index.GameContext({\n            game: this,\n            canvas,\n            sceneManager: scenes,\n            keyAccumulator: this.keyAccumulator,\n            soundManager: soundManager\n        });\n        this.scenes.bindContext(this.context);\n        this.eventsDispatcher = new _index1.GameEventsdispatcher(this, this.keyAccumulator);\n        this.saves = new _index3.GameSaves({\n            namespace: saveNamespace\n        });\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Decorators/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get onColision () {\n        return onColision;\n    },\n    get renderSprite () {\n        return renderSprite;\n    },\n    get renderSpriteAnimation () {\n        return renderSpriteAnimation;\n    },\n    get renderTile () {\n        return renderTile;\n    },\n    get renderTileAnimation () {\n        return renderTileAnimation;\n    }\n});\nconst resolveOption = (value, fallback, obj)=>{\n    if (typeof value === \"function\") {\n        return value(obj);\n    }\n    return value !== null && value !== void 0 ? value : fallback;\n};\nconst onColision = (predicate, handler)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(other, penetration) {\n            if (predicate(this, other)) {\n                handler(this, other);\n            }\n            return original.call(this, other, penetration);\n        };\n        return descriptor;\n    };\n};\nconst renderSprite = (spriteDefinitionName, options = {})=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(canvas, scene) {\n            const shouldRender = resolveOption(options.when, true, this);\n            if (!shouldRender) {\n                return original.call(this, canvas, scene);\n            }\n            const spriteLib = canvas.getSpriteLibrary();\n            const context = canvas.getContext();\n            const definitionName = typeof spriteDefinitionName === \"function\" ? spriteDefinitionName(this) : spriteDefinitionName;\n            const position = resolveOption(options.overridePosition, this.getPosition(), this);\n            const resolvedScale = resolveOption(options.scale, 1, this);\n            const mirroring = resolveOption(options.mirroring, null, this);\n            const mirrorHorizontal = mirroring === \"horizontal\" || mirroring === \"both\";\n            const mirrorVertical = mirroring === \"vertical\" || mirroring === \"both\";\n            spriteLib.drawSpriteDefinition(context, definitionName, position, resolvedScale, mirrorHorizontal, mirrorVertical);\n            return original.call(this, canvas, scene);\n        };\n        return descriptor;\n    };\n};\nconst renderTile = (spriteSheetName, index, options = {})=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(canvas, scene) {\n            const shouldRender = resolveOption(options.when, true, this);\n            if (!shouldRender) {\n                return original.call(this, canvas, scene);\n            }\n            const spriteLib = canvas.getSpriteLibrary();\n            const context = canvas.getContext();\n            const position = resolveOption(options.overridePosition, this.getPosition(), this);\n            const resolvedIndex = resolveOption(index, 0, this);\n            const resolvedScale = resolveOption(options.scale, 1, this);\n            const mirroring = resolveOption(options.mirroring, null, this);\n            spriteLib.drawSpriteFrame(context, resolveOption(spriteSheetName, \"\", this), resolvedIndex, position, resolvedScale, mirroring === \"horizontal\" || mirroring === \"both\", mirroring === \"vertical\" || mirroring === \"both\");\n            return original.call(this, canvas, scene);\n        };\n        return descriptor;\n    };\n};\nconst renderTileAnimation = (spriteSheetName, indexes, options)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(canvas, scene) {\n            const shouldRender = resolveOption(options.when, true, this);\n            if (!shouldRender) {\n                return original.call(this, canvas, scene);\n            }\n            const spriteLib = canvas.getSpriteLibrary();\n            const context = canvas.getContext();\n            const gameContext = this.getContext();\n            const mirroring = resolveOption(options.mirroring, null, this);\n            const ticksPerFrame = resolveOption(options.ticksPerFrame, 1, this);\n            const position = resolveOption(options.overridePosition, this.getPosition(), this);\n            const resolvedScale = resolveOption(options.scale, 1, this);\n            const resolvedIndexes = resolveOption(indexes, [], this);\n            const frameIndex = Math.floor(gameContext.getTickCount() / ticksPerFrame) % resolvedIndexes.length;\n            const spriteIndex = resolvedIndexes[frameIndex];\n            spriteLib.drawSpriteFrame(context, resolveOption(spriteSheetName, \"\", this), spriteIndex, position, resolvedScale, mirroring === \"horizontal\" || mirroring === \"both\", mirroring === \"vertical\" || mirroring === \"both\");\n            return original.call(this, canvas, scene);\n        };\n        return descriptor;\n    };\n};\nconst renderSpriteAnimation = (definitions, options)=>{\n    return function(_target, _propertyKey, descriptor) {\n        const original = descriptor.value;\n        if (!original) return descriptor;\n        descriptor.value = function(canvas, scene) {\n            const shouldRender = resolveOption(options.when, true, this);\n            if (!shouldRender) {\n                return original.call(this, canvas, scene);\n            }\n            const spriteLib = canvas.getSpriteLibrary();\n            const context = canvas.getContext();\n            const gameContext = this.getContext();\n            const mirroring = resolveOption(options.mirroring, null, this);\n            const ticksPerFrame = resolveOption(options.ticksPerFrame, 1, this);\n            const position = resolveOption(options.overridePosition, this.getPosition(), this);\n            const resolvedScale = resolveOption(options.scale, 1, this);\n            const resolvedDefinitions = typeof definitions === \"function\" ? definitions(this) : definitions;\n            const frameIndex = Math.floor(gameContext.getTickCount() / ticksPerFrame) % resolvedDefinitions.length;\n            const definitionName = resolvedDefinitions[frameIndex];\n            spriteLib.drawSpriteDefinition(context, definitionName, position, resolvedScale, mirroring === \"horizontal\" || mirroring === \"both\", mirroring === \"vertical\" || mirroring === \"both\");\n            return original.call(this, canvas, scene);\n        };\n        return descriptor;\n    };\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Hitboxes/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get CircleHitbox () {\n        return CircleHitbox;\n    },\n    get SquareHitbox () {\n        return SquareHitbox;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nconst normalizeHitboxOptions = (options)=>{\n    if (typeof options === \"boolean\") {\n        return {\n            debug: options,\n            solid: true\n        };\n    }\n    var _options_debug, _options_solid;\n    return {\n        debug: (_options_debug = options === null || options === void 0 ? void 0 : options.debug) !== null && _options_debug !== void 0 ? _options_debug : false,\n        solid: (_options_solid = options === null || options === void 0 ? void 0 : options.solid) !== null && _options_solid !== void 0 ? _options_solid : true\n    };\n};\nconst clamp = (value, min, max)=>Math.max(min, Math.min(max, value));\nconst rotateAround = (point, pivot, angle)=>{\n    if (angle === 0) return point.clone();\n    const rotated = point.toSubtracted(pivot).rotate(angle);\n    rotated.add(pivot);\n    return rotated;\n};\nconst projectVertices = (vertices, axis)=>{\n    let min = Number.POSITIVE_INFINITY;\n    let max = Number.NEGATIVE_INFINITY;\n    for (const vertex of vertices){\n        const projection = vertex.dotProduct(axis);\n        min = Math.min(min, projection);\n        max = Math.max(max, projection);\n    }\n    return {\n        min,\n        max\n    };\n};\nconst getObbData = (vertices, size)=>{\n    const center = vertices[0].toAdded(vertices[2]).toMultiplied(0.5);\n    const axisX = vertices[1].toSubtracted(vertices[0]).normalize();\n    const axisY = vertices[3].toSubtracted(vertices[0]).normalize();\n    return {\n        center,\n        axisX,\n        axisY,\n        halfX: size.x / 2,\n        halfY: size.y / 2\n    };\n};\nconst closestPointOnObb = (point, obb)=>{\n    const d = point.toSubtracted(obb.center);\n    const distX = d.dotProduct(obb.axisX);\n    const distY = d.dotProduct(obb.axisY);\n    const clampedX = clamp(distX, -obb.halfX, obb.halfX);\n    const clampedY = clamp(distY, -obb.halfY, obb.halfY);\n    return obb.center.toAdded(obb.axisX.toMultiplied(clampedX)).toAdded(obb.axisY.toMultiplied(clampedY));\n};\nclass SquareHitbox {\n    getTransformedVertices() {\n        const topLeft = this.getAbsolutePosition();\n        const topRight = new _index.Vector(topLeft.x + this.size.x, topLeft.y);\n        const bottomRight = new _index.Vector(topLeft.x + this.size.x, topLeft.y + this.size.y);\n        const bottomLeft = new _index.Vector(topLeft.x, topLeft.y + this.size.y);\n        var _this_gameObject_rotation;\n        const angle = (_this_gameObject_rotation = this.gameObject.rotation) !== null && _this_gameObject_rotation !== void 0 ? _this_gameObject_rotation : 0;\n        if (angle === 0) {\n            return [\n                topLeft,\n                topRight,\n                bottomRight,\n                bottomLeft\n            ];\n        }\n        const pivot = this.gameObject.getRotationCenter();\n        return [\n            rotateAround(topLeft, pivot, angle),\n            rotateAround(topRight, pivot, angle),\n            rotateAround(bottomRight, pivot, angle),\n            rotateAround(bottomLeft, pivot, angle)\n        ];\n    }\n    getAbsolutePosition() {\n        const gameObjectPosition = this.gameObject.getPosition();\n        return gameObjectPosition.toAdded(this.offset);\n    }\n    intersectsWithSquare(other) {\n        const verticesA = this.getTransformedVertices();\n        const verticesB = other.getTransformedVertices();\n        const axes = [\n            verticesA[1].toSubtracted(verticesA[0]).normalize(),\n            verticesA[3].toSubtracted(verticesA[0]).normalize(),\n            verticesB[1].toSubtracted(verticesB[0]).normalize(),\n            verticesB[3].toSubtracted(verticesB[0]).normalize()\n        ];\n        for (const axis of axes){\n            if (axis.squaredMagnitude() === 0) continue;\n            const projA = projectVertices(verticesA, axis);\n            const projB = projectVertices(verticesB, axis);\n            if (projA.max < projB.min || projB.max < projA.min) {\n                return false;\n            }\n        }\n        return true;\n    }\n    intersectsWithCircle(other) {\n        const circleCenter = other.getTransformedPosition();\n        const vertices = this.getTransformedVertices();\n        const obb = getObbData(vertices, this.size);\n        const closest = closestPointOnObb(circleCenter, obb);\n        const dx = circleCenter.x - closest.x;\n        const dy = circleCenter.y - closest.y;\n        return dx * dx + dy * dy <= other.radius * other.radius;\n    }\n    intersects(other) {\n        if (other instanceof CircleHitbox) {\n            return this.intersectsWithCircle(other);\n        }\n        if (other instanceof SquareHitbox) {\n            return this.intersectsWithSquare(other);\n        }\n        return false;\n    }\n    intersectsWithPoint(point) {\n        var _this_gameObject_rotation;\n        const angle = (_this_gameObject_rotation = this.gameObject.rotation) !== null && _this_gameObject_rotation !== void 0 ? _this_gameObject_rotation : 0;\n        if (angle === 0) {\n            const thisPos = this.getAbsolutePosition();\n            return !(point.x < thisPos.x || point.x > thisPos.x + this.size.x || point.y < thisPos.y || point.y > thisPos.y + this.size.y);\n        }\n        const vertices = this.getTransformedVertices();\n        const obb = getObbData(vertices, this.size);\n        const d = point.toSubtracted(obb.center);\n        const localX = d.dotProduct(obb.axisX);\n        const localY = d.dotProduct(obb.axisY);\n        return Math.abs(localX) <= obb.halfX && Math.abs(localY) <= obb.halfY;\n    }\n    renderDebug(canvas) {\n        if (this.debug) {\n            const pos = this.getAbsolutePosition();\n            canvas.getShapeDrawer().drawRectangle(pos.x, pos.y, this.size.x, this.size.y, \"red\", false);\n        }\n    }\n    constructor(offset, size, gameObject, options = {}){\n        _define_property(this, \"offset\", void 0);\n        _define_property(this, \"size\", void 0);\n        _define_property(this, \"gameObject\", void 0);\n        _define_property(this, \"solid\", void 0);\n        _define_property(this, \"debug\", void 0);\n        this.offset = offset;\n        this.size = size;\n        this.gameObject = gameObject;\n        const resolved = normalizeHitboxOptions(options);\n        this.debug = resolved.debug;\n        this.solid = resolved.solid;\n    }\n}\nclass CircleHitbox {\n    getAbsolutePosition() {\n        const gameObjectPosition = this.gameObject.getPosition();\n        return gameObjectPosition.toAdded(this.offset);\n    }\n    getTransformedPosition() {\n        const pos = this.getAbsolutePosition();\n        var _this_gameObject_rotation;\n        const angle = (_this_gameObject_rotation = this.gameObject.rotation) !== null && _this_gameObject_rotation !== void 0 ? _this_gameObject_rotation : 0;\n        if (angle === 0) return pos;\n        const pivot = this.gameObject.getRotationCenter();\n        return rotateAround(pos, pivot, angle);\n    }\n    intersectWithCircle(other, thisPos, otherPos) {\n        const dx = thisPos.x - otherPos.x;\n        const dy = thisPos.y - otherPos.y;\n        const distance = Math.sqrt(dx * dx + dy * dy);\n        return distance < this.radius + other.radius;\n    }\n    intersectWithSquare(other, thisPos, _otherPos) {\n        const vertices = other.getTransformedVertices();\n        const obb = getObbData(vertices, other.size);\n        const closest = closestPointOnObb(thisPos, obb);\n        const dx = thisPos.x - closest.x;\n        const dy = thisPos.y - closest.y;\n        return dx * dx + dy * dy <= this.radius * this.radius;\n    }\n    intersects(other) {\n        const thisPos = this.getTransformedPosition();\n        if (other instanceof CircleHitbox) {\n            return this.intersectWithCircle(other, thisPos, other.getTransformedPosition());\n        }\n        if (other instanceof SquareHitbox) {\n            return this.intersectWithSquare(other, thisPos, other.getAbsolutePosition());\n        }\n        return false;\n    }\n    intersectsWithPoint(point) {\n        const thisPos = this.getTransformedPosition();\n        const dx = point.x - thisPos.x;\n        const dy = point.y - thisPos.y;\n        const distance = Math.sqrt(dx * dx + dy * dy);\n        return distance < this.radius;\n    }\n    renderDebug(canvas) {\n        if (this.debug) {\n            const pos = this.getAbsolutePosition();\n            const drawer = canvas.getShapeDrawer();\n            drawer.drawCircle(pos.x, pos.y, this.radius, \"red\", false);\n            drawer.drawLine(pos, pos.toAdded(new _index.Vector(this.radius, 0)), 2, \"red\");\n        }\n    }\n    constructor(offset, radius, gameObject, options = {}){\n        _define_property(this, \"offset\", void 0);\n        _define_property(this, \"radius\", void 0);\n        _define_property(this, \"gameObject\", void 0);\n        _define_property(this, \"solid\", void 0);\n        _define_property(this, \"debug\", void 0);\n        this.offset = offset;\n        this.radius = radius;\n        this.gameObject = gameObject;\n        const resolved = normalizeHitboxOptions(options);\n        this.debug = resolved.debug;\n        this.solid = resolved.solid;\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/Button.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"Button\", {\n    enumerable: true,\n    get: function() {\n        return Button;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _decorators = require(\"../../Events/decorators.js\");\nconst _index1 = require(\"../../Lib/Vector/index.js\");\nconst _index2 = require(\"../Hitboxes/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nvar __decorate = (void 0) && (void 0).__decorate || function(decorators, target, key, desc) {\n    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;\n    if (typeof Reflect === \"object\" && typeof Reflect.decorate === \"function\") r = Reflect.decorate(decorators, target, key, desc);\n    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;\n    return c > 3 && r && Object.defineProperty(target, key, r), r;\n};\nclass Button extends _index.GameObject {\n    renderButton(obj, canvas) {\n        const shapeDrawer = canvas.getShapeDrawer();\n        const pos = obj.getPosition();\n        shapeDrawer.drawRectangle(pos.x, pos.y, this.size.x, this.size.y, this.color);\n        shapeDrawer.drawText(this.label, pos.x + this.size.x / 2, pos.y + this.size.y / 2, this.textcolor);\n    }\n    handleEvent(event) {\n        super.handleEvent(event);\n    }\n    constructor(name, position, size, label, color = \"red\", textcolor = \"black\", onClick = ()=>{}){\n        super(name, position), _define_property(this, \"size\", void 0), _define_property(this, \"label\", void 0), _define_property(this, \"color\", void 0), _define_property(this, \"textcolor\", void 0), _define_property(this, \"onClick\", void 0);\n        this.size = size;\n        this.label = label;\n        this.color = color;\n        this.textcolor = textcolor;\n        this.onClick = onClick;\n        this.setRenderFunction(this.renderButton);\n        this.addHitbox(new _index2.SquareHitbox(_index1.Vector.zero(), this.size, this, {\n            solid: false\n        }));\n    }\n}\n__decorate([\n    (0, _decorators.onClick)((obj)=>{\n        obj.onClick(obj);\n    }),\n    (0, _decorators.onHover)(()=>{}),\n    (0, _decorators.onStopHovering)(()=>{})\n], Button.prototype, \"handleEvent\", null);",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/Clickableshape.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"ClickableShape\", {\n    enumerable: true,\n    get: function() {\n        return ClickableShape;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _decorators = require(\"../../Events/decorators.js\");\nconst _index1 = require(\"../../Lib/Vector/index.js\");\nconst _index2 = require(\"../Hitboxes/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nvar __decorate = (void 0) && (void 0).__decorate || function(decorators, target, key, desc) {\n    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;\n    if (typeof Reflect === \"object\" && typeof Reflect.decorate === \"function\") r = Reflect.decorate(decorators, target, key, desc);\n    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;\n    return c > 3 && r && Object.defineProperty(target, key, r), r;\n};\nclass ClickableShape extends _index.GameObject {\n    renderShape(obj, canvas) {\n        const context = canvas.getContext();\n        const pos = obj.getPosition();\n        this.shapeRenderFn(context, pos, this.size);\n    }\n    handleEvent(event) {}\n    constructor(position, size, shapeRenderFn, onClick){\n        super(\"ClickableShape\", position), _define_property(this, \"size\", void 0), _define_property(this, \"shapeRenderFn\", void 0), _define_property(this, \"onClick\", void 0);\n        this.size = size;\n        this.shapeRenderFn = shapeRenderFn;\n        this.onClick = onClick;\n        this.setRenderFunction(this.renderShape);\n        this.addHitbox(new _index2.SquareHitbox(_index1.Vector.zero(), this.size, this));\n    }\n}\n__decorate([\n    (0, _decorators.onClick)((obj, event)=>{\n        obj.onClick(obj);\n    })\n], ClickableShape.prototype, \"handleEvent\", null);",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/FloatingView.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"FloatingView\", {\n    enumerable: true,\n    get: function() {\n        return FloatingView;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _index1 = require(\"../../Lib/Vector/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass FloatingView extends _index.GameObject {\n    addChild(child) {\n        super.addChild(child);\n        child.setPositionRelativeToMotherShip(true);\n    }\n    tick() {\n        this.getPosition().add(new _index1.Vector(0, -this.riseSpeed));\n        const nextOpacity = Math.max(0, this.getOpacity() - this.fadeStep);\n        this.setOpacity(nextOpacity);\n        if (nextOpacity <= 0) {\n            this.destroy();\n            return;\n        }\n        super.tick();\n    }\n    constructor(name, position, size, color, { initialOpacity = 1, fadeStep = 0.02, riseSpeed = 1 } = {}){\n        super(name, position), _define_property(this, \"size\", void 0), _define_property(this, \"color\", void 0), _define_property(this, \"fadeStep\", void 0), _define_property(this, \"riseSpeed\", void 0), _define_property(this, \"renderView\", (obj, canvas)=>{\n            const pos = obj.getPosition();\n            canvas.getShapeDrawer().drawRectangle(pos.x, pos.y, this.size.x, this.size.y, this.color, true);\n        });\n        this.size = size;\n        this.color = color;\n        this.setOpacity(Math.min(1, Math.max(0, initialOpacity)));\n        this.fadeStep = fadeStep;\n        this.riseSpeed = riseSpeed;\n        this.setRenderFunction(this.renderView);\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/Group.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"Group\", {\n    enumerable: true,\n    get: function() {\n        return Group;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _decorators = require(\"../../Events/decorators.js\");\nconst _index1 = require(\"../../Lib/Vector/index.js\");\nvar __decorate = (void 0) && (void 0).__decorate || function(decorators, target, key, desc) {\n    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;\n    if (typeof Reflect === \"object\" && typeof Reflect.decorate === \"function\") r = Reflect.decorate(decorators, target, key, desc);\n    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;\n    return c > 3 && r && Object.defineProperty(target, key, r), r;\n};\nclass Group extends _index.GameObject {\n    handleEvent(event) {}\n    constructor(name, children){\n        super(name, new _index1.Vector(0, 0));\n        children.forEach((child)=>{\n            this.addChild(child);\n            if (this.getMotherShip()) {\n                child.setMotherShip(this.getMotherShip());\n            }\n        });\n    }\n}\n__decorate([\n    (0, _decorators.onChildrenEvents)()\n], Group.prototype, \"handleEvent\", null);",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/HoverableShape.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"HoverableShape\", {\n    enumerable: true,\n    get: function() {\n        return HoverableShape;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _decorators = require(\"../../Events/decorators.js\");\nconst _index1 = require(\"../../Lib/Vector/index.js\");\nconst _index2 = require(\"../Hitboxes/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nvar __decorate = (void 0) && (void 0).__decorate || function(decorators, target, key, desc) {\n    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;\n    if (typeof Reflect === \"object\" && typeof Reflect.decorate === \"function\") r = Reflect.decorate(decorators, target, key, desc);\n    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;\n    return c > 3 && r && Object.defineProperty(target, key, r), r;\n};\nclass HoverableShape extends _index.GameObject {\n    renderShape(obj, canvas) {\n        const context = canvas.getContext();\n        const pos = obj.getPosition();\n        this.shapeRenderFn(context, pos, this.size);\n    }\n    handleEvent(event) {}\n    constructor(position, size, shapeRenderFn, onHover, onStopHover){\n        super(\"ClickableShape\", position), _define_property(this, \"size\", void 0), _define_property(this, \"shapeRenderFn\", void 0), _define_property(this, \"onHover\", void 0), _define_property(this, \"onStopHover\", void 0);\n        this.size = size;\n        this.shapeRenderFn = shapeRenderFn;\n        this.onHover = onHover;\n        this.onStopHover = onStopHover;\n        this.setRenderFunction(this.renderShape);\n        this.addHitbox(new _index2.SquareHitbox(_index1.Vector.zero(), this.size, this));\n    }\n}\n__decorate([\n    (0, _decorators.onHover)((obj, event)=>{\n        obj.onHover ? obj.onHover(obj) : null;\n    }),\n    (0, _decorators.onStopHovering)((obj, event)=>{\n        obj.onStopHover ? obj.onStopHover(obj) : null;\n    })\n], HoverableShape.prototype, \"handleEvent\", null);",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/SceneTranslator.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"SceneTranslator\", {\n    enumerable: true,\n    get: function() {\n        return SceneTranslator;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _Arrow = require(\"../../Assets/Shapes/Arrow.js\");\nconst _Transformations = require(\"../../CanvasController/Transformations.js\");\nconst _decorators = require(\"../../Events/decorators.js\");\nconst _index1 = require(\"../../Lib/Vector/index.js\");\nconst _Clickableshape = require(\"./Clickableshape.js\");\nconst _Group = require(\"./Group.js\");\nconst _HoverableShape = require(\"./HoverableShape.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nvar __decorate = (void 0) && (void 0).__decorate || function(decorators, target, key, desc) {\n    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;\n    if (typeof Reflect === \"object\" && typeof Reflect.decorate === \"function\") r = Reflect.decorate(decorators, target, key, desc);\n    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;\n    return c > 3 && r && Object.defineProperty(target, key, r), r;\n};\nconst createSceneTranslatorGameObject = (scene)=>new _Group.Group(\"ArrowGroup\", [\n        new _HoverableShape.HoverableShape(new _index1.Vector(750, 300), new _index1.Vector(50, 50), (0, _Transformations.withRotation)(0, (0, _Arrow.shapeWithParams)(_Arrow.renderChevron, {\n            color: \"purple\",\n            strokeWidth: 6,\n            lineCap: \"round\",\n            lineJoin: \"round\"\n        })), (obj)=>{\n            obj.getMotherShip().getMotherShip().setTranslationVector(new _index1.Vector(10, 0));\n        }, (obj)=>{\n            obj.getMotherShip().getMotherShip().setTranslationVector(new _index1.Vector(0, 0));\n        }),\n        new _HoverableShape.HoverableShape(new _index1.Vector(400, 550), new _index1.Vector(50, 50), (0, _Transformations.withRotation)(Math.PI / 2, (0, _Arrow.shapeWithParams)(_Arrow.renderChevron, {\n            color: \"purple\",\n            strokeWidth: 6,\n            lineCap: \"round\",\n            lineJoin: \"round\"\n        })), (obj)=>{\n            obj.getMotherShip().getMotherShip().setTranslationVector(new _index1.Vector(0, 10));\n        }, (obj)=>{\n            obj.getMotherShip().getMotherShip().setTranslationVector(new _index1.Vector(0, 0));\n        }),\n        new _HoverableShape.HoverableShape(new _index1.Vector(400, 0), new _index1.Vector(50, 50), (0, _Transformations.withRotation)(3 * (Math.PI / 2), (0, _Arrow.shapeWithParams)(_Arrow.renderChevron, {\n            color: \"purple\",\n            strokeWidth: 6,\n            lineCap: \"round\",\n            lineJoin: \"round\"\n        })), (obj)=>{\n            obj.getMotherShip().getMotherShip().setTranslationVector(new _index1.Vector(0, -10));\n        }, (obj)=>{\n            obj.getMotherShip().getMotherShip().setTranslationVector(new _index1.Vector(0, 0));\n        }),\n        new _HoverableShape.HoverableShape(new _index1.Vector(0, 300), new _index1.Vector(50, 50), (0, _Transformations.withRotation)(Math.PI, (0, _Arrow.shapeWithParams)(_Arrow.renderChevron, {\n            color: \"purple\",\n            strokeWidth: 6,\n            lineCap: \"round\",\n            lineJoin: \"round\"\n        })), (obj)=>{\n            obj.getMotherShip().getMotherShip().setTranslationVector(new _index1.Vector(-10, 0));\n        }, (obj)=>{\n            obj.getMotherShip().getMotherShip().setTranslationVector(new _index1.Vector(0, 0));\n        })\n    ]);\nclass SceneTranslator extends _index.GameObject {\n    tick() {\n        var _this_translateScene;\n        console.log(this.translateScene);\n        (_this_translateScene = this.translateScene) === null || _this_translateScene === void 0 ? void 0 : _this_translateScene.getOffset().add(new _index1.Vector(this.translationVector.x, this.translationVector.y));\n    }\n    setTranslationVector(translation) {\n        this.translationVector = translation;\n    }\n    handleEvent(event) {}\n    constructor(scene){\n        super(\"SceneTranslator\", new _index1.Vector(0, 0)), _define_property(this, \"translationVector\", _index1.Vector.zero()), _define_property(this, \"translateScene\", null);\n        this.addChild(createSceneTranslatorGameObject(scene));\n        this.translateScene = scene;\n    }\n}\n__decorate([\n    (0, _decorators.onChildrenEvents)()\n], SceneTranslator.prototype, \"handleEvent\", null);",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/ScrollView.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"ScrollView\", {\n    enumerable: true,\n    get: function() {\n        return ScrollView;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _decorators = require(\"../../Events/decorators.js\");\nconst _index1 = require(\"../../Lib/Vector/index.js\");\nconst _index2 = require(\"../Hitboxes/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nvar __decorate = (void 0) && (void 0).__decorate || function(decorators, target, key, desc) {\n    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;\n    if (typeof Reflect === \"object\" && typeof Reflect.decorate === \"function\") r = Reflect.decorate(decorators, target, key, desc);\n    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;\n    return c > 3 && r && Object.defineProperty(target, key, r), r;\n};\nclass ScrollView extends _index.GameObject {\n    getMaxScroll() {\n        return Math.max(0, this.contentHeight - this.size.y);\n    }\n    clampScroll(next) {\n        return Math.max(0, Math.min(this.getMaxScroll(), next));\n    }\n    scrollBy(deltaY) {\n        const next = this.clampScroll(this.scrollOffset + deltaY);\n        if (next !== this.scrollOffset) {\n            this.scrollOffset = next;\n            this.syncChildrenPositions();\n        }\n    }\n    addChildWithOffset(child, offset) {\n        const relativeOffset = offset !== null && offset !== void 0 ? offset : child.getPosition().clone();\n        this.childOffsets.set(child, relativeOffset.clone());\n        this.bumpContentHeight(relativeOffset);\n        this.syncChildPosition(child, relativeOffset);\n        super.addChild(child);\n    }\n    bumpContentHeight(relativeOffset) {\n        this.contentHeight = Math.max(this.contentHeight, relativeOffset.y + this.size.y);\n    }\n    addChild(child) {\n        this.addChildWithOffset(child);\n    }\n    setPosition(position) {\n        super.setPosition(position);\n        this.syncChildrenPositions();\n    }\n    tick() {\n        this.syncChildrenPositions();\n        super.tick();\n    }\n    syncChildPosition(child, relativeOffset) {\n        const absolute = new _index1.Vector(this.getPosition().x + relativeOffset.x, this.getPosition().y + relativeOffset.y - this.scrollOffset);\n        child.setPosition(absolute);\n    }\n    syncChildrenPositions() {\n        this.childOffsets.forEach((offset, child)=>this.syncChildPosition(child, offset));\n    }\n    renderScrollbar(canvas) {\n        const shapeDrawer = canvas.getShapeDrawer();\n        const pos = this.getPosition();\n        const trackX = pos.x + this.size.x - this.scrollbarWidth;\n        const trackHeight = this.size.y;\n        const maxScroll = this.getMaxScroll();\n        shapeDrawer.drawRectangle(trackX, pos.y, this.scrollbarWidth, trackHeight, this.scrollbarTrackColor);\n        const visibleRatio = Math.min(1, this.size.y / this.contentHeight);\n        const thumbHeight = Math.max(12, trackHeight * visibleRatio);\n        const thumbTravel = trackHeight - thumbHeight;\n        const scrollRatio = maxScroll === 0 ? 0 : this.scrollOffset / maxScroll;\n        const thumbY = pos.y + scrollRatio * thumbTravel;\n        shapeDrawer.drawRectangle(trackX, thumbY, this.scrollbarWidth, thumbHeight, this.scrollbarColor);\n    }\n    render(canvas, scene) {\n        this.syncChildrenPositions();\n        const pos = this.getPosition();\n        canvas.getShapeDrawer().withClippingRect(pos.x, pos.y, this.size.x, this.size.y, ()=>super.render(canvas, scene));\n        this.renderScrollbar(canvas);\n    }\n    handleEvent(event) {}\n    constructor(name, position, size, children = [], options = {}){\n        super(name, position), _define_property(this, \"size\", void 0), _define_property(this, \"scrollOffset\", 0), _define_property(this, \"contentHeight\", void 0), _define_property(this, \"scrollbarWidth\", void 0), _define_property(this, \"scrollbarColor\", void 0), _define_property(this, \"scrollbarTrackColor\", void 0), _define_property(this, \"backgroundColor\", void 0), _define_property(this, \"scrollStep\", void 0), _define_property(this, \"childOffsets\", new Map()), _define_property(this, \"renderContainer\", (obj, canvas)=>{\n            const shapeDrawer = canvas.getShapeDrawer();\n            const pos = obj.getPosition();\n            shapeDrawer.drawRectangle(pos.x, pos.y, this.size.x, this.size.y, this.backgroundColor);\n        });\n        this.size = size;\n        var _options_backgroundColor;\n        this.backgroundColor = (_options_backgroundColor = options.backgroundColor) !== null && _options_backgroundColor !== void 0 ? _options_backgroundColor : \"#f7f7f7\";\n        var _options_scrollbarWidth;\n        this.scrollbarWidth = (_options_scrollbarWidth = options.scrollbarWidth) !== null && _options_scrollbarWidth !== void 0 ? _options_scrollbarWidth : 10;\n        var _options_scrollbarColor;\n        this.scrollbarColor = (_options_scrollbarColor = options.scrollbarColor) !== null && _options_scrollbarColor !== void 0 ? _options_scrollbarColor : \"#8a8a8a\";\n        var _options_scrollbarTrackColor;\n        this.scrollbarTrackColor = (_options_scrollbarTrackColor = options.scrollbarTrackColor) !== null && _options_scrollbarTrackColor !== void 0 ? _options_scrollbarTrackColor : \"#cfcfcf\";\n        var _options_scrollStep;\n        this.scrollStep = (_options_scrollStep = options.scrollStep) !== null && _options_scrollStep !== void 0 ? _options_scrollStep : 40;\n        var _options_contentHeight;\n        this.contentHeight = Math.max((_options_contentHeight = options.contentHeight) !== null && _options_contentHeight !== void 0 ? _options_contentHeight : size.y, size.y);\n        this.setRenderFunction(this.renderContainer);\n        this.addHitbox(new _index2.SquareHitbox(_index1.Vector.zero(), this.size, this));\n        children.forEach((child)=>this.addChildWithOffset(child));\n    }\n}\n__decorate([\n    (0, _decorators.onMouseWheelOverHitbox)((obj, event)=>{\n        obj.scrollBy(event.deltaY > 0 ? obj.scrollStep : -obj.scrollStep);\n        event.stopPropagation = true;\n    }),\n    (0, _decorators.onChildrenEvents)()\n], ScrollView.prototype, \"handleEvent\", null);",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/ShowOnHover.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"ShowOnHover\", {\n    enumerable: true,\n    get: function() {\n        return ShowOnHover;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _index1 = require(\"../../Lib/Vector/index.js\");\nconst _index2 = require(\"../Hitboxes/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass ShowOnHover extends _index.GameObject {\n    setContext(context) {\n        super.setContext(context);\n        this.child.setContext(context);\n        this.child.scene = this.scene;\n    }\n    render(canvas, scene) {\n        if (!this.hovering) return;\n        this.child.render(canvas, scene);\n    }\n    constructor(child, size, position){\n        const anchor = position ? position.clone() : child.getPosition().clone();\n        super(\"ShowOnHover\", anchor), _define_property(this, \"child\", void 0);\n        this.child = child;\n        // Keep the child positioned relative to this anchor so it moves along with scroll views.\n        const relativeChildPosition = child.getPosition().clone().toSubtracted(anchor);\n        this.child.setPosition(relativeChildPosition);\n        this.child.setPositionRelativeToMotherShip(true);\n        this.child.setMotherShip(this);\n        this.addChild(this.child);\n        this.addHitbox(new _index2.SquareHitbox(_index1.Vector.zero(), size, this));\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/Text.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"Text\", {\n    enumerable: true,\n    get: function() {\n        return Text;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _index1 = require(\"../../Lib/Vector/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass Text extends _index.GameObject {\n    setText(text) {\n        this.text = text;\n    }\n    getText() {\n        return this.text;\n    }\n    render(canvas, scene) {\n        super.render(canvas, scene);\n    }\n    constructor(name, position, text, { color = \"black\", size = \"16px\", align = \"center\", font } = {}){\n        super(name, position), _define_property(this, \"text\", void 0), _define_property(this, \"color\", void 0), _define_property(this, \"size\", void 0), _define_property(this, \"align\", void 0), _define_property(this, \"font\", void 0), _define_property(this, \"renderText\", (obj, canvas)=>{\n            const pos = obj.getPosition();\n            canvas.getShapeDrawer().drawText(this.text, pos.x, pos.y, this.color, this.size, this.align, this.font);\n        });\n        this.text = text;\n        this.color = color;\n        this.size = size;\n        this.align = align;\n        this.font = font;\n        this.setRenderFunction(this.renderText);\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/TextBox.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"TextBox\", {\n    enumerable: true,\n    get: function() {\n        return TextBox;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _decorators = require(\"../../Events/decorators.js\");\nconst _index1 = require(\"../../Lib/index.js\");\nconst _index2 = require(\"../Hitboxes/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nvar __decorate = (void 0) && (void 0).__decorate || function(decorators, target, key, desc) {\n    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;\n    if (typeof Reflect === \"object\" && typeof Reflect.decorate === \"function\") r = Reflect.decorate(decorators, target, key, desc);\n    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;\n    return c > 3 && r && Object.defineProperty(target, key, r), r;\n};\nconst __TEXT_BOX_HEIGHT__ = 200;\nconst __SPRITE_PADDING__ = 20;\nconst calculateTextBoxPosition = (placement, ctx)=>{\n    const { height: canvasHeight } = ctx.getCanvas().getCanvas();\n    switch(placement){\n        case \"top\":\n            return new _index1.Vector(0, 0);\n        case \"middle\":\n            return new _index1.Vector(0, canvasHeight / 2 - __TEXT_BOX_HEIGHT__ / 2);\n        case \"bottom\":\n            return new _index1.Vector(0, canvasHeight - __TEXT_BOX_HEIGHT__);\n    }\n};\nclass TextBox extends _index.GameObject {\n    initializeTextBox() {\n        const { width: canvasWidth } = this.getContext().getCanvas().getCanvas();\n        const textboxHitbox = new _index2.SquareHitbox(_index1.Vector.zero(), new _index1.Vector(canvasWidth, __TEXT_BOX_HEIGHT__), this, {\n            solid: false\n        });\n        // Adding hitbox so it can be clicked\n        this.addHitbox(textboxHitbox);\n        // Setting the position accordingly to it's preset\n        this.setPosition(calculateTextBoxPosition(this.options.position, this.getContext()));\n        if (this.options.skipTyping) {\n            this.renderedText = this.text;\n            this.textIndex = this.text.length - 1;\n            this.finishedTyping = true;\n        }\n    }\n    updateRenderdText() {\n        if (this.finishedTyping) {\n            return;\n        }\n        var _this_options_lettersPerTick;\n        const lettersPerTick = (_this_options_lettersPerTick = this.options.lettersPerTick) !== null && _this_options_lettersPerTick !== void 0 ? _this_options_lettersPerTick : 1;\n        this.textIndex += Math.round(lettersPerTick);\n        if (this.textIndex > this.text.length) {\n            this.renderedText = this.text;\n            this.finishedTyping = true;\n        } else {\n            this.renderedText = this.text.substring(0, this.textIndex);\n        }\n    }\n    requestAdvance() {\n        if (this.finishedTyping) {\n            this.onFinished();\n            this.destroy();\n        } else {\n            this.finishedTyping = true;\n            this.renderedText = this.text;\n            this.textIndex = this.text.length - 1;\n        }\n    }\n    tick() {\n        super.tick();\n        this.updateRenderdText();\n    }\n    getSpritePosition() {\n        const sprite = this.options.sprite;\n        if (!sprite) return _index1.Vector.zero();\n        const ctx = this.getContext();\n        if (!ctx) return _index1.Vector.zero();\n        const canvasWidth = ctx.getCanvas().getCanvas().width;\n        const portraitSide = __TEXT_BOX_HEIGHT__; // 200px square area\n        var _ctx_getCanvas_getSpriteLibrary_getSpriteSheetFrameSize;\n        const frameSize = (_ctx_getCanvas_getSpriteLibrary_getSpriteSheetFrameSize = ctx.getCanvas().getSpriteLibrary().getSpriteSheetFrameSize(sprite.spritesheetName)) !== null && _ctx_getCanvas_getSpriteLibrary_getSpriteSheetFrameSize !== void 0 ? _ctx_getCanvas_getSpriteLibrary_getSpriteSheetFrameSize : null;\n        var _frameSize_frameWidth;\n        const frameWidth = (_frameSize_frameWidth = frameSize === null || frameSize === void 0 ? void 0 : frameSize.frameWidth) !== null && _frameSize_frameWidth !== void 0 ? _frameSize_frameWidth : 32;\n        var _frameSize_frameHeight;\n        const frameHeight = (_frameSize_frameHeight = frameSize === null || frameSize === void 0 ? void 0 : frameSize.frameHeight) !== null && _frameSize_frameHeight !== void 0 ? _frameSize_frameHeight : 32;\n        const spriteWidthPx = frameWidth * sprite.scale;\n        const spriteHeightPx = frameHeight * sprite.scale;\n        const base = this.getPosition();\n        const portraitLeft = sprite.position === \"left\" ? base.x + __SPRITE_PADDING__ : base.x + canvasWidth - __SPRITE_PADDING__ - portraitSide;\n        return new _index1.Vector(portraitLeft + (portraitSide - spriteWidthPx) / 2, base.y + (portraitSide - spriteHeightPx) / 2);\n    }\n    getTextPosition() {\n        const canvasWidth = this.getContext().getCanvas().getCanvas().width;\n        let upperCorner = this.getPosition().toAdded(new _index1.Vector(__SPRITE_PADDING__, __SPRITE_PADDING__));\n        let downCorner = upperCorner.toAdded(new _index1.Vector(canvasWidth - __SPRITE_PADDING__ * 2, __TEXT_BOX_HEIGHT__ - __SPRITE_PADDING__ * 2));\n        if (!this.options.sprite) {\n            return {\n                upperCorner,\n                downCorner\n            };\n        }\n        if (this.options.sprite.position === \"left\") {\n            return {\n                upperCorner: upperCorner.toAdded(new _index1.Vector(200, 0)),\n                downCorner\n            };\n        }\n        return {\n            upperCorner,\n            downCorner: downCorner.toAdded(new _index1.Vector(-200, 0))\n        };\n    }\n    renderTextBox(self, canvas) {\n        const { downCorner, upperCorner } = this.getTextPosition();\n        const defaultFont = canvas.getShapeDrawer().getDefaultFont();\n        const width = downCorner.x - upperCorner.x;\n        const { x, y } = this.getPosition();\n        const canvasWidth = this.getContext().getCanvas().getCanvas().width;\n        canvas.getShapeDrawer().drawRectangle(x, y, canvasWidth, __TEXT_BOX_HEIGHT__, this.options.boxColor);\n        const sprite = this.options.sprite;\n        if (sprite) {\n            const gameContext = this.getContext();\n            const frameIndex = Math.floor(gameContext.getTickCount() / sprite.ticksPerFrame) % sprite.indexes.length;\n            var _sprite_indexes_frameIndex, _ref;\n            const spriteIndex = (_ref = (_sprite_indexes_frameIndex = sprite.indexes[frameIndex]) !== null && _sprite_indexes_frameIndex !== void 0 ? _sprite_indexes_frameIndex : sprite.indexes[0]) !== null && _ref !== void 0 ? _ref : 0;\n            var _sprite_mirroring;\n            const mirroring = (_sprite_mirroring = sprite.mirroring) !== null && _sprite_mirroring !== void 0 ? _sprite_mirroring : null;\n            canvas.getSpriteLibrary().drawSpriteFrame(canvas.getContext(), sprite.spritesheetName, spriteIndex, this.getSpritePosition(), sprite.scale, mirroring === \"horizontal\" || mirroring === \"both\", mirroring === \"vertical\" || mirroring === \"both\");\n        }\n        var _this_options_textColor, _this_options_textSize;\n        canvas.getShapeDrawer().drawText(this.renderedText, upperCorner.x, upperCorner.y, (_this_options_textColor = this.options.textColor) !== null && _this_options_textColor !== void 0 ? _this_options_textColor : \"white\", `${(_this_options_textSize = this.options.textSize) !== null && _this_options_textSize !== void 0 ? _this_options_textSize : 16}px`, \"left\", defaultFont, {\n            wrapText: width,\n            baseline: \"top\"\n        });\n    }\n    handleEvent(event) {\n        super.handleEvent(event);\n    }\n    render(canvas, scene) {\n        super.render(canvas, scene);\n    }\n    constructor(name, text, options, onFinished){\n        super(name, _index1.Vector.zero()), _define_property(this, \"text\", void 0), _define_property(this, \"options\", void 0), _define_property(this, \"onFinished\", void 0), _define_property(this, \"textIndex\", 0), _define_property(this, \"renderedText\", \"\"), _define_property(this, \"finishedTyping\", false);\n        this.text = text;\n        this.options = options;\n        this.onFinished = onFinished;\n        // Setting the position once it becomes active\n        this.setTickFunction(()=>{\n            if (this.getContext()) {\n                this.initializeTextBox();\n                // We don't need to check context again after it gets initalized\n                this.setTickFunction(()=>{});\n                this.setRenderFunction(this.renderTextBox.bind(this));\n            }\n        });\n    }\n}\n__decorate([\n    (0, _decorators.onClick)((self)=>self.requestAdvance()),\n    (0, _decorators.onKeyPressed)(\" \", (self)=>self.requestAdvance())\n], TextBox.prototype, \"handleEvent\", null);",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Library/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./Button.js\"), exports);\n_export_star(require(\"./Clickableshape.js\"), exports);\n_export_star(require(\"./FloatingView.js\"), exports);\n_export_star(require(\"./Group.js\"), exports);\n_export_star(require(\"./HoverableShape.js\"), exports);\n_export_star(require(\"./SceneTranslator.js\"), exports);\n_export_star(require(\"./ScrollView.js\"), exports);\n_export_star(require(\"./ShowOnHover.js\"), exports);\n_export_star(require(\"./Text.js\"), exports);\n_export_star(require(\"./TextBox.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/Walker.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"Walker\", {\n    enumerable: true,\n    get: function() {\n        return Walker;\n    }\n});\nconst _index = require(\"../Lib/MinHeap/index.js\");\nconst _index1 = require(\"../Lib/PathfindingProxy/index.js\");\nconst _index2 = require(\"../Lib/Vector/index.js\");\nconst _index3 = require(\"./Hitboxes/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nconst normalizePathfindingOptions = (options)=>{\n    var _options_gridCellSize;\n    const gridCellSize = Math.max(1, Math.floor((_options_gridCellSize = options === null || options === void 0 ? void 0 : options.gridCellSize) !== null && _options_gridCellSize !== void 0 ? _options_gridCellSize : 16));\n    const recalculateEveryTicks = (options === null || options === void 0 ? void 0 : options.recalculateEveryTicks) == null ? Number.POSITIVE_INFINITY : Math.max(1, Math.floor(options.recalculateEveryTicks));\n    const maxExpandedNodes = (options === null || options === void 0 ? void 0 : options.maxExpandedNodes) == null ? 20000 : Math.max(1, Math.floor(options.maxExpandedNodes));\n    const maxSearchRadiusTiles = (options === null || options === void 0 ? void 0 : options.maxSearchRadiusTiles) == null ? 256 : Math.max(1, Math.floor(options.maxSearchRadiusTiles));\n    var _options_snapTargetToEdgeDistance;\n    const snapTargetToEdgeDistance = Math.max(0, (_options_snapTargetToEdgeDistance = options === null || options === void 0 ? void 0 : options.snapTargetToEdgeDistance) !== null && _options_snapTargetToEdgeDistance !== void 0 ? _options_snapTargetToEdgeDistance : gridCellSize);\n    var _options_avoidObstacles, _options_shouldRecalculatePath, _options_pathNotFoundBehavior, _options_onPathNotFound;\n    return {\n        avoidObstacles: (_options_avoidObstacles = options === null || options === void 0 ? void 0 : options.avoidObstacles) !== null && _options_avoidObstacles !== void 0 ? _options_avoidObstacles : false,\n        gridCellSize,\n        recalculateEveryTicks,\n        shouldRecalculatePath: (_options_shouldRecalculatePath = options === null || options === void 0 ? void 0 : options.shouldRecalculatePath) !== null && _options_shouldRecalculatePath !== void 0 ? _options_shouldRecalculatePath : ()=>false,\n        maxExpandedNodes,\n        maxSearchRadiusTiles,\n        pathNotFoundBehavior: (_options_pathNotFoundBehavior = options === null || options === void 0 ? void 0 : options.pathNotFoundBehavior) !== null && _options_pathNotFoundBehavior !== void 0 ? _options_pathNotFoundBehavior : \"throw\",\n        snapTargetToEdgeDistance,\n        onPathNotFound: (_options_onPathNotFound = options === null || options === void 0 ? void 0 : options.onPathNotFound) !== null && _options_onPathNotFound !== void 0 ? _options_onPathNotFound : ()=>undefined\n    };\n};\nclass Walker {\n    getTargetedWaypoint() {\n        if (this.waypoints.length === 0) return null;\n        if (this.ciclic) {\n            return this.waypoints[this.waypointIndex % this.waypoints.length];\n        }\n        const clampedIndex = Math.min(this.waypointIndex, this.waypoints.length - 1);\n        return this.waypoints[clampedIndex];\n    }\n    nextWaypoint() {\n        if (!this.ciclic && this.waypointIndex >= this.waypoints.length - 1) {\n            return this.waypoints[this.waypoints.length - 1];\n        }\n        this.waypointIndex = this.ciclic ? (this.waypointIndex + 1) % this.waypoints.length : this.waypointIndex + 1;\n        this.adjustedWaypoint = null;\n        return this.getTargetedWaypoint();\n    }\n    setWaypoints(waypoints, ciclic = false) {\n        this.waypoints = waypoints;\n        this.ciclic = ciclic;\n        this.waypointIndex = 0;\n        this.active = false;\n        this.currentPath = [];\n        this.currentPathIndex = 0;\n        this.recalcRequested = true;\n        this.adjustedWaypoint = null;\n        this.gameObject.speed = _index2.Vector.zero();\n    }\n    isActive() {\n        return this.active;\n    }\n    setOnComplete(callback) {\n        this.onComplete = callback;\n        return this;\n    }\n    toggle() {\n        this.active = !this.active;\n        if (!this.active) {\n            this.gameObject.speed = _index2.Vector.zero(); //stop moving\n            return;\n        }\n        this.recalcRequested = true;\n    }\n    start() {\n        this.active = true;\n        this.recalcRequested = true;\n        return this;\n    }\n    reset() {\n        this.waypointIndex = 0;\n        this.currentPath = [];\n        this.currentPathIndex = 0;\n        this.recalcRequested = true;\n        this.adjustedWaypoint = null;\n    }\n    hardReset() {\n        this.waypointIndex = 0;\n        this.currentPath = [];\n        this.currentPathIndex = 0;\n        this.recalcRequested = true;\n        this.adjustedWaypoint = null;\n        if (!this.waypoints[0]) {\n            return console.warn(`${this.gameObject.name} has no waypoints, impossible to hard reset it's walker!`);\n        }\n        this.gameObject.setPosition(this.waypoints[0]);\n    }\n    setPathfindingOptions(options) {\n        this.pathfindingOptions = normalizePathfindingOptions(options);\n        this.requestPathRecalculation();\n        return this;\n    }\n    requestPathRecalculation() {\n        this.recalcRequested = true;\n    }\n    getCurrentPath() {\n        return this.currentPath.slice(this.currentPathIndex).map((p)=>p.clone());\n    }\n    compactCurrentPathIfNeeded() {\n        if (this.currentPathIndex < 64) return;\n        if (this.currentPathIndex < Math.floor(this.currentPath.length / 2)) return;\n        this.currentPath = this.currentPath.slice(this.currentPathIndex);\n        this.currentPathIndex = 0;\n    }\n    getSolidHitboxes(obj) {\n        return obj.getHitboxes().filter((hitbox)=>hitbox.solid);\n    }\n    getObstacleHitboxes(scene) {\n        if (!scene) return [];\n        const obstacleHitboxes = [];\n        for (const obj of scene.getGameObjects()){\n            if (obj === this.gameObject) continue;\n            if (!obj.isActive()) continue;\n            const solid = this.getSolidHitboxes(obj);\n            if (solid.length === 0) continue;\n            obstacleHitboxes.push(...solid);\n        }\n        return obstacleHitboxes;\n    }\n    ensurePathfindingProxy() {\n        var _this_pathfindingProxy;\n        const solid = this.getSolidHitboxes(this.gameObject);\n        if (solid.length === 0) {\n            return null;\n        }\n        const signature = solid.map((hitbox)=>{\n            if (hitbox instanceof _index3.CircleHitbox) {\n                return `c:${hitbox.offset.x},${hitbox.offset.y},${hitbox.radius}`;\n            }\n            return `s:${hitbox.offset.x},${hitbox.offset.y},${hitbox.size.x},${hitbox.size.y}`;\n        }).join(\"|\");\n        if (((_this_pathfindingProxy = this.pathfindingProxy) === null || _this_pathfindingProxy === void 0 ? void 0 : _this_pathfindingProxy.signature) === signature) {\n            return this.pathfindingProxy.proxy;\n        }\n        const proxy = new _index1.PathfindingProxy(this.gameObject.getPosition().clone());\n        var _this_gameObject_rotation;\n        proxy.rotation = (_this_gameObject_rotation = this.gameObject.rotation) !== null && _this_gameObject_rotation !== void 0 ? _this_gameObject_rotation : 0;\n        proxy.angularVelocity = 0;\n        proxy.hitboxes = solid.map((hitbox)=>{\n            if (hitbox instanceof _index3.CircleHitbox) {\n                return new _index3.CircleHitbox(hitbox.offset.clone(), hitbox.radius, proxy, {\n                    debug: false,\n                    solid: true\n                });\n            }\n            return new _index3.SquareHitbox(hitbox.offset.clone(), hitbox.size.clone(), proxy, {\n                debug: false,\n                solid: true\n            });\n        });\n        this.pathfindingProxy = {\n            proxy,\n            signature\n        };\n        return proxy;\n    }\n    tileKey(tile) {\n        return `${tile.x},${tile.y}`;\n    }\n    worldToTile(position, cellSize) {\n        return {\n            x: Math.floor(position.x / cellSize),\n            y: Math.floor(position.y / cellSize)\n        };\n    }\n    tileToWorldCenter(tile, cellSize) {\n        return new _index2.Vector((tile.x + 0.5) * cellSize, (tile.y + 0.5) * cellSize);\n    }\n    isProxyPositionFree(proxy, proxyScenePosition, obstacleHitboxes) {\n        var _this_gameObject_scene;\n        var _this_gameObject_rotation;\n        proxy.rotation = (_this_gameObject_rotation = this.gameObject.rotation) !== null && _this_gameObject_rotation !== void 0 ? _this_gameObject_rotation : 0;\n        proxy.speed = _index2.Vector.zero();\n        var _this_gameObject_scene_getVisualOffset;\n        const sceneOffset = (_this_gameObject_scene_getVisualOffset = (_this_gameObject_scene = this.gameObject.scene) === null || _this_gameObject_scene === void 0 ? void 0 : _this_gameObject_scene.getVisualOffset()) !== null && _this_gameObject_scene_getVisualOffset !== void 0 ? _this_gameObject_scene_getVisualOffset : _index2.Vector.zero();\n        proxy.setPosition(proxyScenePosition.toAdded(sceneOffset));\n        for (const selfHitbox of proxy.hitboxes){\n            for (const obstacleHitbox of obstacleHitboxes){\n                if (selfHitbox.intersects(obstacleHitbox)) {\n                    return false;\n                }\n            }\n        }\n        return true;\n    }\n    isProxyEdgeFree(proxy, fromScenePosition, toScenePosition, obstacleHitboxes) {\n        const mid = fromScenePosition.toAdded(toScenePosition).toMultiplied(0.5);\n        return this.isProxyPositionFree(proxy, toScenePosition, obstacleHitboxes) && this.isProxyPositionFree(proxy, mid, obstacleHitboxes);\n    }\n    clamp(value, min, max) {\n        return Math.max(min, Math.min(max, value));\n    }\n    closestPointOnSquareEdge(hitbox, point) {\n        const vertices = hitbox.getTransformedVertices();\n        const center = vertices[0].toAdded(vertices[2]).toMultiplied(0.5);\n        const axisX = vertices[1].toSubtracted(vertices[0]).normalize();\n        const axisY = vertices[3].toSubtracted(vertices[0]).normalize();\n        const halfX = hitbox.size.x / 2;\n        const halfY = hitbox.size.y / 2;\n        const d = point.toSubtracted(center);\n        const distX = d.dotProduct(axisX);\n        const distY = d.dotProduct(axisY);\n        let clampedX = this.clamp(distX, -halfX, halfX);\n        let clampedY = this.clamp(distY, -halfY, halfY);\n        const absX = Math.abs(distX);\n        const absY = Math.abs(distY);\n        const insideX = absX <= halfX;\n        const insideY = absY <= halfY;\n        if (insideX && insideY) {\n            const distToEdgeX = halfX - absX;\n            const distToEdgeY = halfY - absY;\n            if (distToEdgeX <= distToEdgeY) {\n                clampedX = Math.sign(distX || 1) * halfX;\n            } else {\n                clampedY = Math.sign(distY || 1) * halfY;\n            }\n        }\n        return center.toAdded(axisX.toMultiplied(clampedX)).toAdded(axisY.toMultiplied(clampedY));\n    }\n    getSnappedGoal(goalScenePosition, obstacleHitboxes, proxy) {\n        const snapDistance = this.pathfindingOptions.snapTargetToEdgeDistance;\n        if (snapDistance <= 0) return null;\n        const nudgeDistance = Math.max(1, Math.min(4, this.pathfindingOptions.gridCellSize * 0.25));\n        let bestCandidate = null;\n        for (const hitbox of obstacleHitboxes){\n            let edgePoint = null;\n            let distanceToEdge = Number.POSITIVE_INFINITY;\n            let direction = null;\n            if (hitbox instanceof _index3.SquareHitbox) {\n                edgePoint = this.closestPointOnSquareEdge(hitbox, goalScenePosition);\n                distanceToEdge = goalScenePosition.toSubtracted(edgePoint).magnitude();\n                const vertices = hitbox.getTransformedVertices();\n                const center = vertices[0].toAdded(vertices[2]).toMultiplied(0.5);\n                direction = edgePoint.toSubtracted(center);\n            } else if (hitbox instanceof _index3.CircleHitbox) {\n                const center = hitbox.getTransformedPosition();\n                const delta = goalScenePosition.toSubtracted(center);\n                const deltaMagnitude = delta.magnitude();\n                const normalized = deltaMagnitude === 0 ? new _index2.Vector(1, 0) : delta.toNormalized();\n                edgePoint = center.toAdded(normalized.toMultiplied(hitbox.radius));\n                distanceToEdge = Math.abs(deltaMagnitude - hitbox.radius);\n                direction = normalized;\n            }\n            if (!edgePoint || distanceToEdge > snapDistance) continue;\n            const normalizedDirection = direction && direction.squaredMagnitude() > 0 ? direction.toNormalized() : new _index2.Vector(1, 0);\n            const candidates = [\n                edgePoint.toAdded(normalizedDirection.toMultiplied(nudgeDistance)),\n                edgePoint\n            ];\n            for (const candidate of candidates){\n                if (!this.isProxyPositionFree(proxy, candidate, obstacleHitboxes)) {\n                    continue;\n                }\n                if (!bestCandidate || distanceToEdge < bestCandidate.distance) {\n                    bestCandidate = {\n                        point: candidate,\n                        distance: distanceToEdge\n                    };\n                }\n            }\n        }\n        return bestCandidate ? bestCandidate.point.clone() : null;\n    }\n    resolvePathNotFound(startScenePosition, goalScenePosition, obstacleHitboxes, proxy) {\n        const context = {\n            walker: this,\n            gameObject: this.gameObject,\n            scene: this.gameObject.scene,\n            start: startScenePosition,\n            goal: goalScenePosition,\n            obstacleHitboxes\n        };\n        const decision = this.pathfindingOptions.onPathNotFound(context);\n        const resolved = typeof decision === \"string\" ? {\n            behavior: decision\n        } : decision;\n        var _resolved_behavior;\n        const behavior = (_resolved_behavior = resolved === null || resolved === void 0 ? void 0 : resolved.behavior) !== null && _resolved_behavior !== void 0 ? _resolved_behavior : this.pathfindingOptions.pathNotFoundBehavior;\n        if (behavior === \"throw\") {\n            throw new Error(`Walker could not find a path for ${this.gameObject.name} to ${goalScenePosition.x},${goalScenePosition.y}.`);\n        }\n        if (behavior === \"stop\") {\n            this.currentPath = [];\n            this.currentPathIndex = 0;\n            this.adjustedWaypoint = null;\n            this.gameObject.speed = _index2.Vector.zero();\n            this.active = false;\n            return {\n                stop: true\n            };\n        }\n        if (behavior === \"continue\") {\n            this.currentPath = [];\n            this.currentPathIndex = 0;\n            this.adjustedWaypoint = null;\n            return {\n                stop: false\n            };\n        }\n        const snapGoal = resolved && \"goal\" in resolved && resolved.goal ? resolved.goal : this.getSnappedGoal(goalScenePosition, obstacleHitboxes, proxy);\n        if (!snapGoal) {\n            this.currentPath = [];\n            this.currentPathIndex = 0;\n            this.adjustedWaypoint = null;\n            this.gameObject.speed = _index2.Vector.zero();\n            this.active = false;\n            return {\n                stop: true\n            };\n        }\n        const snapResult = this.computePathToTarget(startScenePosition, snapGoal);\n        if (snapResult.status === \"found\") {\n            this.currentPath = snapResult.path;\n            this.currentPathIndex = 0;\n            this.adjustedWaypoint = snapGoal.clone();\n            return {\n                stop: false\n            };\n        }\n        if (snapResult.status === \"skipped\") {\n            this.currentPath = [];\n            this.currentPathIndex = 0;\n            this.adjustedWaypoint = snapGoal.clone();\n            return {\n                stop: false\n            };\n        }\n        this.currentPath = [];\n        this.currentPathIndex = 0;\n        this.adjustedWaypoint = null;\n        this.gameObject.speed = _index2.Vector.zero();\n        this.active = false;\n        return {\n            stop: true\n        };\n    }\n    computePathToTarget(startScenePosition, goalScenePosition) {\n        if (!this.pathfindingOptions.avoidObstacles) {\n            return {\n                status: \"skipped\",\n                path: []\n            };\n        }\n        const scene = this.gameObject.scene;\n        if (!scene) {\n            return {\n                status: \"skipped\",\n                path: []\n            };\n        }\n        const proxy = this.ensurePathfindingProxy();\n        if (!proxy) {\n            return {\n                status: \"skipped\",\n                path: []\n            };\n        }\n        const obstacleHitboxes = this.getObstacleHitboxes(scene);\n        if (obstacleHitboxes.length === 0) {\n            return {\n                status: \"skipped\",\n                path: []\n            };\n        }\n        const cellSize = this.pathfindingOptions.gridCellSize;\n        const startTile = this.worldToTile(startScenePosition, cellSize);\n        const goalTile = this.worldToTile(goalScenePosition, cellSize);\n        const startKey = this.tileKey(startTile);\n        const goalKey = this.tileKey(goalTile);\n        const isInSearchRadius = (tile)=>{\n            const dx = Math.abs(tile.x - startTile.x);\n            const dy = Math.abs(tile.y - startTile.y);\n            return dx <= this.pathfindingOptions.maxSearchRadiusTiles && dy <= this.pathfindingOptions.maxSearchRadiusTiles;\n        };\n        const walkableCache = new Map();\n        const isTileWalkable = (tile)=>{\n            const key = this.tileKey(tile);\n            if (key === startKey) return true;\n            if (key === goalKey) {\n                return this.isProxyPositionFree(proxy, goalScenePosition, obstacleHitboxes);\n            }\n            if (!isInSearchRadius(tile)) return false;\n            const cached = walkableCache.get(key);\n            if (cached != null) return cached;\n            const worldCenter = this.tileToWorldCenter(tile, cellSize);\n            const ok = this.isProxyPositionFree(proxy, worldCenter, obstacleHitboxes);\n            walkableCache.set(key, ok);\n            return ok;\n        };\n        const heuristic = (a, b)=>Math.abs(a.x - b.x) + Math.abs(a.y - b.y);\n        const open = new _index.MinHeap((n)=>n.f);\n        open.push({\n            tile: startTile,\n            g: 0,\n            f: heuristic(startTile, goalTile)\n        });\n        const cameFrom = new Map();\n        const gScore = new Map([\n            [\n                startKey,\n                0\n            ]\n        ]);\n        const dirs = [\n            {\n                x: 1,\n                y: 0\n            },\n            {\n                x: -1,\n                y: 0\n            },\n            {\n                x: 0,\n                y: 1\n            },\n            {\n                x: 0,\n                y: -1\n            }\n        ];\n        let expanded = 0;\n        while(open.size > 0 && expanded < this.pathfindingOptions.maxExpandedNodes){\n            expanded++;\n            const current = open.pop();\n            const currentKey = this.tileKey(current.tile);\n            if (currentKey === goalKey) {\n                const tiles = [\n                    goalTile\n                ];\n                let cursorKey = goalKey;\n                while(cursorKey !== startKey){\n                    const parentKey = cameFrom.get(cursorKey);\n                    if (!parentKey) break;\n                    const [x, y] = parentKey.split(\",\").map((v)=>Number(v));\n                    tiles.push({\n                        x: x,\n                        y: y\n                    });\n                    cursorKey = parentKey;\n                }\n                tiles.reverse();\n                const centers = tiles.map((tile)=>{\n                    const key = this.tileKey(tile);\n                    if (key === goalKey) return goalScenePosition.clone();\n                    return this.tileToWorldCenter(tile, cellSize);\n                });\n                const path = centers.slice(1);\n                if (path.length === 0) {\n                    return {\n                        status: \"found\",\n                        path: []\n                    };\n                }\n                return {\n                    status: \"found\",\n                    path: this.simplifyPath(path)\n                };\n            }\n            for (const dir of dirs){\n                const neighbor = {\n                    x: current.tile.x + dir.x,\n                    y: current.tile.y + dir.y\n                };\n                if (!isTileWalkable(neighbor)) continue;\n                const currentWorld = this.tileToWorldCenter(current.tile, cellSize);\n                const neighborKey = this.tileKey(neighbor);\n                const neighborWorld = neighborKey === goalKey ? goalScenePosition : this.tileToWorldCenter(neighbor, cellSize);\n                if (currentKey !== startKey && !this.isProxyEdgeFree(proxy, currentWorld, neighborWorld, obstacleHitboxes)) {\n                    continue;\n                }\n                const tentativeG = current.g + 1;\n                const bestKnownG = gScore.get(neighborKey);\n                if (bestKnownG != null && tentativeG >= bestKnownG) {\n                    continue;\n                }\n                cameFrom.set(neighborKey, currentKey);\n                gScore.set(neighborKey, tentativeG);\n                open.push({\n                    tile: neighbor,\n                    g: tentativeG,\n                    f: tentativeG + heuristic(neighbor, goalTile)\n                });\n            }\n        }\n        return {\n            status: \"no_path\",\n            path: [],\n            obstacleHitboxes,\n            proxy\n        };\n    }\n    simplifyPath(path) {\n        if (path.length <= 2) return path;\n        const simplified = [\n            path[0].clone()\n        ];\n        for(let i = 1; i < path.length - 1; i++){\n            const prev = simplified[simplified.length - 1];\n            const current = path[i];\n            const next = path[i + 1];\n            const a = current.toSubtracted(prev);\n            const b = next.toSubtracted(current);\n            const cross = a.x * b.y - a.y * b.x;\n            const collinear = Math.abs(cross) < 0.0001;\n            const sameDirection = a.dotProduct(b) >= 0;\n            if (collinear && sameDirection) {\n                continue;\n            }\n            simplified.push(current.clone());\n        }\n        simplified.push(path[path.length - 1].clone());\n        return simplified;\n    }\n    tick() {\n        if (!this.active || this.waypoints.length === 0) {\n            return;\n        }\n        this.tickCounter++;\n        const currentPosition = this.gameObject.getScenePosition();\n        const targetedWaypoint = this.getTargetedWaypoint();\n        if (!targetedWaypoint) {\n            var _this_onComplete, _this;\n            (_this_onComplete = (_this = this).onComplete) === null || _this_onComplete === void 0 ? void 0 : _this_onComplete.call(_this);\n            return;\n        }\n        if (!this.pathfindingOptions.avoidObstacles) {\n            this.currentPath = [];\n            this.currentPathIndex = 0;\n            this.recalcRequested = false;\n            this.adjustedWaypoint = null;\n        } else {\n            var _this_gameObject_scene;\n            var _this_gameObject_scene_getGameObjects;\n            const shouldRecalculate = this.recalcRequested || this.tickCounter % this.pathfindingOptions.recalculateEveryTicks === 0 || this.pathfindingOptions.shouldRecalculatePath({\n                walker: this,\n                gameObject: this.gameObject,\n                scene: this.gameObject.scene,\n                sceneObjects: (_this_gameObject_scene_getGameObjects = (_this_gameObject_scene = this.gameObject.scene) === null || _this_gameObject_scene === void 0 ? void 0 : _this_gameObject_scene.getGameObjects()) !== null && _this_gameObject_scene_getGameObjects !== void 0 ? _this_gameObject_scene_getGameObjects : [],\n                tick: this.tickCounter,\n                start: currentPosition,\n                goal: targetedWaypoint,\n                currentPath: this.currentPath,\n                currentPathIndex: this.currentPathIndex\n            });\n            if (shouldRecalculate) {\n                this.recalcRequested = false;\n                const pathResult = this.computePathToTarget(currentPosition, targetedWaypoint);\n                if (pathResult.status === \"found\") {\n                    this.currentPath = pathResult.path;\n                    this.currentPathIndex = 0;\n                    this.adjustedWaypoint = null;\n                } else if (pathResult.status === \"skipped\") {\n                    this.currentPath = [];\n                    this.currentPathIndex = 0;\n                    this.adjustedWaypoint = null;\n                } else if (pathResult.obstacleHitboxes && pathResult.proxy) {\n                    const result = this.resolvePathNotFound(currentPosition, targetedWaypoint, pathResult.obstacleHitboxes, pathResult.proxy);\n                    if (result.stop) {\n                        return;\n                    }\n                }\n            }\n        }\n        // Drop nodes that were reached via collisions/teleports.\n        while(this.currentPathIndex < this.currentPath.length && this.currentPath[this.currentPathIndex].toSubtracted(currentPosition).squaredMagnitude() <= 0.5){\n            this.currentPathIndex++;\n        }\n        this.compactCurrentPathIfNeeded();\n        var _this_adjustedWaypoint, _this_currentPath_this_currentPathIndex;\n        const movementTarget = (_this_currentPath_this_currentPathIndex = this.currentPath[this.currentPathIndex]) !== null && _this_currentPath_this_currentPathIndex !== void 0 ? _this_currentPath_this_currentPathIndex : (_this_adjustedWaypoint = this.adjustedWaypoint) !== null && _this_adjustedWaypoint !== void 0 ? _this_adjustedWaypoint : targetedWaypoint;\n        const movementVector = movementTarget.toSubtracted(currentPosition);\n        const movementDirection = movementVector.toNormalized();\n        const intendedMovementVector = movementDirection.toMultiplied(this.speed);\n        const maxMovement = movementVector.squaredMagnitude();\n        const shouldClipMovement = intendedMovementVector.squaredMagnitude() >= maxMovement;\n        const movement = shouldClipMovement ? movementVector : intendedMovementVector;\n        if (shouldClipMovement) {\n            if (this.currentPathIndex < this.currentPath.length) {\n                // Reached a path node (not the actual waypoint yet).\n                this.currentPathIndex++;\n                this.compactCurrentPathIfNeeded();\n            } else {\n                const isAtLastWaypoint = !this.ciclic && this.waypointIndex >= this.waypoints.length - 1;\n                if (isAtLastWaypoint) {\n                    var _this_onComplete1, _this1;\n                    var _this_adjustedWaypoint1;\n                    // Snap to the final waypoint, stop, and notify listeners.\n                    this.gameObject.setPosition(((_this_adjustedWaypoint1 = this.adjustedWaypoint) !== null && _this_adjustedWaypoint1 !== void 0 ? _this_adjustedWaypoint1 : targetedWaypoint).clone());\n                    this.gameObject.speed = _index2.Vector.zero();\n                    this.active = false;\n                    (_this_onComplete1 = (_this1 = this).onComplete) === null || _this_onComplete1 === void 0 ? void 0 : _this_onComplete1.call(_this1);\n                    return;\n                }\n                this.currentPath = [];\n                this.currentPathIndex = 0;\n                this.recalcRequested = true;\n                this.adjustedWaypoint = null;\n                this.nextWaypoint();\n            }\n        }\n        this.gameObject.speed = movement;\n    }\n    renderDebug(canvas) {\n        if (!this.debug) return;\n        const drawer = canvas.getShapeDrawer();\n        var _this_gameObject_rotation;\n        const rotation = (_this_gameObject_rotation = this.gameObject.rotation) !== null && _this_gameObject_rotation !== void 0 ? _this_gameObject_rotation : 0;\n        const draw = ()=>{\n            var _this_gameObject_scene;\n            var _this_gameObject_scene_getVisualOffset;\n            const sceneOffset = (_this_gameObject_scene_getVisualOffset = (_this_gameObject_scene = this.gameObject.scene) === null || _this_gameObject_scene === void 0 ? void 0 : _this_gameObject_scene.getVisualOffset()) !== null && _this_gameObject_scene_getVisualOffset !== void 0 ? _this_gameObject_scene_getVisualOffset : _index2.Vector.zero();\n            this.waypoints.forEach((waypoint, index)=>{\n                const renderedWaypoint = waypoint.toAdded(sceneOffset);\n                drawer.drawCircle(renderedWaypoint.x, renderedWaypoint.y, 8, \"red\", true);\n                if (index == 0) return;\n                drawer.drawLine(this.waypoints[index - 1].toAdded(sceneOffset), renderedWaypoint, 8);\n            });\n            if (this.ciclic && this.waypoints.length > 1) {\n                drawer.drawLine(this.waypoints[this.waypoints.length - 1].toAdded(sceneOffset), this.waypoints[0].toAdded(sceneOffset), 8);\n            }\n            if (this.currentPathIndex < this.currentPath.length) {\n                const start = this.gameObject.getPosition();\n                let previous = start;\n                for(let index = this.currentPathIndex; index < this.currentPath.length; index++){\n                    const node = this.currentPath[index];\n                    const rendered = node.toAdded(sceneOffset);\n                    drawer.drawCircle(rendered.x, rendered.y, 6, \"cyan\", true);\n                    drawer.drawLine(previous, rendered, 4, \"cyan\");\n                    previous = rendered;\n                }\n            }\n        };\n        // Walker debug drawing is called from within the GameObject's rotation block.\n        // Cancel it so waypoints/path are drawn in world space (non-rotated).\n        if (rotation !== 0) {\n            const pivot = this.gameObject.getRotationCenter();\n            drawer.withRotation(pivot.x, pivot.y, -rotation, draw);\n            return;\n        }\n        draw();\n    }\n    constructor(gameObject, waypoints = [], speed, debug = false, ciclic = true, pathfindingOptions){\n        _define_property(this, \"gameObject\", void 0);\n        _define_property(this, \"waypoints\", void 0);\n        _define_property(this, \"speed\", void 0);\n        _define_property(this, \"debug\", void 0);\n        _define_property(this, \"waypointIndex\", 0);\n        _define_property(this, \"active\", false);\n        _define_property(this, \"ciclic\", void 0);\n        _define_property(this, \"onComplete\", void 0);\n        _define_property(this, \"pathfindingOptions\", normalizePathfindingOptions());\n        _define_property(this, \"currentPath\", []);\n        _define_property(this, \"currentPathIndex\", 0);\n        _define_property(this, \"recalcRequested\", true);\n        _define_property(this, \"tickCounter\", 0);\n        _define_property(this, \"adjustedWaypoint\", null);\n        _define_property(this, \"pathfindingProxy\", null);\n        this.gameObject = gameObject;\n        this.waypoints = waypoints;\n        this.speed = speed;\n        this.debug = debug;\n        this.ciclic = ciclic;\n        if (pathfindingOptions) {\n            this.pathfindingOptions = normalizePathfindingOptions(pathfindingOptions);\n        }\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/GameObject/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"GameObject\", {\n    enumerable: true,\n    get: function() {\n        return GameObject;\n    }\n});\nconst _index = require(\"../CanvasController/index.js\");\nconst _index1 = require(\"../Lib/Vector/index.js\");\nconst _index2 = require(\"./Hitboxes/index.js\");\nconst _decorators = require(\"../Events/decorators.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nvar __decorate = (void 0) && (void 0).__decorate || function(decorators, target, key, desc) {\n    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;\n    if (typeof Reflect === \"object\" && typeof Reflect.decorate === \"function\") r = Reflect.decorate(decorators, target, key, desc);\n    else for(var i = decorators.length - 1; i >= 0; i--)if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;\n    return c > 3 && r && Object.defineProperty(target, key, r), r;\n};\nclass GameObject {\n    tick() {\n        if (this.active) {\n            var _this_walker;\n            this.runKeyTickHandlers();\n            this.tickFn(this);\n            (_this_walker = this.walker) === null || _this_walker === void 0 ? void 0 : _this_walker.tick();\n            for (const child of this.children){\n                child.tick();\n            }\n        // Using this way messes up with the 'this' keyword\n        // this.children.forEach((child) => child.tick());\n        }\n    }\n    onAddedToScene(_scene, _context) {}\n    onRemovedFromScene(_scene) {}\n    notifyAddedToScene(scene, context) {\n        if (this.didNotifyAddedToScene || !context) {\n            return;\n        }\n        this.didNotifyAddedToScene = true;\n        this.onAddedToScene(scene, context);\n    }\n    notifyRemovedFromScene(scene) {\n        if (!this.didNotifyAddedToScene) {\n            return;\n        }\n        this.didNotifyAddedToScene = false;\n        this.onRemovedFromScene(scene);\n    }\n    /**\n     * Runs when an overlap is detected, before resolution/impulses are applied.\n     * Return `false` to ignore collision handling with `otherGO`.\n     */ beforeColision(otherGO) {\n        return true;\n    }\n    onColision(otherGO, penetration) {}\n    destroy() {\n        var // Remove from scene graph and parent to stop rendering/ticking.\n        _this_motherShip, _this_scene;\n        this.active = false;\n        this.visible = false;\n        (_this_motherShip = this.motherShip) === null || _this_motherShip === void 0 ? void 0 : _this_motherShip.removeChild(this);\n        this.motherShip = null;\n        // Destroy children so they don't linger in the scene.\n        this.children.forEach((child)=>child.destroy());\n        (_this_scene = this.scene) === null || _this_scene === void 0 ? void 0 : _this_scene.removeGameObject(this);\n    }\n    setWalker(walker) {\n        this.walker = walker;\n    }\n    setPhisics(phisics) {\n        this.phisics = {\n            ...this.phisics,\n            ...phisics\n        };\n    }\n    setPosition(position) {\n        this.position = position;\n    }\n    translate(delta) {\n        this.position.add(delta);\n    }\n    setMotherShip(motherShip) {\n        this.motherShip = motherShip;\n    }\n    getMotherShip() {\n        return this.motherShip;\n    }\n    addHitbox(hitbox) {\n        if (this.hitboxes.length >= 5) {\n            console.warn(`GameObject ${this.name} has too many hitboxes.` + `This may cause performance issues.`);\n        }\n        this.hitboxes.push(hitbox);\n    }\n    getHitboxes() {\n        return this.hitboxes;\n    }\n    getRotationCenter() {\n        if (this.hitboxes.length === 0) {\n            return this.getPosition();\n        }\n        let minX = Number.POSITIVE_INFINITY;\n        let maxX = Number.NEGATIVE_INFINITY;\n        let minY = Number.POSITIVE_INFINITY;\n        let maxY = Number.NEGATIVE_INFINITY;\n        for (const hitbox of this.hitboxes){\n            if (hitbox instanceof _index2.CircleHitbox) {\n                const center = hitbox.getAbsolutePosition();\n                minX = Math.min(minX, center.x - hitbox.radius);\n                maxX = Math.max(maxX, center.x + hitbox.radius);\n                minY = Math.min(minY, center.y - hitbox.radius);\n                maxY = Math.max(maxY, center.y + hitbox.radius);\n                continue;\n            }\n            const topLeft = hitbox.getAbsolutePosition();\n            minX = Math.min(minX, topLeft.x);\n            maxX = Math.max(maxX, topLeft.x + hitbox.size.x);\n            minY = Math.min(minY, topLeft.y);\n            maxY = Math.max(maxY, topLeft.y + hitbox.size.y);\n        }\n        return new _index1.Vector((minX + maxX) / 2, (minY + maxY) / 2);\n    }\n    render(canvas, scene) {\n        if (!this.visible) return;\n        const renderSelfAndChildren = ()=>{\n            var _this_walker;\n            (_this_walker = this.walker) === null || _this_walker === void 0 ? void 0 : _this_walker.renderDebug(canvas);\n            this.renderFn(this, canvas);\n            this.children.forEach((child)=>child.render(canvas, scene));\n        };\n        const renderDebug = ()=>{\n            this.hitboxes.forEach((hitbox)=>{\n                hitbox.renderDebug(canvas);\n            });\n            if (this.showOriginDebug) {\n                const origin = this.getPosition();\n                canvas.getShapeDrawer().drawCircle(origin.x, origin.y, 8, \"red\", true, true, \"white\", 4);\n            }\n        };\n        const renderAll = ()=>{\n            if (this.rotation !== 0) {\n                const center = this.getRotationCenter();\n                canvas.getShapeDrawer().withRotation(center.x, center.y, this.rotation, ()=>{\n                    renderSelfAndChildren();\n                    renderDebug();\n                });\n                return;\n            }\n            renderSelfAndChildren();\n            renderDebug();\n        };\n        if (this.opacity < 1) {\n            canvas.getShapeDrawer().withOpacity(this.opacity, renderAll);\n            return;\n        }\n        renderAll();\n    }\n    setOriginDebug(show) {\n        this.showOriginDebug = show;\n    }\n    addChild(child) {\n        child.setMotherShip(this);\n        child.scene = this.scene;\n        child.setContext(this.context);\n        this.children.push(child);\n        if (this.scene) {\n            child.notifyAddedToScene(this.scene, this.context);\n        }\n    }\n    removeChild(gameObject) {\n        if (this.scene) {\n            gameObject.notifyRemovedFromScene(this.scene);\n        }\n        this.children = this.children.filter((child)=>child !== gameObject);\n    }\n    getDominantDirection() {\n        if (this.speed.isZero()) return this.lastDominantDirection;\n        const movement = this.speed.toNormalized();\n        const directions = [\n            {\n                dir: \"down\",\n                vec: new _index1.Vector(0, 1)\n            },\n            {\n                dir: \"up\",\n                vec: new _index1.Vector(0, -1)\n            },\n            {\n                dir: \"left\",\n                vec: new _index1.Vector(-1, 0)\n            },\n            {\n                dir: \"right\",\n                vec: new _index1.Vector(1, 0)\n            }\n        ];\n        const initial = directions[0];\n        const best = directions.slice(1).reduce((current, candidate)=>{\n            const candidateScore = movement.dotProduct(candidate.vec);\n            return candidateScore > current.score ? {\n                dir: candidate.dir,\n                score: candidateScore\n            } : current;\n        }, {\n            dir: initial.dir,\n            score: movement.dotProduct(initial.vec)\n        });\n        this.lastDominantDirection = best.dir;\n        return this.lastDominantDirection;\n    }\n    getChildren() {\n        return this.children;\n    }\n    getChildByName(name) {\n        return this.children.find((child)=>child.name === name) || null;\n    }\n    getPosition() {\n        const sceneOffset = this.scene ? this.scene.getVisualOffset() : _index1.Vector.zero();\n        const absolute = this.getAbsolutePosition();\n        return absolute.toAdded(sceneOffset);\n    }\n    /**\n     * Position in scene/world space (not camera/canvas space).\n     * This does not include the Scene's offset.\n     */ getScenePosition() {\n        return this.getAbsolutePosition();\n    }\n    getAbsolutePosition() {\n        if (this.positionRelativeToMotherShip && this.motherShip) {\n            return this.motherShip.getAbsolutePosition().toAdded(this.position);\n        }\n        return this.position.clone();\n    }\n    setPositionRelativeToMotherShip(useRelative) {\n        this.positionRelativeToMotherShip = useRelative;\n    }\n    isPositionRelativeToMotherShip() {\n        return this.positionRelativeToMotherShip;\n    }\n    setOpacity(opacity) {\n        this.opacity = Math.max(0, Math.min(1, opacity));\n    }\n    getOpacity() {\n        return this.opacity;\n    }\n    isVisible() {\n        return this.visible;\n    }\n    isActive() {\n        return this.active;\n    }\n    setRenderFunction(fn) {\n        this.renderFn = fn;\n    }\n    setTickFunction(fn) {\n        this.tickFn = fn;\n    }\n    handleEvent(event) {}\n    setContext(context) {\n        this.context = context;\n        this.children.forEach((child)=>{\n            child.setContext(context);\n            if (this.scene) {\n                child.notifyAddedToScene(this.scene, context);\n            }\n        });\n    }\n    getContext() {\n        return this.context;\n    }\n    onMessage(channel, handler) {\n        if (!this.context) {\n            return ()=>{};\n        }\n        return this.context.subscribeToMessage(channel, handler);\n    }\n    onceOnMessage(channel, handler) {\n        let unsubscribe = ()=>{};\n        unsubscribe = this.onMessage(channel, (payload, sender)=>{\n            unsubscribe();\n            handler(payload, sender);\n        });\n        return unsubscribe;\n    }\n    sendMessage(channel, payload) {\n        var _this_context;\n        (_this_context = this.context) === null || _this_context === void 0 ? void 0 : _this_context.sendMessage(channel, payload, this);\n    }\n    runKeyTickHandlers() {\n        const handlers = [];\n        let proto = Object.getPrototypeOf(this);\n        // acumulate handlers from the prototype chain\n        // this includes this class and all its superclasses\n        // neat JS trick!\n        while(proto){\n            const protoHandlers = proto[_decorators.KEY_TICK_HANDLERS];\n            if (protoHandlers) {\n                handlers.push(...protoHandlers);\n            }\n            proto = Object.getPrototypeOf(proto);\n        }\n        handlers.forEach((handler)=>handler(this));\n    }\n    constructor(name, position, visible = true, active = true, hitboxes = [], scene = null, children = [], showOriginDebug = false){\n        _define_property(this, \"name\", void 0);\n        _define_property(this, \"position\", void 0);\n        _define_property(this, \"visible\", void 0);\n        _define_property(this, \"active\", void 0);\n        _define_property(this, \"hitboxes\", void 0);\n        _define_property(this, \"scene\", void 0);\n        _define_property(this, \"children\", void 0);\n        _define_property(this, \"renderFn\", (obj, canvas)=>{});\n        _define_property(this, \"tickFn\", (obj)=>{});\n        _define_property(this, \"context\", null);\n        _define_property(this, \"didNotifyAddedToScene\", false);\n        _define_property(this, \"hovering\", false);\n        _define_property(this, \"motherShip\", null);\n        _define_property(this, \"walker\", null);\n        _define_property(this, \"speed\", _index1.Vector.zero());\n        _define_property(this, \"rotation\", 0);\n        _define_property(this, \"angularVelocity\", 0);\n        _define_property(this, \"showOriginDebug\", false);\n        _define_property(this, \"zIndex\", 0);\n        _define_property(this, \"opacity\", 1);\n        _define_property(this, \"lastDominantDirection\", \"down\");\n        _define_property(this, \"positionRelativeToMotherShip\", false);\n        _define_property(this, \"beingGrabbed\", false);\n        _define_property(this, \"phisics\", {\n            immovable: true,\n            restitution: 1,\n            affectedByGravity: false,\n            friction: 0,\n            mass: 1,\n            inertiaScale: 1\n        });\n        _define_property(this, \"id\", Math.floor(Math.random() * 0xffffffff).toString(16));\n        this.name = name;\n        this.position = position;\n        this.visible = visible;\n        this.active = active;\n        this.hitboxes = hitboxes;\n        this.scene = scene;\n        this.children = children;\n        this.showOriginDebug = showOriginDebug;\n    }\n}\n__decorate([\n    (0, _decorators.onHover)((obj, event)=>{\n        obj.hovering = true;\n    }),\n    (0, _decorators.onStopHovering)((obj, event)=>{\n        obj.hovering = false;\n    })\n], GameObject.prototype, \"handleEvent\", null);",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Lib/Math.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"clamp\", {\n    enumerable: true,\n    get: function() {\n        return clamp;\n    }\n});\nconst clamp = (value, min, max)=>{\n    return Math.min(Math.max(value, min), max);\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Lib/MinHeap/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"MinHeap\", {\n    enumerable: true,\n    get: function() {\n        return MinHeap;\n    }\n});\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass MinHeap {\n    get size() {\n        return this.data.length;\n    }\n    push(item) {\n        this.data.push(item);\n        this.bubbleUp(this.data.length - 1);\n    }\n    pop() {\n        if (this.data.length === 0) return undefined;\n        const first = this.data[0];\n        const last = this.data.pop();\n        if (this.data.length > 0) {\n            this.data[0] = last;\n            this.bubbleDown(0);\n        }\n        return first;\n    }\n    bubbleUp(index) {\n        while(index > 0){\n            const parent = Math.floor((index - 1) / 2);\n            if (this.getScore(this.data[index]) >= this.getScore(this.data[parent])) {\n                break;\n            }\n            [this.data[index], this.data[parent]] = [\n                this.data[parent],\n                this.data[index]\n            ];\n            index = parent;\n        }\n    }\n    bubbleDown(index) {\n        const length = this.data.length;\n        while(true){\n            const left = index * 2 + 1;\n            const right = left + 1;\n            let smallest = index;\n            if (left < length && this.getScore(this.data[left]) < this.getScore(this.data[smallest])) {\n                smallest = left;\n            }\n            if (right < length && this.getScore(this.data[right]) < this.getScore(this.data[smallest])) {\n                smallest = right;\n            }\n            if (smallest === index) {\n                return;\n            }\n            [this.data[index], this.data[smallest]] = [\n                this.data[smallest],\n                this.data[index]\n            ];\n            index = smallest;\n        }\n    }\n    constructor(getScore){\n        _define_property(this, \"getScore\", void 0);\n        _define_property(this, \"data\", []);\n        this.getScore = getScore;\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Lib/Mixins/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"mixin\", {\n    enumerable: true,\n    get: function() {\n        return mixin;\n    }\n});\nconst _mixin = {\n    before () {\n        return function(cb) {\n            return function(_t, _k, descriptor) {\n                const original = descriptor.value;\n                descriptor.value = function(...args) {\n                    const shouldCancel = cb(this, ...args);\n                    if (shouldCancel === true) return undefined;\n                    return original.apply(this, args);\n                };\n                return descriptor;\n            };\n        };\n    },\n    after () {\n        return function(cb) {\n            return function(_t, _k, descriptor) {\n                const original = descriptor.value;\n                descriptor.value = function(...args) {\n                    const result = original.apply(this, args);\n                    const newResult = cb(this, result, ...args);\n                    return newResult !== null && newResult !== void 0 ? newResult : result;\n                };\n                return descriptor;\n            };\n        };\n    },\n    replace () {\n        return function(cb) {\n            return function(_t, _k, descriptor) {\n                descriptor.value = function(...args) {\n                    return cb(this, ...args);\n                };\n                return descriptor;\n            };\n        };\n    }\n};\nconst mixin = {\n    before: ()=>_mixin.before(),\n    after: ()=>_mixin.after(),\n    replace: ()=>_mixin.replace()\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Lib/PathfindingProxy/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"PathfindingProxy\", {\n    enumerable: true,\n    get: function() {\n        return PathfindingProxy;\n    }\n});\nconst _index = require(\"../../GameObject/Hitboxes/index.js\");\nconst _index1 = require(\"../Vector/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass PathfindingProxy {\n    setPosition(position) {\n        this.position = position;\n    }\n    getPosition() {\n        return this.position;\n    }\n    getIntendedNextPosition() {\n        return this.getPosition().toAdded(this.speed);\n    }\n    getRotationCenter() {\n        if (this.hitboxes.length === 0) {\n            return this.getPosition();\n        }\n        let minX = Number.POSITIVE_INFINITY;\n        let maxX = Number.NEGATIVE_INFINITY;\n        let minY = Number.POSITIVE_INFINITY;\n        let maxY = Number.NEGATIVE_INFINITY;\n        for (const hitbox of this.hitboxes){\n            if (hitbox instanceof _index.CircleHitbox) {\n                const center = hitbox.getAbsolutePosition();\n                minX = Math.min(minX, center.x - hitbox.radius);\n                maxX = Math.max(maxX, center.x + hitbox.radius);\n                minY = Math.min(minY, center.y - hitbox.radius);\n                maxY = Math.max(maxY, center.y + hitbox.radius);\n                continue;\n            }\n            const topLeft = hitbox.getAbsolutePosition();\n            minX = Math.min(minX, topLeft.x);\n            maxX = Math.max(maxX, topLeft.x + hitbox.size.x);\n            minY = Math.min(minY, topLeft.y);\n            maxY = Math.max(maxY, topLeft.y + hitbox.size.y);\n        }\n        return new _index1.Vector((minX + maxX) / 2, (minY + maxY) / 2);\n    }\n    constructor(position){\n        _define_property(this, \"position\", void 0);\n        _define_property(this, \"speed\", _index1.Vector.zero());\n        _define_property(this, \"rotation\", 0);\n        _define_property(this, \"angularVelocity\", 0);\n        _define_property(this, \"hitboxes\", []);\n        this.position = position;\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Lib/Vector/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"Vector\", {\n    enumerable: true,\n    get: function() {\n        return Vector;\n    }\n});\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass Vector {\n    magnitude() {\n        return Math.sqrt(this.squaredMagnitude());\n    }\n    squaredMagnitude() {\n        return this.x * this.x + this.y * this.y;\n    }\n    isZero(epsilon = 0) {\n        const resolvedEpsilon = Math.max(0, epsilon);\n        return this.squaredMagnitude() <= resolvedEpsilon * resolvedEpsilon;\n    }\n    add(other) {\n        this.x += other.x;\n        this.y += other.y;\n        return this;\n    }\n    toAdded(other) {\n        return new Vector(this.x + other.x, this.y + other.y);\n    }\n    subtract(other) {\n        this.x -= other.x;\n        this.y -= other.y;\n        return this;\n    }\n    toSubtracted(other) {\n        return new Vector(this.x - other.x, this.y - other.y);\n    }\n    multiply(scalar) {\n        this.x *= scalar;\n        this.y *= scalar;\n        return this;\n    }\n    toMultiplied(scalar) {\n        return new Vector(this.x * scalar, this.y * scalar);\n    }\n    dotProduct(other) {\n        return this.x * other.x + this.y * other.y;\n    }\n    crossProduct(other) {\n        return this.x * other.y - this.y * other.x;\n    }\n    leftPerpendicular() {\n        const nextX = -this.y;\n        const nextY = this.x;\n        this.x = nextX;\n        this.y = nextY;\n        return this;\n    }\n    toLeftPerpendicular() {\n        return this.clone().leftPerpendicular();\n    }\n    rightPerpendicular() {\n        const nextX = this.y;\n        const nextY = -this.x;\n        this.x = nextX;\n        this.y = nextY;\n        return this;\n    }\n    toRightPerpendicular() {\n        return this.clone().rightPerpendicular();\n    }\n    clone() {\n        return new Vector(this.x, this.y);\n    }\n    equals(other) {\n        return this.x === other.x && this.y === other.y;\n    }\n    static zero() {\n        return new Vector(0, 0);\n    }\n    normalize(minMagnitude = 0) {\n        const resolvedMinMagnitude = Math.max(0, minMagnitude);\n        const length = this.magnitude();\n        if (length <= resolvedMinMagnitude) {\n            return this;\n        }\n        this.x /= length;\n        this.y /= length;\n        return this;\n    }\n    toNormalized(minMagnitude = 0) {\n        return this.clone().normalize(minMagnitude);\n    }\n    rotate(angle) {\n        const cos = Math.cos(angle);\n        const sin = Math.sin(angle);\n        const nextX = this.x * cos - this.y * sin;\n        const nextY = this.x * sin + this.y * cos;\n        this.x = nextX;\n        this.y = nextY;\n        return this;\n    }\n    toRotated(angle) {\n        return this.clone().rotate(angle);\n    }\n    toWorldSpace(origin, angle) {\n        return this.toRotated(angle).add(origin);\n    }\n    toLocalSpace(origin, angle) {\n        return this.toSubtracted(origin).rotate(-angle);\n    }\n    constructor(x, y){\n        _define_property(this, \"x\", void 0);\n        _define_property(this, \"y\", void 0);\n        this.x = x;\n        this.y = y;\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Lib/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./Math.js\"), exports);\n_export_star(require(\"./MinHeap/index.js\"), exports);\n_export_star(require(\"./Mixins/index.js\"), exports);\n_export_star(require(\"./PathfindingProxy/index.js\"), exports);\n_export_star(require(\"./Vector/index.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Body/PhysicsBody.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get DEFAULT_ANGULAR_DAMPING () {\n        return DEFAULT_ANGULAR_DAMPING;\n    },\n    get DEFAULT_BODY_MASS_PROPERTIES () {\n        return DEFAULT_BODY_MASS_PROPERTIES;\n    },\n    get DEFAULT_COLLISION_FILTER () {\n        return DEFAULT_COLLISION_FILTER;\n    },\n    get DEFAULT_DYNAMIC_FRICTION () {\n        return DEFAULT_DYNAMIC_FRICTION;\n    },\n    get DEFAULT_LINEAR_DAMPING () {\n        return DEFAULT_LINEAR_DAMPING;\n    },\n    get DEFAULT_RESTITUTION () {\n        return DEFAULT_RESTITUTION;\n    },\n    get DEFAULT_SLEEP_STATE () {\n        return DEFAULT_SLEEP_STATE;\n    },\n    get DEFAULT_STATIC_FRICTION () {\n        return DEFAULT_STATIC_FRICTION;\n    },\n    get PhysicsBody () {\n        return PhysicsBody;\n    },\n    get mixFriction () {\n        return mixFriction;\n    },\n    get mixRestitution () {\n        return mixRestitution;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nconst DEFAULT_BODY_MASS_PROPERTIES = {\n    area: 1,\n    mass: 1,\n    invMass: 1,\n    centroid: _index.Vector.zero(),\n    inertia: 1,\n    invInertia: 1\n};\nconst DEFAULT_LINEAR_DAMPING = 0;\nconst DEFAULT_ANGULAR_DAMPING = 0;\nconst DEFAULT_STATIC_FRICTION = 0.6;\nconst DEFAULT_DYNAMIC_FRICTION = 0.4;\nconst DEFAULT_RESTITUTION = 0;\nconst DEFAULT_COLLISION_FILTER = {\n    category: 0x0001,\n    mask: 0xffffffff\n};\nconst DEFAULT_SLEEP_STATE = {\n    canSleep: true,\n    isSleeping: false,\n    sleepTime: 0\n};\nconst assertFiniteNumber = (value, label)=>{\n    if (!Number.isFinite(value)) {\n        throw new Error(`${label} must be a finite number.`);\n    }\n};\nconst assertFiniteVector = (value, label)=>{\n    assertFiniteNumber(value.x, `${label}.x`);\n    assertFiniteNumber(value.y, `${label}.y`);\n};\nconst assertNonNegativeNumber = (value, label)=>{\n    assertFiniteNumber(value, label);\n    if (value < 0) {\n        throw new Error(`${label} must be non-negative.`);\n    }\n};\nconst cloneMassProperties = (massProperties)=>{\n    return {\n        area: massProperties.area,\n        mass: massProperties.mass,\n        invMass: massProperties.invMass,\n        centroid: massProperties.centroid.clone(),\n        inertia: massProperties.inertia,\n        invInertia: massProperties.invInertia\n    };\n};\nconst resolveCollisionFilter = (filter)=>{\n    var _filter_category, _filter_mask;\n    return {\n        category: ((_filter_category = filter === null || filter === void 0 ? void 0 : filter.category) !== null && _filter_category !== void 0 ? _filter_category : DEFAULT_COLLISION_FILTER.category) >>> 0,\n        mask: ((_filter_mask = filter === null || filter === void 0 ? void 0 : filter.mask) !== null && _filter_mask !== void 0 ? _filter_mask : DEFAULT_COLLISION_FILTER.mask) >>> 0\n    };\n};\nconst resolveSleepState = (sleep)=>{\n    var _sleep_sleepTime;\n    const sleepTime = (_sleep_sleepTime = sleep === null || sleep === void 0 ? void 0 : sleep.sleepTime) !== null && _sleep_sleepTime !== void 0 ? _sleep_sleepTime : DEFAULT_SLEEP_STATE.sleepTime;\n    assertNonNegativeNumber(sleepTime, \"Sleep time\");\n    var _sleep_canSleep, _sleep_isSleeping;\n    return {\n        canSleep: (_sleep_canSleep = sleep === null || sleep === void 0 ? void 0 : sleep.canSleep) !== null && _sleep_canSleep !== void 0 ? _sleep_canSleep : DEFAULT_SLEEP_STATE.canSleep,\n        isSleeping: (_sleep_isSleeping = sleep === null || sleep === void 0 ? void 0 : sleep.isSleeping) !== null && _sleep_isSleeping !== void 0 ? _sleep_isSleeping : DEFAULT_SLEEP_STATE.isSleeping,\n        sleepTime\n    };\n};\nconst resolveBodyMassProperties = (bodyType, massProperties)=>{\n    return {\n        localCenter: massProperties.centroid.clone(),\n        mass: massProperties.mass,\n        invMass: bodyType === \"dynamic\" ? massProperties.invMass : 0,\n        inertia: massProperties.inertia,\n        invInertia: bodyType === \"dynamic\" ? massProperties.invInertia : 0\n    };\n};\nconst mixFriction = (frictionA, frictionB)=>{\n    assertNonNegativeNumber(frictionA, \"Friction\");\n    assertNonNegativeNumber(frictionB, \"Friction\");\n    return Math.sqrt(frictionA * frictionB);\n};\nconst mixRestitution = (restitutionA, restitutionB)=>{\n    assertNonNegativeNumber(restitutionA, \"Restitution\");\n    assertNonNegativeNumber(restitutionB, \"Restitution\");\n    return Math.max(restitutionA, restitutionB);\n};\nclass PhysicsBody {\n    setBodyType(bodyType) {\n        this.bodyType = bodyType;\n        const resolvedMassProperties = resolveBodyMassProperties(bodyType, this.massProperties);\n        this.localCenter = resolvedMassProperties.localCenter;\n        this.mass = resolvedMassProperties.mass;\n        this.invMass = resolvedMassProperties.invMass;\n        this.inertia = resolvedMassProperties.inertia;\n        this.invInertia = resolvedMassProperties.invInertia;\n    }\n    setMassProperties(massProperties) {\n        this.massProperties = cloneMassProperties(massProperties);\n        const resolvedMassProperties = resolveBodyMassProperties(this.bodyType, this.massProperties);\n        this.localCenter = resolvedMassProperties.localCenter;\n        this.mass = resolvedMassProperties.mass;\n        this.invMass = resolvedMassProperties.invMass;\n        this.inertia = resolvedMassProperties.inertia;\n        this.invInertia = resolvedMassProperties.invInertia;\n    }\n    getMassProperties() {\n        return cloneMassProperties(this.massProperties);\n    }\n    constructor(options = {}){\n        var _options_position, _options_linearVelocity, _options_force;\n        _define_property(this, \"position\", void 0);\n        _define_property(this, \"angle\", void 0);\n        _define_property(this, \"linearVelocity\", void 0);\n        _define_property(this, \"angularVelocity\", void 0);\n        _define_property(this, \"force\", void 0);\n        _define_property(this, \"torque\", void 0);\n        _define_property(this, \"localCenter\", void 0);\n        _define_property(this, \"mass\", void 0);\n        _define_property(this, \"invMass\", void 0);\n        _define_property(this, \"inertia\", void 0);\n        _define_property(this, \"invInertia\", void 0);\n        _define_property(this, \"linearDamping\", void 0);\n        _define_property(this, \"angularDamping\", void 0);\n        _define_property(this, \"staticFriction\", void 0);\n        _define_property(this, \"dynamicFriction\", void 0);\n        _define_property(this, \"restitution\", void 0);\n        _define_property(this, \"bodyType\", void 0);\n        _define_property(this, \"filter\", void 0);\n        _define_property(this, \"sleep\", void 0);\n        _define_property(this, \"massProperties\", void 0);\n        var _options_bodyType;\n        const bodyType = (_options_bodyType = options.bodyType) !== null && _options_bodyType !== void 0 ? _options_bodyType : \"dynamic\";\n        var _options_massProperties;\n        const massProperties = cloneMassProperties((_options_massProperties = options.massProperties) !== null && _options_massProperties !== void 0 ? _options_massProperties : DEFAULT_BODY_MASS_PROPERTIES);\n        var _options_position_clone;\n        this.position = (_options_position_clone = (_options_position = options.position) === null || _options_position === void 0 ? void 0 : _options_position.clone()) !== null && _options_position_clone !== void 0 ? _options_position_clone : _index.Vector.zero();\n        var _options_angle;\n        this.angle = (_options_angle = options.angle) !== null && _options_angle !== void 0 ? _options_angle : 0;\n        var _options_linearVelocity_clone;\n        this.linearVelocity = (_options_linearVelocity_clone = (_options_linearVelocity = options.linearVelocity) === null || _options_linearVelocity === void 0 ? void 0 : _options_linearVelocity.clone()) !== null && _options_linearVelocity_clone !== void 0 ? _options_linearVelocity_clone : _index.Vector.zero();\n        var _options_angularVelocity;\n        this.angularVelocity = (_options_angularVelocity = options.angularVelocity) !== null && _options_angularVelocity !== void 0 ? _options_angularVelocity : 0;\n        var _options_force_clone;\n        this.force = (_options_force_clone = (_options_force = options.force) === null || _options_force === void 0 ? void 0 : _options_force.clone()) !== null && _options_force_clone !== void 0 ? _options_force_clone : _index.Vector.zero();\n        var _options_torque;\n        this.torque = (_options_torque = options.torque) !== null && _options_torque !== void 0 ? _options_torque : 0;\n        var _options_linearDamping;\n        this.linearDamping = (_options_linearDamping = options.linearDamping) !== null && _options_linearDamping !== void 0 ? _options_linearDamping : DEFAULT_LINEAR_DAMPING;\n        var _options_angularDamping;\n        this.angularDamping = (_options_angularDamping = options.angularDamping) !== null && _options_angularDamping !== void 0 ? _options_angularDamping : DEFAULT_ANGULAR_DAMPING;\n        var _options_staticFriction;\n        this.staticFriction = (_options_staticFriction = options.staticFriction) !== null && _options_staticFriction !== void 0 ? _options_staticFriction : DEFAULT_STATIC_FRICTION;\n        var _options_dynamicFriction;\n        this.dynamicFriction = (_options_dynamicFriction = options.dynamicFriction) !== null && _options_dynamicFriction !== void 0 ? _options_dynamicFriction : DEFAULT_DYNAMIC_FRICTION;\n        var _options_restitution;\n        this.restitution = (_options_restitution = options.restitution) !== null && _options_restitution !== void 0 ? _options_restitution : DEFAULT_RESTITUTION;\n        this.bodyType = bodyType;\n        this.filter = resolveCollisionFilter(options.filter);\n        this.sleep = resolveSleepState(options.sleep);\n        this.massProperties = massProperties;\n        assertFiniteVector(this.position, \"Position\");\n        assertFiniteNumber(this.angle, \"Angle\");\n        assertFiniteVector(this.linearVelocity, \"Linear velocity\");\n        assertFiniteNumber(this.angularVelocity, \"Angular velocity\");\n        assertFiniteVector(this.force, \"Force\");\n        assertFiniteNumber(this.torque, \"Torque\");\n        assertNonNegativeNumber(this.linearDamping, \"Linear damping\");\n        assertNonNegativeNumber(this.angularDamping, \"Angular damping\");\n        assertNonNegativeNumber(this.staticFriction, \"Static friction\");\n        assertNonNegativeNumber(this.dynamicFriction, \"Dynamic friction\");\n        assertNonNegativeNumber(this.restitution, \"Restitution\");\n        const resolvedMassProperties = resolveBodyMassProperties(this.bodyType, this.massProperties);\n        this.localCenter = resolvedMassProperties.localCenter;\n        this.mass = resolvedMassProperties.mass;\n        this.invMass = resolvedMassProperties.invMass;\n        this.inertia = resolvedMassProperties.inertia;\n        this.invInertia = resolvedMassProperties.invInertia;\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Body/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./PhysicsBody.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/BroadPhase/NaiveBroadPhase.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"NaiveBroadPhase\", {\n    enumerable: true,\n    get: function() {\n        return NaiveBroadPhase;\n    }\n});\nconst _Proxy = require(\"./Proxy.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass NaiveBroadPhase {\n    updateProxies(proxies) {\n        this.proxies = proxies.map((proxy)=>(0, _Proxy.cloneBroadPhaseProxy)(proxy));\n    }\n    getCandidatePairs() {\n        const pairs = [];\n        for(let indexA = 0; indexA < this.proxies.length; indexA++){\n            const proxyA = this.proxies[indexA];\n            for(let indexB = indexA + 1; indexB < this.proxies.length; indexB++){\n                const proxyB = this.proxies[indexB];\n                if (!(0, _Proxy.shouldEmitCandidatePair)(proxyA, proxyB)) {\n                    continue;\n                }\n                if (!(0, _Proxy.aabbsOverlap)(proxyA.aabb, proxyB.aabb)) {\n                    continue;\n                }\n                pairs.push((0, _Proxy.createBroadPhasePair)(proxyA, proxyB));\n            }\n        }\n        return pairs;\n    }\n    constructor(){\n        _define_property(this, \"proxies\", []);\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/BroadPhase/Proxy.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get DEFAULT_BROAD_PHASE_FILTER () {\n        return DEFAULT_BROAD_PHASE_FILTER;\n    },\n    get aabbsOverlap () {\n        return aabbsOverlap;\n    },\n    get cloneAabb () {\n        return cloneAabb;\n    },\n    get cloneBroadPhaseProxy () {\n        return cloneBroadPhaseProxy;\n    },\n    get createBroadPhasePair () {\n        return createBroadPhasePair;\n    },\n    get createBroadPhaseProxy () {\n        return createBroadPhaseProxy;\n    },\n    get expandAabb () {\n        return expandAabb;\n    },\n    get filtersAllowCandidatePair () {\n        return filtersAllowCandidatePair;\n    },\n    get resolveBroadPhaseFilter () {\n        return resolveBroadPhaseFilter;\n    },\n    get shouldEmitCandidatePair () {\n        return shouldEmitCandidatePair;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst _index1 = require(\"../Shapes/index.js\");\nconst DEFAULT_BROAD_PHASE_FILTER = {\n    category: 0xffffffff,\n    mask: 0xffffffff\n};\nconst resolveBroadPhaseFilter = (filter)=>{\n    var _filter_category, _filter_mask;\n    return {\n        category: ((_filter_category = filter === null || filter === void 0 ? void 0 : filter.category) !== null && _filter_category !== void 0 ? _filter_category : DEFAULT_BROAD_PHASE_FILTER.category) >>> 0,\n        mask: ((_filter_mask = filter === null || filter === void 0 ? void 0 : filter.mask) !== null && _filter_mask !== void 0 ? _filter_mask : DEFAULT_BROAD_PHASE_FILTER.mask) >>> 0\n    };\n};\nconst cloneAabb = (aabb)=>{\n    return {\n        min: aabb.min.clone(),\n        max: aabb.max.clone()\n    };\n};\nconst cloneBroadPhaseProxy = (proxy)=>{\n    return {\n        proxyId: proxy.proxyId,\n        bodyId: proxy.bodyId,\n        shapeId: proxy.shapeId,\n        aabb: cloneAabb(proxy.aabb),\n        filter: resolveBroadPhaseFilter(proxy.filter),\n        isSensor: proxy.isSensor\n    };\n};\nconst createBroadPhaseProxy = (options)=>{\n    var _options_isSensor;\n    return {\n        proxyId: options.proxyId,\n        bodyId: options.bodyId,\n        shapeId: options.shapeId,\n        aabb: options.shape.getAabb(options.transform),\n        filter: resolveBroadPhaseFilter(options.filter),\n        isSensor: (_options_isSensor = options.isSensor) !== null && _options_isSensor !== void 0 ? _options_isSensor : false\n    };\n};\nconst aabbsOverlap = (a, b)=>{\n    return !(a.max.x < b.min.x || b.max.x < a.min.x || a.max.y < b.min.y || b.max.y < a.min.y);\n};\nconst filtersAllowCandidatePair = (proxyA, proxyB)=>{\n    return (proxyA.filter.mask & proxyB.filter.category) !== 0 && (proxyB.filter.mask & proxyA.filter.category) !== 0;\n};\nconst shouldEmitCandidatePair = (proxyA, proxyB)=>{\n    if (proxyA.bodyId === proxyB.bodyId) {\n        return false;\n    }\n    return filtersAllowCandidatePair(proxyA, proxyB);\n};\nconst createBroadPhasePair = (proxyA, proxyB)=>{\n    if (proxyA.proxyId <= proxyB.proxyId) {\n        return {\n            proxyA,\n            proxyB\n        };\n    }\n    return {\n        proxyA: proxyB,\n        proxyB: proxyA\n    };\n};\nconst expandAabb = (aabb, margin)=>{\n    return {\n        min: aabb.min.toSubtracted(margin),\n        max: aabb.max.toAdded(margin)\n    };\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/BroadPhase/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./NaiveBroadPhase.js\"), exports);\n_export_star(require(\"./Proxy.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Contacts/ContactManifold.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nconst _index = require(\"../../Lib/Vector/index.js\");",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Contacts/ContactProxy.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"createContactProxy\", {\n    enumerable: true,\n    get: function() {\n        return createContactProxy;\n    }\n});\nconst _index = require(\"../Shapes/index.js\");\nconst cloneTransform = (transform)=>{\n    return {\n        position: transform.position.clone(),\n        angle: transform.angle\n    };\n};\nconst createContactProxy = (options)=>{\n    return {\n        bodyId: options.bodyId,\n        shapeId: options.shapeId,\n        shape: options.shape,\n        transform: cloneTransform(options.transform)\n    };\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Contacts/NarrowPhase.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"generateContactManifold\", {\n    enumerable: true,\n    get: function() {\n        return generateContactManifold;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst _index1 = require(\"../Shapes/index.js\");\nconst CONTACT_EPSILON = 1e-9;\nconst normalizeOrFallback = (vector, fallback = new _index.Vector(1, 0))=>{\n    const magnitude = vector.magnitude();\n    if (magnitude <= CONTACT_EPSILON) {\n        return fallback.clone();\n    }\n    return vector.toMultiplied(1 / magnitude);\n};\nconst getPolygonData = (shape, transform)=>{\n    const vertices = shape.getWorldVertices(transform);\n    const normals = vertices.map((vertex, index)=>{\n        const next = vertices[(index + 1) % vertices.length];\n        const edge = next.toSubtracted(vertex);\n        return normalizeOrFallback(edge.toRightPerpendicular());\n    });\n    return {\n        vertices,\n        normals\n    };\n};\nconst buildManifold = (proxyA, proxyB, normal, points)=>{\n    if (points.length === 0) {\n        return null;\n    }\n    const penetration = points.reduce((maximumPenetration, point)=>Math.max(maximumPenetration, point.penetration), 0);\n    if (penetration <= 0) {\n        return null;\n    }\n    return {\n        bodyIdA: proxyA.bodyId,\n        bodyIdB: proxyB.bodyId,\n        shapeIdA: proxyA.shapeId,\n        shapeIdB: proxyB.shapeId,\n        normal: normalizeOrFallback(normal),\n        penetration,\n        points: points.map((point)=>({\n                id: point.id,\n                position: point.position.clone(),\n                penetration: point.penetration\n            }))\n    };\n};\nconst flipManifold = (manifold, proxyA, proxyB)=>{\n    return {\n        bodyIdA: proxyA.bodyId,\n        bodyIdB: proxyB.bodyId,\n        shapeIdA: proxyA.shapeId,\n        shapeIdB: proxyB.shapeId,\n        normal: manifold.normal.toMultiplied(-1),\n        penetration: manifold.penetration,\n        points: manifold.points.map((point)=>({\n                id: point.id,\n                position: point.position.clone(),\n                penetration: point.penetration\n            }))\n    };\n};\nconst collideCircleCircle = (proxyA, proxyB)=>{\n    const circleA = proxyA.shape;\n    const circleB = proxyB.shape;\n    const centerA = circleA.getWorldCenter(proxyA.transform);\n    const centerB = circleB.getWorldCenter(proxyB.transform);\n    const centerDelta = centerB.toSubtracted(centerA);\n    const radiusSum = circleA.radius + circleB.radius;\n    const distanceSquared = centerDelta.squaredMagnitude();\n    if (distanceSquared > radiusSum * radiusSum) {\n        return null;\n    }\n    let normal = new _index.Vector(1, 0);\n    let contactPoint = centerA.clone();\n    let penetration = radiusSum;\n    if (distanceSquared > CONTACT_EPSILON) {\n        const distance = Math.sqrt(distanceSquared);\n        normal = centerDelta.toMultiplied(1 / distance);\n        penetration = radiusSum - distance;\n        const pointOnA = centerA.toAdded(normal.toMultiplied(circleA.radius));\n        const pointOnB = centerB.toSubtracted(normal.toMultiplied(circleB.radius));\n        contactPoint = pointOnA.toAdded(pointOnB).toMultiplied(0.5);\n    }\n    return buildManifold(proxyA, proxyB, normal, [\n        {\n            id: \"circle-circle\",\n            position: contactPoint,\n            penetration\n        }\n    ]);\n};\nconst collideCirclePolygon = (circleProxy, polygonProxy)=>{\n    const circle = circleProxy.shape;\n    const polygon = polygonProxy.shape;\n    const circleCenter = circle.getWorldCenter(circleProxy.transform);\n    const polygonData = getPolygonData(polygon, polygonProxy.transform);\n    let bestSeparation = Number.NEGATIVE_INFINITY;\n    let bestFaceIndex = 0;\n    for(let index = 0; index < polygonData.vertices.length; index++){\n        const vertex = polygonData.vertices[index];\n        const separation = polygonData.normals[index].dotProduct(circleCenter.toSubtracted(vertex));\n        if (separation > circle.radius) {\n            return null;\n        }\n        if (separation > bestSeparation) {\n            bestSeparation = separation;\n            bestFaceIndex = index;\n        }\n    }\n    const faceNormal = polygonData.normals[bestFaceIndex];\n    const faceStart = polygonData.vertices[bestFaceIndex];\n    const faceEnd = polygonData.vertices[(bestFaceIndex + 1) % polygonData.vertices.length];\n    const edge = faceEnd.toSubtracted(faceStart);\n    const edgeLengthSquared = edge.squaredMagnitude();\n    const projectedDistance = edgeLengthSquared <= CONTACT_EPSILON ? 0 : circleCenter.toSubtracted(faceStart).dotProduct(edge) / edgeLengthSquared;\n    if (bestSeparation < 0) {\n        const contactPoint = circleCenter.toAdded(faceNormal.toMultiplied(-bestSeparation));\n        return buildManifold(circleProxy, polygonProxy, faceNormal, [\n            {\n                id: `face-${bestFaceIndex}`,\n                position: contactPoint,\n                penetration: circle.radius - bestSeparation\n            }\n        ]);\n    }\n    if (projectedDistance <= 0) {\n        const closestVertex = faceStart;\n        const delta = closestVertex.toSubtracted(circleCenter);\n        const distanceSquared = delta.squaredMagnitude();\n        if (distanceSquared > circle.radius * circle.radius) {\n            return null;\n        }\n        const distance = Math.sqrt(distanceSquared);\n        const normal = distance <= CONTACT_EPSILON ? faceNormal.toMultiplied(-1) : delta.toMultiplied(1 / distance);\n        return buildManifold(circleProxy, polygonProxy, normal, [\n            {\n                id: `vertex-${bestFaceIndex}`,\n                position: closestVertex,\n                penetration: circle.radius - distance\n            }\n        ]);\n    }\n    if (projectedDistance >= 1) {\n        const closestVertex = faceEnd;\n        const delta = closestVertex.toSubtracted(circleCenter);\n        const distanceSquared = delta.squaredMagnitude();\n        if (distanceSquared > circle.radius * circle.radius) {\n            return null;\n        }\n        const distance = Math.sqrt(distanceSquared);\n        const normal = distance <= CONTACT_EPSILON ? faceNormal.toMultiplied(-1) : delta.toMultiplied(1 / distance);\n        return buildManifold(circleProxy, polygonProxy, normal, [\n            {\n                id: `vertex-${(bestFaceIndex + 1) % polygonData.vertices.length}`,\n                position: closestVertex,\n                penetration: circle.radius - distance\n            }\n        ]);\n    }\n    const closestPoint = faceStart.toAdded(edge.toMultiplied(projectedDistance));\n    const delta = closestPoint.toSubtracted(circleCenter);\n    const distanceSquared = delta.squaredMagnitude();\n    if (distanceSquared > circle.radius * circle.radius) {\n        return null;\n    }\n    const distance = Math.sqrt(distanceSquared);\n    const normal = distance <= CONTACT_EPSILON ? faceNormal.toMultiplied(-1) : delta.toMultiplied(1 / distance);\n    return buildManifold(circleProxy, polygonProxy, normal, [\n        {\n            id: `face-${bestFaceIndex}`,\n            position: closestPoint,\n            penetration: circle.radius - distance\n        }\n    ]);\n};\nconst projectVerticesOntoAxis = (vertices, axis)=>{\n    let min = Number.POSITIVE_INFINITY;\n    let max = Number.NEGATIVE_INFINITY;\n    for (const vertex of vertices){\n        const projection = vertex.dotProduct(axis);\n        min = Math.min(min, projection);\n        max = Math.max(max, projection);\n    }\n    return {\n        min,\n        max\n    };\n};\nconst findMaxSeparation = (reference, incident)=>{\n    let bestSeparation = Number.NEGATIVE_INFINITY;\n    let bestFaceIndex = 0;\n    for(let index = 0; index < reference.normals.length; index++){\n        const normal = reference.normals[index];\n        const referenceOffset = normal.dotProduct(reference.vertices[index]);\n        const incidentProjection = projectVerticesOntoAxis(incident.vertices, normal).min;\n        const separation = incidentProjection - referenceOffset;\n        if (separation > bestSeparation) {\n            bestSeparation = separation;\n            bestFaceIndex = index;\n        }\n    }\n    return {\n        separation: bestSeparation,\n        faceIndex: bestFaceIndex\n    };\n};\nconst findIncidentEdge = (polygon, referenceNormal)=>{\n    let bestDot = Number.POSITIVE_INFINITY;\n    let bestEdgeIndex = 0;\n    for(let index = 0; index < polygon.normals.length; index++){\n        const dot = referenceNormal.dotProduct(polygon.normals[index]);\n        if (dot < bestDot) {\n            bestDot = dot;\n            bestEdgeIndex = index;\n        }\n    }\n    return {\n        edgeIndex: bestEdgeIndex,\n        vertices: [\n            {\n                id: `v${bestEdgeIndex}`,\n                point: polygon.vertices[bestEdgeIndex].clone()\n            },\n            {\n                id: `v${(bestEdgeIndex + 1) % polygon.vertices.length}`,\n                point: polygon.vertices[(bestEdgeIndex + 1) % polygon.vertices.length].clone()\n            }\n        ]\n    };\n};\nconst clipSegmentToLine = (vertices, normal, offset)=>{\n    const distanceA = normal.dotProduct(vertices[0].point) - offset;\n    const distanceB = normal.dotProduct(vertices[1].point) - offset;\n    const output = [];\n    if (distanceA <= CONTACT_EPSILON) {\n        output.push({\n            id: vertices[0].id,\n            point: vertices[0].point.clone()\n        });\n    }\n    if (distanceB <= CONTACT_EPSILON) {\n        output.push({\n            id: vertices[1].id,\n            point: vertices[1].point.clone()\n        });\n    }\n    if (distanceA * distanceB < -CONTACT_EPSILON) {\n        const interpolation = distanceA / (distanceA - distanceB);\n        const point = vertices[0].point.toAdded(vertices[1].point.toSubtracted(vertices[0].point).toMultiplied(interpolation));\n        output.push({\n            id: distanceA > 0 ? vertices[1].id : vertices[0].id,\n            point\n        });\n    }\n    if (output.length > 2) {\n        return output.slice(0, 2);\n    }\n    return output;\n};\nconst collidePolygonPolygon = (proxyA, proxyB)=>{\n    const polygonA = getPolygonData(proxyA.shape, proxyA.transform);\n    const polygonB = getPolygonData(proxyB.shape, proxyB.transform);\n    const separationA = findMaxSeparation(polygonA, polygonB);\n    if (separationA.separation > 0) {\n        return null;\n    }\n    const separationB = findMaxSeparation(polygonB, polygonA);\n    if (separationB.separation > 0) {\n        return null;\n    }\n    const usePolygonBAsReference = separationB.separation > separationA.separation + 1e-6;\n    const referencePolygon = usePolygonBAsReference ? polygonB : polygonA;\n    const incidentPolygon = usePolygonBAsReference ? polygonA : polygonB;\n    const referenceFaceIndex = usePolygonBAsReference ? separationB.faceIndex : separationA.faceIndex;\n    const referenceNormal = referencePolygon.normals[referenceFaceIndex];\n    const referenceFaceStart = referencePolygon.vertices[referenceFaceIndex];\n    const referenceFaceEnd = referencePolygon.vertices[(referenceFaceIndex + 1) % referencePolygon.vertices.length];\n    const sideNormal = normalizeOrFallback(referenceFaceEnd.toSubtracted(referenceFaceStart));\n    const incidentEdge = findIncidentEdge(incidentPolygon, referenceNormal);\n    let clippedPoints = incidentEdge.vertices.map((vertex)=>({\n            id: vertex.id,\n            point: vertex.point.clone()\n        }));\n    clippedPoints = clipSegmentToLine(clippedPoints, sideNormal.toMultiplied(-1), -sideNormal.dotProduct(referenceFaceStart));\n    if (clippedPoints.length < 2) {\n        return null;\n    }\n    clippedPoints = clipSegmentToLine(clippedPoints, sideNormal, sideNormal.dotProduct(referenceFaceEnd));\n    if (clippedPoints.length < 1) {\n        return null;\n    }\n    const referenceOffset = referenceNormal.dotProduct(referenceFaceStart);\n    const points = clippedPoints.map((point)=>{\n        const separation = referenceNormal.dotProduct(point.point) - referenceOffset;\n        if (separation > CONTACT_EPSILON) {\n            return null;\n        }\n        return {\n            id: `f${referenceFaceIndex}|${point.id}`,\n            position: point.point.toSubtracted(referenceNormal.toMultiplied(separation)),\n            penetration: -separation\n        };\n    }).filter((point)=>point !== null).sort((pointA, pointB)=>{\n        return pointA.position.dotProduct(sideNormal) - pointB.position.dotProduct(sideNormal);\n    });\n    const normal = usePolygonBAsReference ? referenceNormal.toMultiplied(-1) : referenceNormal;\n    return buildManifold(proxyA, proxyB, normal, points);\n};\nconst generateContactManifold = (proxyA, proxyB)=>{\n    if (proxyA.shape instanceof _index1.CircleShape && proxyB.shape instanceof _index1.CircleShape) {\n        return collideCircleCircle(proxyA, proxyB);\n    }\n    if (proxyA.shape instanceof _index1.CircleShape && proxyB.shape instanceof _index1.ConvexPolygonShape) {\n        return collideCirclePolygon(proxyA, proxyB);\n    }\n    if (proxyA.shape instanceof _index1.ConvexPolygonShape && proxyB.shape instanceof _index1.CircleShape) {\n        const manifold = collideCirclePolygon(proxyB, proxyA);\n        if (manifold === null) {\n            return null;\n        }\n        return flipManifold(manifold, proxyA, proxyB);\n    }\n    if (proxyA.shape instanceof _index1.ConvexPolygonShape && proxyB.shape instanceof _index1.ConvexPolygonShape) {\n        return collidePolygonPolygon(proxyA, proxyB);\n    }\n    return null;\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Contacts/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./ContactManifold.js\"), exports);\n_export_star(require(\"./ContactProxy.js\"), exports);\n_export_star(require(\"./NarrowPhase.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Decomposition/PolygonDecomposition.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get MAX_CONVEX_PART_VERTICES () {\n        return MAX_CONVEX_PART_VERTICES;\n    },\n    get decomposePolygonIntoConvexParts () {\n        return decomposePolygonIntoConvexParts;\n    },\n    get isConvexPolygon () {\n        return isConvexPolygon;\n    },\n    get normalizeSimplePolygonVertices () {\n        return normalizeSimplePolygonVertices;\n    },\n    get triangulateSimplePolygon () {\n        return triangulateSimplePolygon;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst _index1 = require(\"../Shapes/index.js\");\nconst _ShapeMath = require(\"../Shapes/ShapeMath.js\");\nconst MAX_CONVEX_PART_VERTICES = 8;\nconst arePointsEqual = (a, b)=>{\n    return Math.abs(a.x - b.x) <= _ShapeMath.SHAPE_EPSILON && Math.abs(a.y - b.y) <= _ShapeMath.SHAPE_EPSILON;\n};\nconst computeSignedArea = (vertices)=>{\n    let doubledArea = 0;\n    for(let index = 0; index < vertices.length; index++){\n        const current = vertices[index];\n        const next = vertices[(index + 1) % vertices.length];\n        doubledArea += current.crossProduct(next);\n    }\n    return doubledArea * 0.5;\n};\nconst computeArea = (vertices)=>{\n    return Math.abs(computeSignedArea(vertices));\n};\nconst assertFiniteVertices = (vertices)=>{\n    for (const vertex of vertices){\n        if (!Number.isFinite(vertex.x) || !Number.isFinite(vertex.y)) {\n            throw new Error(\"Polygon vertices must contain only finite coordinates.\");\n        }\n    }\n};\nconst stripRepeatedClosingVertex = (vertices)=>{\n    const clonedVertices = vertices.map((vertex)=>vertex.clone());\n    if (clonedVertices.length < 2) {\n        return clonedVertices;\n    }\n    const firstVertex = clonedVertices[0];\n    const lastVertex = clonedVertices[clonedVertices.length - 1];\n    if (arePointsEqual(firstVertex, lastVertex)) {\n        clonedVertices.pop();\n    }\n    return clonedVertices;\n};\nconst assertNoConsecutiveDuplicates = (vertices)=>{\n    for(let index = 0; index < vertices.length; index++){\n        const current = vertices[index];\n        const next = vertices[(index + 1) % vertices.length];\n        if (arePointsEqual(current, next)) {\n            throw new Error(\"Polygon vertices must not contain consecutive duplicates.\");\n        }\n    }\n};\nconst assertNoRepeatedVertices = (vertices)=>{\n    for(let index = 0; index < vertices.length; index++){\n        for(let compareIndex = index + 1; compareIndex < vertices.length; compareIndex++){\n            if (arePointsEqual(vertices[index], vertices[compareIndex])) {\n                throw new Error(\"Polygon vertices must not repeat.\");\n            }\n        }\n    }\n};\nconst removeCollinearVertices = (vertices)=>{\n    let normalizedVertices = vertices.map((vertex)=>vertex.clone());\n    let changed = true;\n    while(changed){\n        if (normalizedVertices.length < 3) {\n            return normalizedVertices;\n        }\n        changed = false;\n        const nextVertices = [];\n        for(let index = 0; index < normalizedVertices.length; index++){\n            const previous = normalizedVertices[(index - 1 + normalizedVertices.length) % normalizedVertices.length];\n            const current = normalizedVertices[index];\n            const next = normalizedVertices[(index + 1) % normalizedVertices.length];\n            const previousEdge = current.toSubtracted(previous);\n            const nextEdge = next.toSubtracted(current);\n            const cross = previousEdge.crossProduct(nextEdge);\n            const dot = previousEdge.dotProduct(nextEdge);\n            if (Math.abs(cross) <= _ShapeMath.SHAPE_EPSILON && dot >= 0) {\n                changed = true;\n                continue;\n            }\n            nextVertices.push(current);\n        }\n        normalizedVertices = nextVertices;\n    }\n    return normalizedVertices;\n};\nconst orientation = (a, b, c)=>{\n    return b.toSubtracted(a).crossProduct(c.toSubtracted(a));\n};\nconst isPointOnSegment = (point, start, end)=>{\n    if (Math.abs(orientation(start, end, point)) > _ShapeMath.SHAPE_EPSILON) {\n        return false;\n    }\n    return point.x >= Math.min(start.x, end.x) - _ShapeMath.SHAPE_EPSILON && point.x <= Math.max(start.x, end.x) + _ShapeMath.SHAPE_EPSILON && point.y >= Math.min(start.y, end.y) - _ShapeMath.SHAPE_EPSILON && point.y <= Math.max(start.y, end.y) + _ShapeMath.SHAPE_EPSILON;\n};\nconst segmentsIntersect = (startA, endA, startB, endB)=>{\n    const orientationAStart = orientation(startA, endA, startB);\n    const orientationAEnd = orientation(startA, endA, endB);\n    const orientationBStart = orientation(startB, endB, startA);\n    const orientationBEnd = orientation(startB, endB, endA);\n    const hasProperIntersection = (orientationAStart > _ShapeMath.SHAPE_EPSILON && orientationAEnd < -_ShapeMath.SHAPE_EPSILON || orientationAStart < -_ShapeMath.SHAPE_EPSILON && orientationAEnd > _ShapeMath.SHAPE_EPSILON) && (orientationBStart > _ShapeMath.SHAPE_EPSILON && orientationBEnd < -_ShapeMath.SHAPE_EPSILON || orientationBStart < -_ShapeMath.SHAPE_EPSILON && orientationBEnd > _ShapeMath.SHAPE_EPSILON);\n    if (hasProperIntersection) {\n        return true;\n    }\n    return isPointOnSegment(startB, startA, endA) || isPointOnSegment(endB, startA, endA) || isPointOnSegment(startA, startB, endB) || isPointOnSegment(endA, startB, endB);\n};\nconst assertSimplePolygon = (vertices)=>{\n    for(let firstEdgeIndex = 0; firstEdgeIndex < vertices.length; firstEdgeIndex++){\n        const firstStart = vertices[firstEdgeIndex];\n        const firstEnd = vertices[(firstEdgeIndex + 1) % vertices.length];\n        for(let secondEdgeIndex = firstEdgeIndex + 1; secondEdgeIndex < vertices.length; secondEdgeIndex++){\n            const areSameEdge = firstEdgeIndex === secondEdgeIndex;\n            const shareForwardVertex = (firstEdgeIndex + 1) % vertices.length === secondEdgeIndex;\n            const shareBackwardVertex = firstEdgeIndex === (secondEdgeIndex + 1) % vertices.length;\n            if (areSameEdge || shareForwardVertex || shareBackwardVertex) {\n                continue;\n            }\n            const secondStart = vertices[secondEdgeIndex];\n            const secondEnd = vertices[(secondEdgeIndex + 1) % vertices.length];\n            if (segmentsIntersect(firstStart, firstEnd, secondStart, secondEnd)) {\n                throw new Error(\"Polygon must be simple and non-self-intersecting.\");\n            }\n        }\n    }\n};\nconst normalizeSimplePolygonVertices = (vertices)=>{\n    assertFiniteVertices(vertices);\n    const withoutRepeatedClosingVertex = stripRepeatedClosingVertex(vertices);\n    if (withoutRepeatedClosingVertex.length < 3) {\n        throw new Error(\"Polygons require at least three distinct vertices.\");\n    }\n    assertNoConsecutiveDuplicates(withoutRepeatedClosingVertex);\n    assertNoRepeatedVertices(withoutRepeatedClosingVertex);\n    const normalizedVertices = removeCollinearVertices(withoutRepeatedClosingVertex);\n    if (normalizedVertices.length < 3) {\n        throw new Error(\"Polygon must span a non-zero area.\");\n    }\n    const signedArea = computeSignedArea(normalizedVertices);\n    if (Math.abs(signedArea) <= _ShapeMath.SHAPE_EPSILON) {\n        throw new Error(\"Polygon must span a non-zero area.\");\n    }\n    assertSimplePolygon(normalizedVertices);\n    if (signedArea < 0) {\n        normalizedVertices.reverse();\n    }\n    return normalizedVertices;\n};\nconst isConvexNormalizedPolygon = (vertices)=>{\n    let hasPositiveCross = false;\n    for(let index = 0; index < vertices.length; index++){\n        const previous = vertices[index];\n        const current = vertices[(index + 1) % vertices.length];\n        const next = vertices[(index + 2) % vertices.length];\n        const cross = current.toSubtracted(previous).crossProduct(next.toSubtracted(current));\n        if (cross < -_ShapeMath.SHAPE_EPSILON) {\n            return false;\n        }\n        if (cross > _ShapeMath.SHAPE_EPSILON) {\n            hasPositiveCross = true;\n        }\n    }\n    return hasPositiveCross;\n};\nconst isConvexPolygon = (vertices)=>{\n    return isConvexNormalizedPolygon(normalizeSimplePolygonVertices(vertices));\n};\nconst isPointInTriangle = (point, a, b, c)=>{\n    const ab = orientation(a, b, point);\n    const bc = orientation(b, c, point);\n    const ca = orientation(c, a, point);\n    return ab >= -_ShapeMath.SHAPE_EPSILON && bc >= -_ShapeMath.SHAPE_EPSILON && ca >= -_ShapeMath.SHAPE_EPSILON;\n};\nconst triangulateNormalizedSimplePolygon = (vertices)=>{\n    if (vertices.length === 3) {\n        return [\n            vertices.map((vertex)=>vertex.clone())\n        ];\n    }\n    const remainingVertices = vertices.map((vertex)=>vertex.clone());\n    const triangles = [];\n    while(remainingVertices.length > 3){\n        let earFound = false;\n        for(let index = 0; index < remainingVertices.length; index++){\n            const previous = remainingVertices[(index - 1 + remainingVertices.length) % remainingVertices.length];\n            const current = remainingVertices[index];\n            const next = remainingVertices[(index + 1) % remainingVertices.length];\n            const earCross = orientation(previous, current, next);\n            if (earCross <= _ShapeMath.SHAPE_EPSILON) {\n                continue;\n            }\n            const triangle = [\n                previous,\n                current,\n                next\n            ];\n            if (computeArea(triangle) <= _ShapeMath.SHAPE_EPSILON) {\n                throw new Error(\"Polygon triangulation produced a degenerate triangle.\");\n            }\n            let containsOtherVertex = false;\n            for(let vertexIndex = 0; vertexIndex < remainingVertices.length; vertexIndex++){\n                if (vertexIndex === index || vertexIndex === (index - 1 + remainingVertices.length) % remainingVertices.length || vertexIndex === (index + 1) % remainingVertices.length) {\n                    continue;\n                }\n                if (isPointInTriangle(remainingVertices[vertexIndex], previous, current, next)) {\n                    containsOtherVertex = true;\n                    break;\n                }\n            }\n            if (containsOtherVertex) {\n                continue;\n            }\n            triangles.push(triangle.map((vertex)=>vertex.clone()));\n            remainingVertices.splice(index, 1);\n            earFound = true;\n            break;\n        }\n        if (!earFound) {\n            throw new Error(\"Failed to triangulate polygon deterministically.\");\n        }\n    }\n    if (computeArea(remainingVertices) <= _ShapeMath.SHAPE_EPSILON) {\n        throw new Error(\"Polygon triangulation produced a degenerate triangle.\");\n    }\n    triangles.push(remainingVertices.map((vertex)=>vertex.clone()));\n    return triangles;\n};\nconst triangulateSimplePolygon = (vertices)=>{\n    return triangulateNormalizedSimplePolygon(normalizeSimplePolygonVertices(vertices)).map((triangleVertices)=>new _index1.ConvexPolygonShape(triangleVertices));\n};\nconst collectUniqueVertices = (firstPolygon, secondPolygon)=>{\n    const uniqueVertices = [];\n    for (const vertex of [\n        ...firstPolygon,\n        ...secondPolygon\n    ]){\n        const alreadyPresent = uniqueVertices.some((existingVertex)=>arePointsEqual(existingVertex, vertex));\n        if (!alreadyPresent) {\n            uniqueVertices.push(vertex.clone());\n        }\n    }\n    return uniqueVertices;\n};\nconst countSharedVertices = (firstPolygon, secondPolygon)=>{\n    let sharedVertexCount = 0;\n    for (const firstVertex of firstPolygon){\n        if (secondPolygon.some((secondVertex)=>arePointsEqual(firstVertex, secondVertex))) {\n            sharedVertexCount += 1;\n        }\n    }\n    return sharedVertexCount;\n};\nconst computeConvexHull = (vertices)=>{\n    const sortedVertices = vertices.map((vertex)=>vertex.clone()).sort((left, right)=>{\n        if (left.x !== right.x) {\n            return left.x - right.x;\n        }\n        return left.y - right.y;\n    });\n    const uniqueSortedVertices = [];\n    for (const vertex of sortedVertices){\n        if (uniqueSortedVertices.length > 0 && arePointsEqual(uniqueSortedVertices[uniqueSortedVertices.length - 1], vertex)) {\n            continue;\n        }\n        uniqueSortedVertices.push(vertex);\n    }\n    if (uniqueSortedVertices.length <= 1) {\n        return uniqueSortedVertices;\n    }\n    const lowerHull = [];\n    for (const vertex of uniqueSortedVertices){\n        while(lowerHull.length >= 2){\n            const cross = orientation(lowerHull[lowerHull.length - 2], lowerHull[lowerHull.length - 1], vertex);\n            if (cross > _ShapeMath.SHAPE_EPSILON) {\n                break;\n            }\n            lowerHull.pop();\n        }\n        lowerHull.push(vertex);\n    }\n    const upperHull = [];\n    for(let index = uniqueSortedVertices.length - 1; index >= 0; index--){\n        const vertex = uniqueSortedVertices[index];\n        while(upperHull.length >= 2){\n            const cross = orientation(upperHull[upperHull.length - 2], upperHull[upperHull.length - 1], vertex);\n            if (cross > _ShapeMath.SHAPE_EPSILON) {\n                break;\n            }\n            upperHull.pop();\n        }\n        upperHull.push(vertex);\n    }\n    lowerHull.pop();\n    upperHull.pop();\n    return [\n        ...lowerHull,\n        ...upperHull\n    ];\n};\nconst tryMergeConvexPolygons = (firstPolygon, secondPolygon)=>{\n    if (countSharedVertices(firstPolygon, secondPolygon) !== 2) {\n        return null;\n    }\n    const uniqueVertices = collectUniqueVertices(firstPolygon, secondPolygon);\n    if (uniqueVertices.length > MAX_CONVEX_PART_VERTICES) {\n        return null;\n    }\n    const hull = computeConvexHull(uniqueVertices);\n    if (hull.length < 3 || hull.length !== uniqueVertices.length) {\n        return null;\n    }\n    const mergedArea = computeArea(hull);\n    const sourceArea = computeArea(firstPolygon) + computeArea(secondPolygon);\n    const areaTolerance = Math.max(1, sourceArea) * _ShapeMath.SHAPE_EPSILON * 10;\n    if (Math.abs(mergedArea - sourceArea) > areaTolerance) {\n        return null;\n    }\n    return hull;\n};\nconst greedyMergeConvexPolygons = (polygons)=>{\n    const remainingPolygons = polygons.map((polygon)=>polygon.map((vertex)=>vertex.clone()));\n    let merged = true;\n    while(merged){\n        merged = false;\n        for(let firstIndex = 0; firstIndex < remainingPolygons.length; firstIndex++){\n            for(let secondIndex = firstIndex + 1; secondIndex < remainingPolygons.length; secondIndex++){\n                const mergedPolygon = tryMergeConvexPolygons(remainingPolygons[firstIndex], remainingPolygons[secondIndex]);\n                if (mergedPolygon === null) {\n                    continue;\n                }\n                remainingPolygons.splice(secondIndex, 1);\n                remainingPolygons.splice(firstIndex, 1, mergedPolygon);\n                merged = true;\n                break;\n            }\n            if (merged) {\n                break;\n            }\n        }\n    }\n    return remainingPolygons;\n};\nconst decomposePolygonIntoConvexParts = (vertices)=>{\n    const normalizedVertices = normalizeSimplePolygonVertices(vertices);\n    if (isConvexNormalizedPolygon(normalizedVertices)) {\n        return [\n            new _index1.ConvexPolygonShape(normalizedVertices)\n        ];\n    }\n    return greedyMergeConvexPolygons(triangulateNormalizedSimplePolygon(normalizedVertices)).map((polygonVertices)=>new _index1.ConvexPolygonShape(polygonVertices));\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Decomposition/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./PolygonDecomposition.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Gameplay/GameplayCollision.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get createGameplayCollisionParticipantFromHitbox () {\n        return createGameplayCollisionParticipantFromHitbox;\n    },\n    get createGameplayCollisionParticipantsFromGameObject () {\n        return createGameplayCollisionParticipantsFromGameObject;\n    },\n    get createPhysicsShapeFromHitbox () {\n        return createPhysicsShapeFromHitbox;\n    },\n    get stepGameplayCollisions () {\n        return stepGameplayCollisions;\n    }\n});\nconst _index = require(\"../../GameObject/Hitboxes/index.js\");\nconst _index1 = require(\"../BroadPhase/index.js\");\nconst _index2 = require(\"../Contacts/index.js\");\nconst _index3 = require(\"../Solver/index.js\");\nconst _index4 = require(\"../Shapes/index.js\");\nconst _index5 = require(\"../../Lib/Vector/index.js\");\nconst resolveFilter = (body, filter)=>{\n    var _filter_category, _filter_mask;\n    return {\n        category: ((_filter_category = filter === null || filter === void 0 ? void 0 : filter.category) !== null && _filter_category !== void 0 ? _filter_category : body.filter.category) >>> 0,\n        mask: ((_filter_mask = filter === null || filter === void 0 ? void 0 : filter.mask) !== null && _filter_mask !== void 0 ? _filter_mask : body.filter.mask) >>> 0\n    };\n};\nconst computeHitboxLocalRotationOffset = (hitbox)=>{\n    // body.position is the renderer's rotation pivot (bounding-box center of all\n    // hitboxes on the game object). Local shape vertices must be expressed relative\n    // to that same pivot so that physics rotation matches visual rotation.\n    return hitbox.gameObject.getRotationCenter().toSubtracted(hitbox.gameObject.getPosition());\n};\nconst createPhysicsShapeFromHitbox = (hitbox)=>{\n    // Shift all local coordinates so they are relative to the rotation pivot,\n    // which is where body.position will be placed by ScenePhysicsWorld.\n    const localRotationOffset = computeHitboxLocalRotationOffset(hitbox);\n    if (hitbox instanceof _index.CircleHitbox) {\n        return new _index4.CircleShape(hitbox.offset.toSubtracted(localRotationOffset), hitbox.radius);\n    }\n    if (hitbox instanceof _index.SquareHitbox) {\n        const localCenter = hitbox.offset.toAdded(hitbox.size.toMultiplied(0.5)).toSubtracted(localRotationOffset);\n        return _index4.ConvexPolygonShape.fromRectangle(hitbox.size.clone(), localCenter);\n    }\n    throw new Error(\"Unsupported hitbox type.\");\n};\nconst createGameplayCollisionParticipantFromHitbox = (hitbox, options)=>{\n    var _options_bodyId;\n    return {\n        proxyId: options.proxyId,\n        bodyId: (_options_bodyId = options.bodyId) !== null && _options_bodyId !== void 0 ? _options_bodyId : hitbox.gameObject.id,\n        shapeId: options.shapeId,\n        gameObject: hitbox.gameObject,\n        body: options.body,\n        shape: createPhysicsShapeFromHitbox(hitbox),\n        solid: hitbox.solid,\n        filter: resolveFilter(options.body, options.filter)\n    };\n};\nconst createGameplayCollisionParticipantsFromGameObject = (gameObject, body, options = {})=>{\n    var _options_bodyId;\n    const bodyId = (_options_bodyId = options.bodyId) !== null && _options_bodyId !== void 0 ? _options_bodyId : gameObject.id;\n    var _options_proxyIdPrefix;\n    const proxyIdPrefix = (_options_proxyIdPrefix = options.proxyIdPrefix) !== null && _options_proxyIdPrefix !== void 0 ? _options_proxyIdPrefix : `${bodyId}:hitbox`;\n    var _options_shapeIdPrefix;\n    const shapeIdPrefix = (_options_shapeIdPrefix = options.shapeIdPrefix) !== null && _options_shapeIdPrefix !== void 0 ? _options_shapeIdPrefix : \"hitbox\";\n    return gameObject.getHitboxes().map((hitbox, index)=>createGameplayCollisionParticipantFromHitbox(hitbox, {\n            body,\n            bodyId,\n            proxyId: `${proxyIdPrefix}-${index}`,\n            shapeId: `${shapeIdPrefix}-${index}`,\n            filter: options.filter\n        }));\n};\nconst collectBodies = (participants)=>{\n    const bodies = new Map();\n    for (const participant of participants){\n        if (!bodies.has(participant.bodyId)) {\n            bodies.set(participant.bodyId, participant.body);\n        }\n    }\n    return bodies;\n};\nconst createPairKey = (gameObjectA, gameObjectB)=>{\n    return gameObjectA.id <= gameObjectB.id ? `${gameObjectA.id}|${gameObjectB.id}` : `${gameObjectB.id}|${gameObjectA.id}`;\n};\nconst createPenetrationVector = (manifold)=>{\n    return manifold.normal.toMultiplied(manifold.penetration);\n};\nconst stepGameplayCollisions = (state, participants, dt, options)=>{\n    const broadPhase = new _index1.NaiveBroadPhase();\n    const participantByProxyId = new Map();\n    const proxies = participants.map((participant)=>{\n        participantByProxyId.set(participant.proxyId, participant);\n        return (0, _index1.createBroadPhaseProxy)({\n            proxyId: participant.proxyId,\n            bodyId: participant.bodyId,\n            shapeId: participant.shapeId,\n            shape: participant.shape,\n            transform: {\n                position: participant.body.position,\n                angle: participant.body.angle\n            },\n            filter: participant.filter,\n            isSensor: !participant.solid\n        });\n    });\n    broadPhase.updateProxies(proxies);\n    const candidatePairs = broadPhase.getCandidatePairs();\n    const overlapManifolds = [];\n    const sensorManifolds = [];\n    const solidManifolds = [];\n    const allowedPairs = new Map();\n    const notifiedPairs = new Set();\n    const suppressedPairs = new Set();\n    for (const pair of candidatePairs){\n        const participantA = participantByProxyId.get(pair.proxyA.proxyId);\n        const participantB = participantByProxyId.get(pair.proxyB.proxyId);\n        if (!participantA || !participantB) {\n            continue;\n        }\n        const manifold = (0, _index2.generateContactManifold)((0, _index2.createContactProxy)({\n            bodyId: participantA.bodyId,\n            shapeId: participantA.shapeId,\n            shape: participantA.shape,\n            transform: {\n                position: participantA.body.position,\n                angle: participantA.body.angle\n            }\n        }), (0, _index2.createContactProxy)({\n            bodyId: participantB.bodyId,\n            shapeId: participantB.shapeId,\n            shape: participantB.shape,\n            transform: {\n                position: participantB.body.position,\n                angle: participantB.body.angle\n            }\n        }));\n        if (!manifold) {\n            continue;\n        }\n        overlapManifolds.push(manifold);\n        const pairKey = createPairKey(participantA.gameObject, participantB.gameObject);\n        let isAllowed = allowedPairs.get(pairKey);\n        if (isAllowed === undefined) {\n            isAllowed = participantA.gameObject.beforeColision(participantB.gameObject) && participantB.gameObject.beforeColision(participantA.gameObject);\n            allowedPairs.set(pairKey, isAllowed);\n        }\n        if (!isAllowed) {\n            suppressedPairs.add(pairKey);\n            continue;\n        }\n        if (!notifiedPairs.has(pairKey)) {\n            notifiedPairs.add(pairKey);\n            const penetration = createPenetrationVector(manifold);\n            participantA.gameObject.onColision(participantB.gameObject, penetration);\n            participantB.gameObject.onColision(participantA.gameObject, penetration.toMultiplied(-1));\n        }\n        if (participantA.solid && participantB.solid) {\n            solidManifolds.push(manifold);\n            continue;\n        }\n        sensorManifolds.push(manifold);\n    }\n    (0, _index3.stepPersistentContacts)(state, collectBodies(participants), solidManifolds, dt, options);\n    return {\n        candidatePairs,\n        overlapManifolds,\n        sensorManifolds,\n        solidManifolds,\n        notifiedPairIds: Array.from(notifiedPairs),\n        suppressedPairIds: Array.from(suppressedPairs)\n    };\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Gameplay/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./GameplayCollision.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Integration/BodyIntegration.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get clearBodyAccumulators () {\n        return clearBodyAccumulators;\n    },\n    get integrateBody () {\n        return integrateBody;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst assertFiniteNumber = (value, label)=>{\n    if (!Number.isFinite(value)) {\n        throw new Error(`${label} must be a finite number.`);\n    }\n};\nconst assertFiniteVector = (value, label)=>{\n    assertFiniteNumber(value.x, `${label}.x`);\n    assertFiniteNumber(value.y, `${label}.y`);\n};\nconst assertPositiveTimeStep = (dt)=>{\n    assertFiniteNumber(dt, \"Time step\");\n    if (dt <= 0) {\n        throw new Error(\"Time step must be positive.\");\n    }\n};\nconst BODY_WAKE_EPSILON = 1e-8;\nconst clearBodyAccumulators = (body)=>{\n    body.force = _index.Vector.zero();\n    body.torque = 0;\n};\nconst applyLinearDamping = (body, dt)=>{\n    if (body.linearDamping <= 0) {\n        return;\n    }\n    // Box2D-style Pade approximation for v(t + h) = v(t) * exp(-c h).\n    body.linearVelocity.multiply(1 / (1 + body.linearDamping * dt));\n};\nconst applyAngularDamping = (body, dt)=>{\n    if (body.angularDamping <= 0) {\n        return;\n    }\n    body.angularVelocity *= 1 / (1 + body.angularDamping * dt);\n};\nconst integrateDynamicBody = (body, gravity, dt)=>{\n    const linearAcceleration = gravity.toAdded(body.force.toMultiplied(body.invMass));\n    const angularAcceleration = body.torque * body.invInertia;\n    body.linearVelocity.add(linearAcceleration.toMultiplied(dt));\n    body.angularVelocity += angularAcceleration * dt;\n    applyLinearDamping(body, dt);\n    applyAngularDamping(body, dt);\n    body.position.add(body.linearVelocity.toMultiplied(dt));\n    body.angle += body.angularVelocity * dt;\n};\nconst integrateKinematicBody = (body, dt)=>{\n    body.position.add(body.linearVelocity.toMultiplied(dt));\n    body.angle += body.angularVelocity * dt;\n};\nconst integrateStaticBody = (body)=>{\n    body.linearVelocity = _index.Vector.zero();\n    body.angularVelocity = 0;\n};\nconst hasExternalWakeRequest = (body)=>{\n    return body.force.squaredMagnitude() > BODY_WAKE_EPSILON * BODY_WAKE_EPSILON || Math.abs(body.torque) > BODY_WAKE_EPSILON || body.linearVelocity.squaredMagnitude() > BODY_WAKE_EPSILON * BODY_WAKE_EPSILON || Math.abs(body.angularVelocity) > BODY_WAKE_EPSILON;\n};\nconst integrateBody = (body, gravity, dt)=>{\n    assertFiniteVector(gravity, \"Gravity\");\n    assertPositiveTimeStep(dt);\n    if (body.bodyType === \"dynamic\" && body.sleep.isSleeping) {\n        if (hasExternalWakeRequest(body)) {\n            body.sleep.isSleeping = false;\n            body.sleep.sleepTime = 0;\n        } else {\n            clearBodyAccumulators(body);\n            return;\n        }\n    }\n    switch(body.bodyType){\n        case \"dynamic\":\n            integrateDynamicBody(body, gravity, dt);\n            break;\n        case \"kinematic\":\n            integrateKinematicBody(body, dt);\n            break;\n        case \"static\":\n            integrateStaticBody(body);\n            break;\n        default:\n            throw new Error(`Unsupported body type: ${String(body.bodyType)}`);\n    }\n    clearBodyAccumulators(body);\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Integration/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./BodyIntegration.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/MassProperties/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get computeCircleMassProperties () {\n        return computeCircleMassProperties;\n    },\n    get computeCompoundMassProperties () {\n        return computeCompoundMassProperties;\n    },\n    get computePolygonMassProperties () {\n        return computePolygonMassProperties;\n    },\n    get toStaticMassProperties () {\n        return toStaticMassProperties;\n    }\n});\nconst _index = require(\"../Shapes/index.js\");\nconst _index1 = require(\"../../Lib/Vector/index.js\");\nconst resolveInverse = (value)=>{\n    if (!Number.isFinite(value) || value <= 0) {\n        return 0;\n    }\n    return 1 / value;\n};\nconst createMassProperties = (area, mass, centroid, inertia)=>{\n    return {\n        area,\n        mass,\n        invMass: resolveInverse(mass),\n        centroid: centroid.clone(),\n        inertia,\n        invInertia: resolveInverse(inertia)\n    };\n};\nconst computeCircleMassProperties = (shape, density)=>{\n    if (!Number.isFinite(density) || density <= 0) {\n        throw new Error(\"Density must be a finite positive number.\");\n    }\n    const area = Math.PI * shape.radius * shape.radius;\n    const mass = area * density;\n    const centroid = shape.getLocalCenter();\n    const inertia = mass * 0.5 * shape.radius * shape.radius;\n    return createMassProperties(area, mass, centroid, inertia);\n};\nconst computePolygonAreaAndCentroid = (vertices)=>{\n    let doubledArea = 0;\n    let centroidFactorX = 0;\n    let centroidFactorY = 0;\n    for(let index = 0; index < vertices.length; index++){\n        const current = vertices[index];\n        const next = vertices[(index + 1) % vertices.length];\n        const cross = current.crossProduct(next);\n        doubledArea += cross;\n        centroidFactorX += (current.x + next.x) * cross;\n        centroidFactorY += (current.y + next.y) * cross;\n    }\n    const area = doubledArea * 0.5;\n    if (Math.abs(area) <= 1e-12) {\n        throw new Error(\"Polygon area must be non-zero.\");\n    }\n    return {\n        area,\n        centroid: new _index1.Vector(centroidFactorX / (6 * area), centroidFactorY / (6 * area))\n    };\n};\nconst computePolygonSecondMomentAboutOrigin = (vertices, density)=>{\n    let integral = 0;\n    for(let index = 0; index < vertices.length; index++){\n        const current = vertices[index];\n        const next = vertices[(index + 1) % vertices.length];\n        const cross = current.crossProduct(next);\n        const term = current.dotProduct(current) + current.dotProduct(next) + next.dotProduct(next);\n        integral += cross * term;\n    }\n    return density * integral / 12;\n};\nconst computePolygonMassProperties = (shape, density)=>{\n    if (!Number.isFinite(density) || density <= 0) {\n        throw new Error(\"Density must be a finite positive number.\");\n    }\n    const vertices = shape.getLocalVertices();\n    const { area, centroid } = computePolygonAreaAndCentroid(vertices);\n    const resolvedArea = Math.abs(area);\n    const mass = resolvedArea * density;\n    const inertiaAboutOrigin = computePolygonSecondMomentAboutOrigin(vertices, density);\n    const inertiaAboutCentroid = inertiaAboutOrigin - mass * centroid.squaredMagnitude();\n    return createMassProperties(resolvedArea, mass, centroid, Math.max(inertiaAboutCentroid, 0));\n};\nconst computeCompoundMassProperties = (contributions)=>{\n    if (contributions.length === 0) {\n        throw new Error(\"Compound mass properties require at least one shape contribution.\");\n    }\n    let totalArea = 0;\n    let totalMass = 0;\n    let centroidAccumulator = _index1.Vector.zero();\n    for (const contribution of contributions){\n        totalArea += contribution.area;\n        totalMass += contribution.mass;\n        centroidAccumulator.add(contribution.centroid.toMultiplied(contribution.mass));\n    }\n    if (!Number.isFinite(totalMass) || totalMass <= 0) {\n        throw new Error(\"Compound mass properties require a positive total mass.\");\n    }\n    const centroid = centroidAccumulator.toMultiplied(1 / totalMass);\n    // Shift each shape's centroid-space inertia into the compound centroid frame.\n    const totalInertia = contributions.reduce((sum, contribution)=>{\n        const centroidOffsetSq = contribution.centroid.toSubtracted(centroid).squaredMagnitude();\n        return sum + contribution.inertia + contribution.mass * centroidOffsetSq;\n    }, 0);\n    return createMassProperties(totalArea, totalMass, centroid, totalInertia);\n};\nconst toStaticMassProperties = (massProperties)=>{\n    return {\n        area: massProperties.area,\n        mass: massProperties.mass,\n        invMass: 0,\n        centroid: massProperties.centroid.clone(),\n        inertia: massProperties.inertia,\n        invInertia: 0\n    };\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/ScenePhysicsWorld.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get DEFAULT_SCENE_TICK_RATE () {\n        return DEFAULT_SCENE_TICK_RATE;\n    },\n    get ScenePhysicsWorld () {\n        return ScenePhysicsWorld;\n    },\n    get resolveSceneTimeStep () {\n        return resolveSceneTimeStep;\n    }\n});\nconst _index = require(\"../Lib/Vector/index.js\");\nconst _index1 = require(\"./Body/index.js\");\nconst _index2 = require(\"./MassProperties/index.js\");\nconst _index3 = require(\"./Gameplay/index.js\");\nconst _index4 = require(\"./Integration/index.js\");\nconst _index5 = require(\"./Solver/index.js\");\nconst _index6 = require(\"./Shapes/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nconst DEFAULT_SCENE_TICK_RATE = 60;\nconst DEFAULT_DYNAMIC_MASS = 1;\nconst DEFAULT_INERTIA_SCALE = 1;\nconst WAKE_EPSILON = 1e-8;\nconst EMPTY_GAMEPLAY_COLLISION_STEP_RESULT = {\n    candidatePairs: [],\n    overlapManifolds: [],\n    sensorManifolds: [],\n    solidManifolds: [],\n    notifiedPairIds: [],\n    suppressedPairIds: []\n};\nconst resolveSceneTimeStep = (tickRate)=>{\n    if (!Number.isFinite(tickRate) || !tickRate || tickRate <= 0) {\n        return 1 / DEFAULT_SCENE_TICK_RATE;\n    }\n    return 1 / tickRate;\n};\nconst resolveLegacyMass = (gameObject)=>{\n    var _gameObject_phisics_mass;\n    const requestedMass = (_gameObject_phisics_mass = gameObject.phisics.mass) !== null && _gameObject_phisics_mass !== void 0 ? _gameObject_phisics_mass : DEFAULT_DYNAMIC_MASS;\n    if (!Number.isFinite(requestedMass) || requestedMass <= 0) {\n        return DEFAULT_DYNAMIC_MASS;\n    }\n    return requestedMass;\n};\nconst scaleMassProperties = (massProperties, targetMass, inertiaScale = DEFAULT_INERTIA_SCALE)=>{\n    if (!Number.isFinite(targetMass) || targetMass <= 0) {\n        throw new Error(\"Target mass must be a finite positive number.\");\n    }\n    if (!Number.isFinite(inertiaScale) || inertiaScale <= 0) {\n        throw new Error(\"Inertia scale must be a finite positive number.\");\n    }\n    const massScale = targetMass / massProperties.mass;\n    const scaledInertia = massProperties.inertia * massScale * inertiaScale;\n    return {\n        area: massProperties.area,\n        mass: targetMass,\n        invMass: 1 / targetMass,\n        centroid: massProperties.centroid.clone(),\n        inertia: scaledInertia,\n        invInertia: scaledInertia > 0 ? 1 / scaledInertia : 0\n    };\n};\nconst resolveInertiaScale = (gameObject)=>{\n    var _gameObject_phisics_inertiaScale;\n    const requestedInertiaScale = (_gameObject_phisics_inertiaScale = gameObject.phisics.inertiaScale) !== null && _gameObject_phisics_inertiaScale !== void 0 ? _gameObject_phisics_inertiaScale : DEFAULT_INERTIA_SCALE;\n    if (!Number.isFinite(requestedInertiaScale) || requestedInertiaScale <= 0) {\n        return DEFAULT_INERTIA_SCALE;\n    }\n    return requestedInertiaScale;\n};\nconst resolveStaticFriction = (gameObject)=>{\n    var _gameObject_phisics_staticFriction, _ref;\n    return (_ref = (_gameObject_phisics_staticFriction = gameObject.phisics.staticFriction) !== null && _gameObject_phisics_staticFriction !== void 0 ? _gameObject_phisics_staticFriction : gameObject.phisics.friction) !== null && _ref !== void 0 ? _ref : 0;\n};\nconst resolveDynamicFriction = (gameObject)=>{\n    var _gameObject_phisics_dynamicFriction, _ref;\n    return (_ref = (_gameObject_phisics_dynamicFriction = gameObject.phisics.dynamicFriction) !== null && _gameObject_phisics_dynamicFriction !== void 0 ? _gameObject_phisics_dynamicFriction : gameObject.phisics.friction) !== null && _ref !== void 0 ? _ref : 0;\n};\nconst computeMassPropertiesFromHitbox = (hitbox)=>{\n    const shape = (0, _index3.createPhysicsShapeFromHitbox)(hitbox);\n    if (shape instanceof _index6.CircleShape) {\n        return (0, _index2.computeCircleMassProperties)(shape, 1);\n    }\n    return (0, _index2.computePolygonMassProperties)(shape, 1);\n};\nconst computeLocalRotationOffset = (gameObject)=>{\n    // The renderer rotates around getRotationCenter() (the bounding-box center of\n    // all hitboxes). We store body.position at that same pivot so physics and visual\n    // rotation share one anchor point.\n    return gameObject.getRotationCenter().toSubtracted(gameObject.getPosition());\n};\nconst computeMassPropertiesFromGameObject = (gameObject)=>{\n    const hitboxes = gameObject.getHitboxes();\n    const requestedMass = resolveLegacyMass(gameObject);\n    const inertiaScale = resolveInertiaScale(gameObject);\n    if (hitboxes.length === 0) {\n        const inertia = requestedMass * inertiaScale;\n        return {\n            area: 1,\n            mass: requestedMass,\n            invMass: 1 / requestedMass,\n            centroid: _index.Vector.zero(),\n            inertia,\n            invInertia: 1 / inertia\n        };\n    }\n    const contributions = hitboxes.map((hitbox)=>computeMassPropertiesFromHitbox(hitbox));\n    const combinedMassProperties = contributions.length === 1 ? contributions[0] : (0, _index2.computeCompoundMassProperties)(contributions);\n    return scaleMassProperties(combinedMassProperties, requestedMass, inertiaScale);\n};\nconst shouldWakeForTransformChange = (body, gameObject)=>{\n    if (body.bodyType !== \"dynamic\") {\n        return false;\n    }\n    // body.position is the rotation-center pivot, so compare against that same point.\n    const localRotationOffset = computeLocalRotationOffset(gameObject);\n    const nextPosition = gameObject.getScenePosition().toAdded(localRotationOffset);\n    return body.position.toSubtracted(nextPosition).squaredMagnitude() > WAKE_EPSILON * WAKE_EPSILON || Math.abs(body.angle - gameObject.rotation) > WAKE_EPSILON || body.linearVelocity.toSubtracted(gameObject.speed).squaredMagnitude() > WAKE_EPSILON * WAKE_EPSILON || Math.abs(body.angularVelocity - gameObject.angularVelocity) > WAKE_EPSILON;\n};\nconst syncBodyFromGameObject = (body, gameObject)=>{\n    if (shouldWakeForTransformChange(body, gameObject)) {\n        body.sleep.isSleeping = false;\n        body.sleep.sleepTime = 0;\n    }\n    body.setBodyType(gameObject.phisics.immovable ? \"static\" : \"dynamic\");\n    body.setMassProperties(computeMassPropertiesFromGameObject(gameObject));\n    // body.position is the rotation pivot (bounding-box center) so that physics\n    // rotation and visual rotation share the same anchor point.\n    const localRotationOffset = computeLocalRotationOffset(gameObject);\n    body.position = gameObject.getScenePosition().toAdded(localRotationOffset);\n    body.angle = gameObject.rotation;\n    body.linearVelocity = gameObject.speed.clone();\n    body.angularVelocity = gameObject.angularVelocity;\n    body.staticFriction = resolveStaticFriction(gameObject);\n    body.dynamicFriction = resolveDynamicFriction(gameObject);\n    var _gameObject_phisics_restitution;\n    body.restitution = (_gameObject_phisics_restitution = gameObject.phisics.restitution) !== null && _gameObject_phisics_restitution !== void 0 ? _gameObject_phisics_restitution : 1;\n};\nconst syncGameObjectFromBody = (gameObject, body)=>{\n    const motherShip = gameObject.getMotherShip();\n    // body.position is the rotation pivot; subtract the offset to get the\n    // gameObject's top-left origin position.\n    const localRotationOffset = computeLocalRotationOffset(gameObject);\n    const worldPosition = body.position.toSubtracted(localRotationOffset);\n    if (gameObject.isPositionRelativeToMotherShip() && motherShip) {\n        gameObject.setPosition(worldPosition.toSubtracted(motherShip.getScenePosition()));\n    } else {\n        gameObject.setPosition(worldPosition);\n    }\n    gameObject.rotation = body.angle;\n    gameObject.speed = body.linearVelocity.clone();\n    gameObject.angularVelocity = body.angularVelocity;\n};\nclass ScenePhysicsWorld {\n    getBody(gameObject) {\n        return this.bodies.get(gameObject.id);\n    }\n    getLastStepResult() {\n        return this.lastStepResult;\n    }\n    reset() {\n        this.bodies.clear();\n        this.state = (0, _index5.createPersistentContactSolverState)();\n        this.lastStepResult = EMPTY_GAMEPLAY_COLLISION_STEP_RESULT;\n    }\n    step(gameObjects, gravity, dt, options) {\n        const currentBodyIds = new Set(gameObjects.map((gameObject)=>gameObject.id));\n        for (const bodyId of this.bodies.keys()){\n            if (!currentBodyIds.has(bodyId)) {\n                this.bodies.delete(bodyId);\n            }\n        }\n        const activeParticipants = [];\n        const activeObjects = [];\n        for (const gameObject of gameObjects){\n            let body = this.bodies.get(gameObject.id);\n            if (!body) {\n                var _gameObject_phisics_restitution;\n                body = new _index1.PhysicsBody({\n                    bodyType: gameObject.phisics.immovable ? \"static\" : \"dynamic\",\n                    position: gameObject.getScenePosition(),\n                    angle: gameObject.rotation,\n                    linearVelocity: gameObject.speed,\n                    angularVelocity: gameObject.angularVelocity,\n                    staticFriction: resolveStaticFriction(gameObject),\n                    dynamicFriction: resolveDynamicFriction(gameObject),\n                    restitution: (_gameObject_phisics_restitution = gameObject.phisics.restitution) !== null && _gameObject_phisics_restitution !== void 0 ? _gameObject_phisics_restitution : 1,\n                    massProperties: computeMassPropertiesFromGameObject(gameObject)\n                });\n                this.bodies.set(gameObject.id, body);\n            }\n            syncBodyFromGameObject(body, gameObject);\n            if (!gameObject.isActive()) {\n                continue;\n            }\n            activeObjects.push({\n                gameObject,\n                body\n            });\n            activeParticipants.push(...(0, _index3.createGameplayCollisionParticipantsFromGameObject)(gameObject, body, {\n                bodyId: gameObject.id\n            }));\n        }\n        for (const { gameObject, body } of activeObjects){\n            (0, _index4.integrateBody)(body, gameObject.phisics.affectedByGravity && !gameObject.beingGrabbed ? gravity : _index.Vector.zero(), dt);\n        }\n        const stepResult = (0, _index3.stepGameplayCollisions)(this.state, activeParticipants, dt, options);\n        for (const { gameObject, body } of activeObjects){\n            syncGameObjectFromBody(gameObject, body);\n        }\n        this.lastStepResult = stepResult;\n        return stepResult;\n    }\n    constructor(){\n        _define_property(this, \"bodies\", new Map());\n        _define_property(this, \"state\", (0, _index5.createPersistentContactSolverState)());\n        _define_property(this, \"lastStepResult\", EMPTY_GAMEPLAY_COLLISION_STEP_RESULT);\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Shapes/CircleShape.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"CircleShape\", {\n    enumerable: true,\n    get: function() {\n        return CircleShape;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst _Transform = require(\"./Transform.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass CircleShape {\n    getLocalCenter() {\n        return this.center.clone();\n    }\n    getWorldCenter(transform) {\n        return (0, _Transform.transformPoint)(this.center, transform);\n    }\n    getAabb(transform) {\n        const worldCenter = this.getWorldCenter(transform);\n        return (0, _Transform.createAabbFromPoints)([\n            new _index.Vector(worldCenter.x - this.radius, worldCenter.y - this.radius),\n            new _index.Vector(worldCenter.x + this.radius, worldCenter.y + this.radius)\n        ]);\n    }\n    containsPoint(point, transform) {\n        const worldCenter = this.getWorldCenter(transform);\n        return point.toSubtracted(worldCenter).squaredMagnitude() <= this.radius * this.radius;\n    }\n    constructor(center, radius){\n        _define_property(this, \"center\", void 0);\n        _define_property(this, \"radius\", void 0);\n        if (!Number.isFinite(radius) || radius <= 0) {\n            throw new Error(\"Circle radius must be a positive finite number.\");\n        }\n        this.center = center.clone();\n        this.radius = radius;\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Shapes/ConvexPolygonShape.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"ConvexPolygonShape\", {\n    enumerable: true,\n    get: function() {\n        return ConvexPolygonShape;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst _ShapeMath = require(\"./ShapeMath.js\");\nconst _Transform = require(\"./Transform.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass ConvexPolygonShape {\n    static fromRectangle(size, center = _index.Vector.zero()) {\n        return new ConvexPolygonShape((0, _ShapeMath.buildRectangleVertices)(size, center));\n    }\n    getLocalVertices() {\n        return this.vertices.map((vertex)=>vertex.clone());\n    }\n    getWorldVertices(transform) {\n        return (0, _Transform.transformPoints)(this.vertices, transform);\n    }\n    getAabb(transform) {\n        return (0, _Transform.createAabbFromPoints)(this.getWorldVertices(transform));\n    }\n    containsPoint(point, transform) {\n        const localPoint = (0, _Transform.inverseTransformPoint)(point, transform);\n        for(let index = 0; index < this.vertices.length; index++){\n            const current = this.vertices[index];\n            const next = this.vertices[(index + 1) % this.vertices.length];\n            const edge = next.toSubtracted(current);\n            const pointOffset = localPoint.toSubtracted(current);\n            const cross = edge.crossProduct(pointOffset);\n            if (cross < -_ShapeMath.SHAPE_EPSILON) {\n                return false;\n            }\n        }\n        return true;\n    }\n    constructor(vertices){\n        _define_property(this, \"vertices\", void 0);\n        this.vertices = (0, _ShapeMath.normalizeConvexVertices)(vertices);\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Shapes/ShapeMath.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get SHAPE_EPSILON () {\n        return SHAPE_EPSILON;\n    },\n    get buildRectangleVertices () {\n        return buildRectangleVertices;\n    },\n    get normalizeConvexVertices () {\n        return normalizeConvexVertices;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst SHAPE_EPSILON = 1e-9;\nconst computeSignedArea = (vertices)=>{\n    let doubledArea = 0;\n    for(let index = 0; index < vertices.length; index++){\n        const current = vertices[index];\n        const next = vertices[(index + 1) % vertices.length];\n        doubledArea += current.x * next.y - next.x * current.y;\n    }\n    return doubledArea * 0.5;\n};\nconst ensureConvexVertices = (vertices)=>{\n    if (vertices.length < 3) {\n        throw new Error(\"Convex polygons require at least three vertices.\");\n    }\n    let windingDirection = 0;\n    for(let index = 0; index < vertices.length; index++){\n        const previous = vertices[index];\n        const current = vertices[(index + 1) % vertices.length];\n        const next = vertices[(index + 2) % vertices.length];\n        const edgeA = current.toSubtracted(previous);\n        const edgeB = next.toSubtracted(current);\n        const cross = edgeA.crossProduct(edgeB);\n        if (Math.abs(cross) <= SHAPE_EPSILON) {\n            continue;\n        }\n        const currentDirection = Math.sign(cross);\n        if (windingDirection === 0) {\n            windingDirection = currentDirection;\n            continue;\n        }\n        if (currentDirection !== windingDirection) {\n            throw new Error(\"Vertices must define a convex polygon.\");\n        }\n    }\n    if (windingDirection === 0) {\n        throw new Error(\"Convex polygons must have non-zero area.\");\n    }\n};\nconst normalizeConvexVertices = (vertices)=>{\n    const clonedVertices = vertices.map((vertex)=>vertex.clone());\n    ensureConvexVertices(clonedVertices);\n    if (computeSignedArea(clonedVertices) < 0) {\n        clonedVertices.reverse();\n    }\n    return clonedVertices;\n};\nconst buildRectangleVertices = (size, center = _index.Vector.zero())=>{\n    if (size.x <= 0 || size.y <= 0) {\n        throw new Error(\"Rectangle size must be positive on both axes.\");\n    }\n    const halfX = size.x / 2;\n    const halfY = size.y / 2;\n    return [\n        new _index.Vector(center.x - halfX, center.y - halfY),\n        new _index.Vector(center.x + halfX, center.y - halfY),\n        new _index.Vector(center.x + halfX, center.y + halfY),\n        new _index.Vector(center.x - halfX, center.y + halfY)\n    ];\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Shapes/Transform.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get createAabbFromPoints () {\n        return createAabbFromPoints;\n    },\n    get inverseTransformPoint () {\n        return inverseTransformPoint;\n    },\n    get transformPoint () {\n        return transformPoint;\n    },\n    get transformPoints () {\n        return transformPoints;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst transformPoint = (point, transform)=>{\n    return point.toWorldSpace(transform.position, transform.angle);\n};\nconst inverseTransformPoint = (point, transform)=>{\n    return point.toLocalSpace(transform.position, transform.angle);\n};\nconst transformPoints = (points, transform)=>{\n    return points.map((point)=>transformPoint(point, transform));\n};\nconst createAabbFromPoints = (points)=>{\n    if (points.length === 0) {\n        throw new Error(\"Cannot create an AABB from an empty point set.\");\n    }\n    let minX = Number.POSITIVE_INFINITY;\n    let minY = Number.POSITIVE_INFINITY;\n    let maxX = Number.NEGATIVE_INFINITY;\n    let maxY = Number.NEGATIVE_INFINITY;\n    for (const point of points){\n        minX = Math.min(minX, point.x);\n        minY = Math.min(minY, point.y);\n        maxX = Math.max(maxX, point.x);\n        maxY = Math.max(maxY, point.y);\n    }\n    return {\n        min: new _index.Vector(minX, minY),\n        max: new _index.Vector(maxX, maxY)\n    };\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Shapes/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./CircleShape.js\"), exports);\n_export_star(require(\"./ConvexPolygonShape.js\"), exports);\n_export_star(require(\"./Transform.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Solver/ContactSolver.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get DEFAULT_CONTACT_SOLVER_OPTIONS () {\n        return DEFAULT_CONTACT_SOLVER_OPTIONS;\n    },\n    get buildContactConstraints () {\n        return buildContactConstraints;\n    },\n    get solveContactConstraints () {\n        return solveContactConstraints;\n    },\n    get solveContactPositions () {\n        return solveContactPositions;\n    },\n    get solveContacts () {\n        return solveContacts;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst _index1 = require(\"../Body/index.js\");\nconst _NormalContactSolver = require(\"./NormalContactSolver.js\");\nconst DEFAULT_CONTACT_SOLVER_OPTIONS = {\n    iterations: 10,\n    positionIterations: 3,\n    baumgarte: 0.2,\n    penetrationSlop: 0.005,\n    restitutionThreshold: 1\n};\nconst assertFiniteNumber = (value, label)=>{\n    if (!Number.isFinite(value)) {\n        throw new Error(`${label} must be a finite number.`);\n    }\n};\nconst assertNonNegativeNumber = (value, label)=>{\n    assertFiniteNumber(value, label);\n    if (value < 0) {\n        throw new Error(`${label} must be non-negative.`);\n    }\n};\nconst assertPositiveTimeStep = (dt)=>{\n    assertFiniteNumber(dt, \"Time step\");\n    if (dt <= 0) {\n        throw new Error(\"Time step must be positive.\");\n    }\n};\nconst resolveContactSolverOptions = (options)=>{\n    var _options_iterations;\n    const iterations = (_options_iterations = options === null || options === void 0 ? void 0 : options.iterations) !== null && _options_iterations !== void 0 ? _options_iterations : DEFAULT_CONTACT_SOLVER_OPTIONS.iterations;\n    assertFiniteNumber(iterations, \"Solver iterations\");\n    if (!Number.isInteger(iterations) || iterations <= 0) {\n        throw new Error(\"Solver iterations must be a positive integer.\");\n    }\n    var _options_positionIterations;\n    const positionIterations = (_options_positionIterations = options === null || options === void 0 ? void 0 : options.positionIterations) !== null && _options_positionIterations !== void 0 ? _options_positionIterations : DEFAULT_CONTACT_SOLVER_OPTIONS.positionIterations;\n    assertFiniteNumber(positionIterations, \"Position solver iterations\");\n    if (!Number.isInteger(positionIterations) || positionIterations <= 0) {\n        throw new Error(\"Position solver iterations must be a positive integer.\");\n    }\n    var _options_baumgarte;\n    const baumgarte = (_options_baumgarte = options === null || options === void 0 ? void 0 : options.baumgarte) !== null && _options_baumgarte !== void 0 ? _options_baumgarte : DEFAULT_CONTACT_SOLVER_OPTIONS.baumgarte;\n    assertNonNegativeNumber(baumgarte, \"Baumgarte factor\");\n    var _options_penetrationSlop;\n    const penetrationSlop = (_options_penetrationSlop = options === null || options === void 0 ? void 0 : options.penetrationSlop) !== null && _options_penetrationSlop !== void 0 ? _options_penetrationSlop : DEFAULT_CONTACT_SOLVER_OPTIONS.penetrationSlop;\n    assertNonNegativeNumber(penetrationSlop, \"Penetration slop\");\n    var _options_restitutionThreshold;\n    const restitutionThreshold = (_options_restitutionThreshold = options === null || options === void 0 ? void 0 : options.restitutionThreshold) !== null && _options_restitutionThreshold !== void 0 ? _options_restitutionThreshold : DEFAULT_CONTACT_SOLVER_OPTIONS.restitutionThreshold;\n    assertNonNegativeNumber(restitutionThreshold, \"Restitution threshold\");\n    return {\n        iterations,\n        positionIterations,\n        baumgarte,\n        penetrationSlop,\n        restitutionThreshold\n    };\n};\nconst scalarCrossVector = (scalar, vector)=>{\n    return new _index.Vector(-scalar * vector.y, scalar * vector.x);\n};\nconst cross = (a, b)=>{\n    return a.crossProduct(b);\n};\nconst rotateLocalCenter = (localCenter, angle)=>{\n    return localCenter.toRotated(angle);\n};\nconst updateWorldCenter = (body)=>{\n    body.worldCenter = body.position.toAdded(rotateLocalCenter(body.localCenter, body.angle));\n};\nconst getVelocityAtPoint = (body, contactOffset)=>{\n    return body.linearVelocity.toAdded(scalarCrossVector(body.angularVelocity, contactOffset));\n};\nconst applyVelocityImpulse = (body, impulse, contactOffset, sign)=>{\n    if (body.invMass <= 0 && body.invInertia <= 0) {\n        return;\n    }\n    body.linearVelocity.add(impulse.toMultiplied(body.invMass * sign));\n    body.angularVelocity += body.invInertia * cross(contactOffset, impulse) * sign;\n};\nconst applyPositionImpulse = (body, impulse, contactOffset, sign)=>{\n    if (body.invMass <= 0 && body.invInertia <= 0) {\n        return;\n    }\n    body.position.add(impulse.toMultiplied(body.invMass * sign));\n    body.angle += body.invInertia * cross(contactOffset, impulse) * sign;\n    updateWorldCenter(body);\n};\nconst buildContactConstraints = (bodies, solverBodies, manifolds, dt, options)=>{\n    const resolvedOptions = resolveContactSolverOptions(options);\n    assertPositiveTimeStep(dt);\n    const constraints = [];\n    for (const manifold of manifolds){\n        const bodyA = bodies.get(manifold.bodyIdA);\n        const bodyB = bodies.get(manifold.bodyIdB);\n        const solverBodyA = solverBodies.get(manifold.bodyIdA);\n        const solverBodyB = solverBodies.get(manifold.bodyIdB);\n        if (!bodyA || !bodyB || !solverBodyA || !solverBodyB) {\n            continue;\n        }\n        const tangent = manifold.normal.toLeftPerpendicular();\n        const staticFriction = (0, _index1.mixFriction)(bodyA.staticFriction, bodyB.staticFriction);\n        const dynamicFriction = (0, _index1.mixFriction)(bodyA.dynamicFriction, bodyB.dynamicFriction);\n        const restitution = (0, _index1.mixRestitution)(bodyA.restitution, bodyB.restitution);\n        const points = manifold.points.map((point)=>{\n            const ra = point.position.toSubtracted(solverBodyA.worldCenter);\n            const rb = point.position.toSubtracted(solverBodyB.worldCenter);\n            const raCrossN = cross(ra, manifold.normal);\n            const rbCrossN = cross(rb, manifold.normal);\n            const normalMassDenominator = solverBodyA.invMass + solverBodyB.invMass + raCrossN * raCrossN * solverBodyA.invInertia + rbCrossN * rbCrossN * solverBodyB.invInertia;\n            if (normalMassDenominator <= 0) {\n                return null;\n            }\n            const raCrossT = cross(ra, tangent);\n            const rbCrossT = cross(rb, tangent);\n            const tangentMassDenominator = solverBodyA.invMass + solverBodyB.invMass + raCrossT * raCrossT * solverBodyA.invInertia + rbCrossT * rbCrossT * solverBodyB.invInertia;\n            if (tangentMassDenominator <= 0) {\n                return null;\n            }\n            const velocityA = getVelocityAtPoint(solverBodyA, ra);\n            const velocityB = getVelocityAtPoint(solverBodyB, rb);\n            const normalVelocity = velocityB.toSubtracted(velocityA).dotProduct(manifold.normal);\n            const velocityBias = restitution > 0 && normalVelocity < -resolvedOptions.restitutionThreshold ? -restitution * normalVelocity : 0;\n            return {\n                id: point.id,\n                position: point.position.clone(),\n                penetration: point.penetration,\n                ra,\n                rb,\n                normalMass: 1 / normalMassDenominator,\n                tangentMass: 1 / tangentMassDenominator,\n                velocityBias,\n                accumulatedNormalImpulse: 0,\n                accumulatedTangentImpulse: 0\n            };\n        }).filter((point)=>point !== null);\n        if (points.length === 0) {\n            continue;\n        }\n        constraints.push({\n            bodyIdA: manifold.bodyIdA,\n            bodyIdB: manifold.bodyIdB,\n            shapeIdA: manifold.shapeIdA,\n            shapeIdB: manifold.shapeIdB,\n            normal: manifold.normal.clone(),\n            tangent,\n            staticFriction,\n            dynamicFriction,\n            points\n        });\n    }\n    return constraints;\n};\nconst solveContactConstraints = (solverBodies, constraints, options)=>{\n    const resolvedOptions = resolveContactSolverOptions(options);\n    for(let iteration = 0; iteration < resolvedOptions.iterations; iteration++){\n        for (const constraint of constraints){\n            const bodyA = solverBodies.get(constraint.bodyIdA);\n            const bodyB = solverBodies.get(constraint.bodyIdB);\n            if (!bodyA || !bodyB) {\n                continue;\n            }\n            for (const point of constraint.points){\n                const normalVelocity = getVelocityAtPoint(bodyB, point.rb).toSubtracted(getVelocityAtPoint(bodyA, point.ra)).dotProduct(constraint.normal);\n                const normalImpulseDelta = -(normalVelocity - point.velocityBias) * point.normalMass;\n                const nextNormalImpulse = Math.max(point.accumulatedNormalImpulse + normalImpulseDelta, 0);\n                const appliedNormalImpulse = nextNormalImpulse - point.accumulatedNormalImpulse;\n                point.accumulatedNormalImpulse = nextNormalImpulse;\n                if (appliedNormalImpulse !== 0) {\n                    const normalImpulse = constraint.normal.toMultiplied(appliedNormalImpulse);\n                    applyVelocityImpulse(bodyA, normalImpulse, point.ra, -1);\n                    applyVelocityImpulse(bodyB, normalImpulse, point.rb, 1);\n                }\n                const tangentVelocity = getVelocityAtPoint(bodyB, point.rb).toSubtracted(getVelocityAtPoint(bodyA, point.ra)).dotProduct(constraint.tangent);\n                const tangentImpulseDelta = -tangentVelocity * point.tangentMass;\n                const tangentCandidate = point.accumulatedTangentImpulse + tangentImpulseDelta;\n                const maxStaticImpulse = constraint.staticFriction * point.accumulatedNormalImpulse;\n                const maxDynamicImpulse = constraint.dynamicFriction * point.accumulatedNormalImpulse;\n                const nextTangentImpulse = Math.abs(tangentCandidate) <= maxStaticImpulse ? tangentCandidate : Math.max(-maxDynamicImpulse, Math.min(tangentCandidate, maxDynamicImpulse));\n                const appliedTangentImpulse = nextTangentImpulse - point.accumulatedTangentImpulse;\n                point.accumulatedTangentImpulse = nextTangentImpulse;\n                if (appliedTangentImpulse !== 0) {\n                    const tangentImpulse = constraint.tangent.toMultiplied(appliedTangentImpulse);\n                    applyVelocityImpulse(bodyA, tangentImpulse, point.ra, -1);\n                    applyVelocityImpulse(bodyB, tangentImpulse, point.rb, 1);\n                }\n            }\n        }\n    }\n};\nconst solveContactPositions = (solverBodies, constraints, options)=>{\n    const resolvedOptions = resolveContactSolverOptions(options);\n    // Run a small number of position correction iterations (separate from\n    // velocity iterations) — typically 3 vs 10 velocity iterations, matching\n    // the Box2D approach to avoid over-correction jitter.\n    for(let iteration = 0; iteration < resolvedOptions.positionIterations; iteration++){\n        for (const constraint of constraints){\n            const bodyA = solverBodies.get(constraint.bodyIdA);\n            const bodyB = solverBodies.get(constraint.bodyIdB);\n            if (!bodyA || !bodyB) {\n                continue;\n            }\n            for (const point of constraint.points){\n                const correction = Math.max(point.penetration - resolvedOptions.penetrationSlop, 0);\n                if (correction <= 0) {\n                    continue;\n                }\n                const impulseMagnitude = resolvedOptions.baumgarte * correction * point.normalMass;\n                const impulse = constraint.normal.toMultiplied(impulseMagnitude);\n                applyPositionImpulse(bodyA, impulse, point.ra, -1);\n                applyPositionImpulse(bodyB, impulse, point.rb, 1);\n            }\n        }\n    }\n};\nconst solveContacts = (bodies, manifolds, dt, options)=>{\n    assertPositiveTimeStep(dt);\n    const solverBodies = (0, _NormalContactSolver.createSolverBodyMap)(bodies);\n    const constraints = buildContactConstraints(bodies, solverBodies, manifolds, dt, options);\n    solveContactConstraints(solverBodies, constraints, options);\n    solveContactPositions(solverBodies, constraints, options);\n    (0, _NormalContactSolver.writeBackSolverBodies)(bodies, solverBodies);\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Solver/NormalContactSolver.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get DEFAULT_NORMAL_SOLVER_OPTIONS () {\n        return DEFAULT_NORMAL_SOLVER_OPTIONS;\n    },\n    get buildNormalContactConstraints () {\n        return buildNormalContactConstraints;\n    },\n    get createSolverBodyMap () {\n        return createSolverBodyMap;\n    },\n    get solveNormalContactConstraints () {\n        return solveNormalContactConstraints;\n    },\n    get solveNormalContactPositions () {\n        return solveNormalContactPositions;\n    },\n    get solveNormalContacts () {\n        return solveNormalContacts;\n    },\n    get writeBackSolverBodies () {\n        return writeBackSolverBodies;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst DEFAULT_NORMAL_SOLVER_OPTIONS = {\n    iterations: 10,\n    positionIterations: 3,\n    baumgarte: 0.2,\n    penetrationSlop: 0.005\n};\nconst assertFiniteNumber = (value, label)=>{\n    if (!Number.isFinite(value)) {\n        throw new Error(`${label} must be a finite number.`);\n    }\n};\nconst assertNonNegativeNumber = (value, label)=>{\n    assertFiniteNumber(value, label);\n    if (value < 0) {\n        throw new Error(`${label} must be non-negative.`);\n    }\n};\nconst assertPositiveTimeStep = (dt)=>{\n    assertFiniteNumber(dt, \"Time step\");\n    if (dt <= 0) {\n        throw new Error(\"Time step must be positive.\");\n    }\n};\nconst resolveNormalSolverOptions = (options)=>{\n    var _options_iterations;\n    const iterations = (_options_iterations = options === null || options === void 0 ? void 0 : options.iterations) !== null && _options_iterations !== void 0 ? _options_iterations : DEFAULT_NORMAL_SOLVER_OPTIONS.iterations;\n    assertFiniteNumber(iterations, \"Solver iterations\");\n    if (!Number.isInteger(iterations) || iterations <= 0) {\n        throw new Error(\"Solver iterations must be a positive integer.\");\n    }\n    var _options_positionIterations;\n    const positionIterations = (_options_positionIterations = options === null || options === void 0 ? void 0 : options.positionIterations) !== null && _options_positionIterations !== void 0 ? _options_positionIterations : DEFAULT_NORMAL_SOLVER_OPTIONS.positionIterations;\n    assertFiniteNumber(positionIterations, \"Position solver iterations\");\n    if (!Number.isInteger(positionIterations) || positionIterations <= 0) {\n        throw new Error(\"Position solver iterations must be a positive integer.\");\n    }\n    var _options_baumgarte;\n    const baumgarte = (_options_baumgarte = options === null || options === void 0 ? void 0 : options.baumgarte) !== null && _options_baumgarte !== void 0 ? _options_baumgarte : DEFAULT_NORMAL_SOLVER_OPTIONS.baumgarte;\n    assertNonNegativeNumber(baumgarte, \"Baumgarte factor\");\n    var _options_penetrationSlop;\n    const penetrationSlop = (_options_penetrationSlop = options === null || options === void 0 ? void 0 : options.penetrationSlop) !== null && _options_penetrationSlop !== void 0 ? _options_penetrationSlop : DEFAULT_NORMAL_SOLVER_OPTIONS.penetrationSlop;\n    assertNonNegativeNumber(penetrationSlop, \"Penetration slop\");\n    return {\n        iterations,\n        positionIterations,\n        baumgarte,\n        penetrationSlop\n    };\n};\nconst scalarCrossVector = (scalar, vector)=>{\n    return new _index.Vector(-scalar * vector.y, scalar * vector.x);\n};\nconst cross = (a, b)=>{\n    return a.crossProduct(b);\n};\nconst rotateLocalCenter = (localCenter, angle)=>{\n    return localCenter.toRotated(angle);\n};\nconst createSolverBodyMap = (bodies)=>{\n    const solverBodies = new Map();\n    for (const [bodyId, body] of bodies.entries()){\n        const localCenter = body.localCenter.clone();\n        const worldCenter = body.position.toAdded(rotateLocalCenter(localCenter, body.angle));\n        solverBodies.set(bodyId, {\n            bodyId,\n            position: body.position.clone(),\n            angle: body.angle,\n            localCenter,\n            worldCenter,\n            linearVelocity: body.linearVelocity.clone(),\n            angularVelocity: body.angularVelocity,\n            invMass: body.invMass,\n            invInertia: body.invInertia\n        });\n    }\n    return solverBodies;\n};\nconst updateWorldCenter = (body)=>{\n    body.worldCenter = body.position.toAdded(rotateLocalCenter(body.localCenter, body.angle));\n};\nconst getVelocityAtPoint = (body, contactOffset)=>{\n    return body.linearVelocity.toAdded(scalarCrossVector(body.angularVelocity, contactOffset));\n};\nconst applyImpulse = (body, impulse, contactOffset, sign)=>{\n    if (body.invMass <= 0 && body.invInertia <= 0) {\n        return;\n    }\n    body.linearVelocity.add(impulse.toMultiplied(body.invMass * sign));\n    body.angularVelocity += body.invInertia * cross(contactOffset, impulse) * sign;\n};\nconst applyPositionImpulse = (body, impulse, contactOffset, sign)=>{\n    if (body.invMass <= 0 && body.invInertia <= 0) {\n        return;\n    }\n    body.position.add(impulse.toMultiplied(body.invMass * sign));\n    body.angle += body.invInertia * cross(contactOffset, impulse) * sign;\n    updateWorldCenter(body);\n};\nconst buildNormalContactConstraints = (solverBodies, manifolds, dt, options)=>{\n    resolveNormalSolverOptions(options);\n    assertPositiveTimeStep(dt);\n    const constraints = [];\n    for (const manifold of manifolds){\n        const bodyA = solverBodies.get(manifold.bodyIdA);\n        const bodyB = solverBodies.get(manifold.bodyIdB);\n        if (!bodyA || !bodyB) {\n            continue;\n        }\n        const points = manifold.points.map((point)=>{\n            const ra = point.position.toSubtracted(bodyA.worldCenter);\n            const rb = point.position.toSubtracted(bodyB.worldCenter);\n            const raCrossN = cross(ra, manifold.normal);\n            const rbCrossN = cross(rb, manifold.normal);\n            const normalMassDenominator = bodyA.invMass + bodyB.invMass + raCrossN * raCrossN * bodyA.invInertia + rbCrossN * rbCrossN * bodyB.invInertia;\n            if (normalMassDenominator <= 0) {\n                return null;\n            }\n            return {\n                id: point.id,\n                position: point.position.clone(),\n                penetration: point.penetration,\n                ra,\n                rb,\n                normalMass: 1 / normalMassDenominator,\n                accumulatedImpulse: 0\n            };\n        }).filter((point)=>point !== null);\n        if (points.length === 0) {\n            continue;\n        }\n        constraints.push({\n            bodyIdA: manifold.bodyIdA,\n            bodyIdB: manifold.bodyIdB,\n            normal: manifold.normal.clone(),\n            points\n        });\n    }\n    return constraints;\n};\nconst solveNormalContactConstraints = (solverBodies, constraints, options)=>{\n    const resolvedOptions = resolveNormalSolverOptions(options);\n    for(let iteration = 0; iteration < resolvedOptions.iterations; iteration++){\n        for (const constraint of constraints){\n            const bodyA = solverBodies.get(constraint.bodyIdA);\n            const bodyB = solverBodies.get(constraint.bodyIdB);\n            if (!bodyA || !bodyB) {\n                continue;\n            }\n            for (const point of constraint.points){\n                const velocityA = getVelocityAtPoint(bodyA, point.ra);\n                const velocityB = getVelocityAtPoint(bodyB, point.rb);\n                const relativeVelocity = velocityB.toSubtracted(velocityA);\n                const normalVelocity = relativeVelocity.dotProduct(constraint.normal);\n                const impulseDelta = -normalVelocity * point.normalMass;\n                const nextAccumulatedImpulse = Math.max(point.accumulatedImpulse + impulseDelta, 0);\n                const appliedImpulse = nextAccumulatedImpulse - point.accumulatedImpulse;\n                if (appliedImpulse <= 0) {\n                    continue;\n                }\n                point.accumulatedImpulse = nextAccumulatedImpulse;\n                const impulse = constraint.normal.toMultiplied(appliedImpulse);\n                applyImpulse(bodyA, impulse, point.ra, -1);\n                applyImpulse(bodyB, impulse, point.rb, 1);\n            }\n        }\n    }\n};\nconst solveNormalContactPositions = (solverBodies, constraints, options)=>{\n    const resolvedOptions = resolveNormalSolverOptions(options);\n    // Run a small number of position correction iterations (separate from\n    // velocity iterations) — typically 3 vs 10 velocity iterations, matching\n    // the Box2D approach to avoid over-correction jitter.\n    for(let iteration = 0; iteration < resolvedOptions.positionIterations; iteration++){\n        for (const constraint of constraints){\n            const bodyA = solverBodies.get(constraint.bodyIdA);\n            const bodyB = solverBodies.get(constraint.bodyIdB);\n            if (!bodyA || !bodyB) {\n                continue;\n            }\n            for (const point of constraint.points){\n                const correction = Math.max(point.penetration - resolvedOptions.penetrationSlop, 0);\n                if (correction <= 0) {\n                    continue;\n                }\n                const impulseMagnitude = resolvedOptions.baumgarte * correction * point.normalMass;\n                const impulse = constraint.normal.toMultiplied(impulseMagnitude);\n                applyPositionImpulse(bodyA, impulse, point.ra, -1);\n                applyPositionImpulse(bodyB, impulse, point.rb, 1);\n            }\n        }\n    }\n};\nconst writeBackSolverBodies = (bodies, solverBodies)=>{\n    for (const [bodyId, body] of bodies.entries()){\n        const solverBody = solverBodies.get(bodyId);\n        if (!solverBody) {\n            continue;\n        }\n        body.position = solverBody.position.clone();\n        body.angle = solverBody.angle;\n        body.linearVelocity = solverBody.linearVelocity.clone();\n        body.angularVelocity = solverBody.angularVelocity;\n    }\n};\nconst solveNormalContacts = (bodies, manifolds, dt, options)=>{\n    assertPositiveTimeStep(dt);\n    const solverBodies = createSolverBodyMap(bodies);\n    const constraints = buildNormalContactConstraints(solverBodies, manifolds, dt, options);\n    solveNormalContactConstraints(solverBodies, constraints, options);\n    solveNormalContactPositions(solverBodies, constraints, options);\n    writeBackSolverBodies(bodies, solverBodies);\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Solver/PersistentContactSolver.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS () {\n        return DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS;\n    },\n    get buildPersistentContactConstraints () {\n        return buildPersistentContactConstraints;\n    },\n    get cacheSolvedContactImpulses () {\n        return cacheSolvedContactImpulses;\n    },\n    get createPersistentContactSolverState () {\n        return createPersistentContactSolverState;\n    },\n    get stepPersistentContacts () {\n        return stepPersistentContacts;\n    },\n    get updateBodySleepStates () {\n        return updateBodySleepStates;\n    },\n    get wakeBodiesForExternalMotion () {\n        return wakeBodiesForExternalMotion;\n    },\n    get wakeBodiesFromManifolds () {\n        return wakeBodiesFromManifolds;\n    },\n    get wakeBody () {\n        return wakeBody;\n    },\n    get warmStartContactConstraints () {\n        return warmStartContactConstraints;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst _ContactSolver = require(\"./ContactSolver.js\");\nconst _NormalContactSolver = require(\"./NormalContactSolver.js\");\nconst DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS = {\n    ..._ContactSolver.DEFAULT_CONTACT_SOLVER_OPTIONS,\n    enableWarmStarting: true,\n    warmStartScale: 1,\n    linearSleepTolerance: 0.05,\n    angularSleepTolerance: 0.05,\n    timeToSleep: 0.5\n};\nconst EXTERNAL_WAKE_EPSILON = 1e-8;\nconst assertFiniteNumber = (value, label)=>{\n    if (!Number.isFinite(value)) {\n        throw new Error(`${label} must be a finite number.`);\n    }\n};\nconst assertNonNegativeNumber = (value, label)=>{\n    assertFiniteNumber(value, label);\n    if (value < 0) {\n        throw new Error(`${label} must be non-negative.`);\n    }\n};\nconst assertPositiveTimeStep = (dt)=>{\n    assertFiniteNumber(dt, \"Time step\");\n    if (dt <= 0) {\n        throw new Error(\"Time step must be positive.\");\n    }\n};\nconst resolvePersistentContactSolverOptions = (options)=>{\n    var _options_iterations;\n    const iterations = (_options_iterations = options === null || options === void 0 ? void 0 : options.iterations) !== null && _options_iterations !== void 0 ? _options_iterations : DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS.iterations;\n    assertFiniteNumber(iterations, \"Solver iterations\");\n    if (!Number.isInteger(iterations) || iterations <= 0) {\n        throw new Error(\"Solver iterations must be a positive integer.\");\n    }\n    var _options_positionIterations;\n    const positionIterations = (_options_positionIterations = options === null || options === void 0 ? void 0 : options.positionIterations) !== null && _options_positionIterations !== void 0 ? _options_positionIterations : DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS.positionIterations;\n    assertFiniteNumber(positionIterations, \"Position solver iterations\");\n    if (!Number.isInteger(positionIterations) || positionIterations <= 0) {\n        throw new Error(\"Position solver iterations must be a positive integer.\");\n    }\n    var _options_baumgarte;\n    const baumgarte = (_options_baumgarte = options === null || options === void 0 ? void 0 : options.baumgarte) !== null && _options_baumgarte !== void 0 ? _options_baumgarte : DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS.baumgarte;\n    assertNonNegativeNumber(baumgarte, \"Baumgarte factor\");\n    var _options_penetrationSlop;\n    const penetrationSlop = (_options_penetrationSlop = options === null || options === void 0 ? void 0 : options.penetrationSlop) !== null && _options_penetrationSlop !== void 0 ? _options_penetrationSlop : DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS.penetrationSlop;\n    assertNonNegativeNumber(penetrationSlop, \"Penetration slop\");\n    var _options_restitutionThreshold;\n    const restitutionThreshold = (_options_restitutionThreshold = options === null || options === void 0 ? void 0 : options.restitutionThreshold) !== null && _options_restitutionThreshold !== void 0 ? _options_restitutionThreshold : DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS.restitutionThreshold;\n    assertNonNegativeNumber(restitutionThreshold, \"Restitution threshold\");\n    var _options_warmStartScale;\n    const warmStartScale = (_options_warmStartScale = options === null || options === void 0 ? void 0 : options.warmStartScale) !== null && _options_warmStartScale !== void 0 ? _options_warmStartScale : DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS.warmStartScale;\n    assertNonNegativeNumber(warmStartScale, \"Warm-start scale\");\n    var _options_linearSleepTolerance;\n    const linearSleepTolerance = (_options_linearSleepTolerance = options === null || options === void 0 ? void 0 : options.linearSleepTolerance) !== null && _options_linearSleepTolerance !== void 0 ? _options_linearSleepTolerance : DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS.linearSleepTolerance;\n    assertNonNegativeNumber(linearSleepTolerance, \"Linear sleep tolerance\");\n    var _options_angularSleepTolerance;\n    const angularSleepTolerance = (_options_angularSleepTolerance = options === null || options === void 0 ? void 0 : options.angularSleepTolerance) !== null && _options_angularSleepTolerance !== void 0 ? _options_angularSleepTolerance : DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS.angularSleepTolerance;\n    assertNonNegativeNumber(angularSleepTolerance, \"Angular sleep tolerance\");\n    var _options_timeToSleep;\n    const timeToSleep = (_options_timeToSleep = options === null || options === void 0 ? void 0 : options.timeToSleep) !== null && _options_timeToSleep !== void 0 ? _options_timeToSleep : DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS.timeToSleep;\n    assertNonNegativeNumber(timeToSleep, \"Time to sleep\");\n    var _options_enableWarmStarting;\n    return {\n        iterations,\n        positionIterations,\n        baumgarte,\n        penetrationSlop,\n        restitutionThreshold,\n        enableWarmStarting: (_options_enableWarmStarting = options === null || options === void 0 ? void 0 : options.enableWarmStarting) !== null && _options_enableWarmStarting !== void 0 ? _options_enableWarmStarting : DEFAULT_PERSISTENT_CONTACT_SOLVER_OPTIONS.enableWarmStarting,\n        warmStartScale,\n        linearSleepTolerance,\n        angularSleepTolerance,\n        timeToSleep\n    };\n};\nconst cross = (a, b)=>{\n    return a.crossProduct(b);\n};\nconst applyVelocityImpulse = (body, impulse, contactOffset, sign)=>{\n    if (body.invMass <= 0 && body.invInertia <= 0) {\n        return;\n    }\n    body.linearVelocity.add(impulse.toMultiplied(body.invMass * sign));\n    body.angularVelocity += body.invInertia * cross(contactOffset, impulse) * sign;\n};\nconst createPersistentContactSolverState = ()=>{\n    return {\n        contactImpulses: new Map()\n    };\n};\nconst createContactCacheKey = (constraint, point)=>{\n    return JSON.stringify([\n        constraint.bodyIdA,\n        constraint.shapeIdA,\n        constraint.bodyIdB,\n        constraint.shapeIdB,\n        point.id\n    ]);\n};\nconst buildPersistentContactConstraints = (state, bodies, solverBodies, manifolds, dt, options)=>{\n    const resolvedOptions = resolvePersistentContactSolverOptions(options);\n    const constraints = (0, _ContactSolver.buildContactConstraints)(bodies, solverBodies, manifolds, dt, resolvedOptions);\n    if (!resolvedOptions.enableWarmStarting) {\n        return constraints;\n    }\n    for (const constraint of constraints){\n        for (const point of constraint.points){\n            const cachedImpulse = state.contactImpulses.get(createContactCacheKey(constraint, point));\n            if (!cachedImpulse) {\n                continue;\n            }\n            point.accumulatedNormalImpulse = cachedImpulse.normalImpulse * resolvedOptions.warmStartScale;\n            point.accumulatedTangentImpulse = cachedImpulse.tangentImpulse * resolvedOptions.warmStartScale;\n        }\n    }\n    return constraints;\n};\nconst warmStartContactConstraints = (solverBodies, constraints)=>{\n    for (const constraint of constraints){\n        const bodyA = solverBodies.get(constraint.bodyIdA);\n        const bodyB = solverBodies.get(constraint.bodyIdB);\n        if (!bodyA || !bodyB) {\n            continue;\n        }\n        for (const point of constraint.points){\n            if (point.accumulatedNormalImpulse === 0 && point.accumulatedTangentImpulse === 0) {\n                continue;\n            }\n            const totalImpulse = constraint.normal.toMultiplied(point.accumulatedNormalImpulse).toAdded(constraint.tangent.toMultiplied(point.accumulatedTangentImpulse));\n            applyVelocityImpulse(bodyA, totalImpulse, point.ra, -1);\n            applyVelocityImpulse(bodyB, totalImpulse, point.rb, 1);\n        }\n    }\n};\nconst cacheSolvedContactImpulses = (state, constraints)=>{\n    const nextContactImpulses = new Map();\n    for (const constraint of constraints){\n        for (const point of constraint.points){\n            if (point.accumulatedNormalImpulse === 0 && point.accumulatedTangentImpulse === 0) {\n                continue;\n            }\n            nextContactImpulses.set(createContactCacheKey(constraint, point), {\n                normalImpulse: point.accumulatedNormalImpulse,\n                tangentImpulse: point.accumulatedTangentImpulse\n            });\n        }\n    }\n    state.contactImpulses = nextContactImpulses;\n};\nconst wakeBody = (body)=>{\n    if (body.bodyType !== \"dynamic\") {\n        return;\n    }\n    body.sleep.isSleeping = false;\n    body.sleep.sleepTime = 0;\n};\nconst wakeBodiesForExternalMotion = (bodies)=>{\n    for (const body of bodies.values()){\n        if (body.bodyType !== \"dynamic\" || !body.sleep.isSleeping) {\n            continue;\n        }\n        if (body.force.squaredMagnitude() > EXTERNAL_WAKE_EPSILON * EXTERNAL_WAKE_EPSILON || Math.abs(body.torque) > EXTERNAL_WAKE_EPSILON || body.linearVelocity.squaredMagnitude() > EXTERNAL_WAKE_EPSILON * EXTERNAL_WAKE_EPSILON || Math.abs(body.angularVelocity) > EXTERNAL_WAKE_EPSILON) {\n            wakeBody(body);\n        }\n    }\n};\nconst wakeBodiesFromManifolds = (bodies, manifolds)=>{\n    const adjacency = new Map();\n    for (const manifold of manifolds){\n        if (!adjacency.has(manifold.bodyIdA)) {\n            adjacency.set(manifold.bodyIdA, new Set());\n        }\n        if (!adjacency.has(manifold.bodyIdB)) {\n            adjacency.set(manifold.bodyIdB, new Set());\n        }\n        adjacency.get(manifold.bodyIdA).add(manifold.bodyIdB);\n        adjacency.get(manifold.bodyIdB).add(manifold.bodyIdA);\n    }\n    const queue = [];\n    const visited = new Set();\n    for (const [bodyId, body] of bodies.entries()){\n        if (body.bodyType === \"kinematic\" || body.bodyType === \"dynamic\" && !body.sleep.isSleeping) {\n            queue.push(bodyId);\n            visited.add(bodyId);\n        }\n    }\n    while(queue.length > 0){\n        const bodyId = queue.shift();\n        const neighbors = adjacency.get(bodyId);\n        if (!neighbors) {\n            continue;\n        }\n        for (const neighborId of neighbors){\n            if (visited.has(neighborId)) {\n                continue;\n            }\n            visited.add(neighborId);\n            const neighbor = bodies.get(neighborId);\n            if (!neighbor || neighbor.bodyType === \"static\") {\n                continue;\n            }\n            if (neighbor.bodyType === \"dynamic\" && neighbor.sleep.isSleeping) {\n                wakeBody(neighbor);\n            }\n            queue.push(neighborId);\n        }\n    }\n};\nconst freezeSleepingSolverBodies = (bodies, solverBodies)=>{\n    for (const [bodyId, body] of bodies.entries()){\n        if (body.bodyType !== \"dynamic\" || !body.sleep.isSleeping) {\n            continue;\n        }\n        const solverBody = solverBodies.get(bodyId);\n        if (!solverBody) {\n            continue;\n        }\n        solverBody.linearVelocity = _index.Vector.zero();\n        solverBody.angularVelocity = 0;\n        solverBody.invMass = 0;\n        solverBody.invInertia = 0;\n    }\n};\nconst updateBodySleepStates = (bodies, dt, options)=>{\n    const resolvedOptions = resolvePersistentContactSolverOptions(options);\n    const linearSleepToleranceSquared = resolvedOptions.linearSleepTolerance * resolvedOptions.linearSleepTolerance;\n    for (const body of bodies.values()){\n        if (body.bodyType !== \"dynamic\" || !body.sleep.canSleep) {\n            body.sleep.isSleeping = false;\n            body.sleep.sleepTime = 0;\n            continue;\n        }\n        if (body.sleep.isSleeping) {\n            body.sleep.sleepTime = resolvedOptions.timeToSleep;\n            body.linearVelocity = _index.Vector.zero();\n            body.angularVelocity = 0;\n            continue;\n        }\n        const isSlowEnough = body.linearVelocity.squaredMagnitude() <= linearSleepToleranceSquared && Math.abs(body.angularVelocity) <= resolvedOptions.angularSleepTolerance;\n        if (!isSlowEnough) {\n            body.sleep.isSleeping = false;\n            body.sleep.sleepTime = 0;\n            continue;\n        }\n        body.sleep.sleepTime += dt;\n        if (body.sleep.sleepTime < resolvedOptions.timeToSleep) {\n            continue;\n        }\n        body.sleep.isSleeping = true;\n        body.sleep.sleepTime = resolvedOptions.timeToSleep;\n        body.linearVelocity = _index.Vector.zero();\n        body.angularVelocity = 0;\n    }\n};\nconst stepPersistentContacts = (state, bodies, manifolds, dt, options)=>{\n    const resolvedOptions = resolvePersistentContactSolverOptions(options);\n    assertPositiveTimeStep(dt);\n    wakeBodiesForExternalMotion(bodies);\n    wakeBodiesFromManifolds(bodies, manifolds);\n    if (manifolds.length === 0) {\n        state.contactImpulses.clear();\n        updateBodySleepStates(bodies, dt, resolvedOptions);\n        return;\n    }\n    const solverBodies = (0, _NormalContactSolver.createSolverBodyMap)(bodies);\n    freezeSleepingSolverBodies(bodies, solverBodies);\n    const constraints = buildPersistentContactConstraints(state, bodies, solverBodies, manifolds, dt, resolvedOptions);\n    if (resolvedOptions.enableWarmStarting) {\n        warmStartContactConstraints(solverBodies, constraints);\n    }\n    (0, _ContactSolver.solveContactConstraints)(solverBodies, constraints, resolvedOptions);\n    (0, _ContactSolver.solveContactPositions)(solverBodies, constraints, resolvedOptions);\n    (0, _NormalContactSolver.writeBackSolverBodies)(bodies, solverBodies);\n    cacheSolvedContactImpulses(state, constraints);\n    updateBodySleepStates(bodies, dt, resolvedOptions);\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/Solver/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./ContactSolver.js\"), exports);\n_export_star(require(\"./NormalContactSolver.js\"), exports);\n_export_star(require(\"./PersistentContactSolver.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Physics/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./BroadPhase/index.js\"), exports);\n_export_star(require(\"./Body/index.js\"), exports);\n_export_star(require(\"./Contacts/index.js\"), exports);\n_export_star(require(\"./Decomposition/index.js\"), exports);\n_export_star(require(\"./Gameplay/index.js\"), exports);\n_export_star(require(\"./Integration/index.js\"), exports);\n_export_star(require(\"./MassProperties/index.js\"), exports);\n_export_star(require(\"./ScenePhysicsWorld.js\"), exports);\n_export_star(require(\"./Shapes/index.js\"), exports);\n_export_star(require(\"./Solver/index.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Scenes/SceneManager/Transitions.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get colorFlash () {\n        return colorFlash;\n    },\n    get createSceneTransition () {\n        return createSceneTransition;\n    },\n    get easeInOut () {\n        return easeInOut;\n    },\n    get fadeTransition () {\n        return fadeTransition;\n    },\n    get linearEasing () {\n        return linearEasing;\n    },\n    get slidePop () {\n        return slidePop;\n    },\n    get slidePush () {\n        return slidePush;\n    },\n    get slideReplace () {\n        return slideReplace;\n    }\n});\nconst _index = require(\"../../Lib/Vector/index.js\");\nconst linearEasing = (t)=>t;\nconst easeInOut = (t)=>t < 0.5 ? 2 * t * t : 1 - Math.pow(-2 * t + 2, 2) / 2;\nconst createSceneTransition = (step, options)=>{\n    var _options_duration;\n    return {\n        duration: (_options_duration = options === null || options === void 0 ? void 0 : options.duration) !== null && _options_duration !== void 0 ? _options_duration : 500,\n        easing: options === null || options === void 0 ? void 0 : options.easing,\n        incomingOnTop: options === null || options === void 0 ? void 0 : options.incomingOnTop,\n        setup: options === null || options === void 0 ? void 0 : options.setup,\n        step,\n        cleanup: options === null || options === void 0 ? void 0 : options.cleanup\n    };\n};\nconst fadeTransition = (duration = 450)=>createSceneTransition((progress, { from, to })=>{\n        if (from) {\n            from.setOpacity(1 - progress);\n        }\n        to.setOpacity(progress);\n    }, {\n        duration,\n        setup: (_, { to })=>{\n            to.setOpacity(0);\n        },\n        cleanup: (_, { from, to })=>{\n            if (from) {\n                from.setOpacity(1);\n            }\n            to.setOpacity(1);\n        }\n    });\nconst defaultSlideDistance = 400;\nconst resolveSlideDistance = (direction, scenes, distance)=>{\n    var _scenes_to_getContext;\n    if (typeof distance === \"number\") {\n        return distance;\n    }\n    const canvas = (_scenes_to_getContext = scenes.to.getContext()) === null || _scenes_to_getContext === void 0 ? void 0 : _scenes_to_getContext.getCanvas().getCanvas();\n    if (!canvas) {\n        return defaultSlideDistance;\n    }\n    const isHorizontal = direction === \"left\" || direction === \"right\";\n    return isHorizontal ? canvas.width : canvas.height;\n};\nconst slideReplace = (direction, distance, duration = 500)=>{\n    let offsetVector = _index.Vector.zero();\n    return createSceneTransition((progress, { from, to })=>{\n        const incomingOffset = offsetVector.toMultiplied(1 - progress);\n        to.setRenderOffset(incomingOffset);\n        if (from) {\n            const outgoingOffset = offsetVector.toMultiplied(-progress);\n            from.setRenderOffset(outgoingOffset);\n        }\n    }, {\n        duration,\n        easing: easeInOut,\n        setup: (_, scenes)=>{\n            const resolvedDistance = resolveSlideDistance(direction, scenes, distance);\n            const directionVectorMap = {\n                left: new _index.Vector(-resolvedDistance, 0),\n                right: new _index.Vector(resolvedDistance, 0),\n                up: new _index.Vector(0, -resolvedDistance),\n                down: new _index.Vector(0, resolvedDistance)\n            };\n            offsetVector = directionVectorMap[direction];\n            const { to } = scenes;\n            to.setRenderOffset(offsetVector);\n            to.setOpacity(1);\n        },\n        cleanup: (_, { from, to })=>{\n            to.setRenderOffset(_index.Vector.zero());\n            if (from) {\n                from.setRenderOffset(_index.Vector.zero());\n            }\n        }\n    });\n};\nconst slidePush = (direction, distance, duration = 500)=>{\n    let offsetVector = _index.Vector.zero();\n    return createSceneTransition((progress, { from, to })=>{\n        const incomingOffset = offsetVector.toMultiplied(1 - progress);\n        to.setRenderOffset(incomingOffset);\n    }, {\n        duration,\n        easing: easeInOut,\n        setup: (_, scenes)=>{\n            const resolvedDistance = resolveSlideDistance(direction, scenes, distance);\n            const directionVectorMap = {\n                left: new _index.Vector(-resolvedDistance, 0),\n                right: new _index.Vector(resolvedDistance, 0),\n                up: new _index.Vector(0, -resolvedDistance),\n                down: new _index.Vector(0, resolvedDistance)\n            };\n            offsetVector = directionVectorMap[direction];\n            const { to } = scenes;\n            to.setRenderOffset(offsetVector);\n            to.setOpacity(1);\n        },\n        cleanup: (_, { from, to })=>{\n            to.setRenderOffset(_index.Vector.zero());\n        }\n    });\n};\nconst slidePop = (direction, distance, duration = 500)=>{\n    let offsetVector = _index.Vector.zero();\n    return createSceneTransition((progress, { from, to })=>{\n        if (from) {\n            const outgoingOffset = offsetVector.toMultiplied(-progress);\n            from.setRenderOffset(outgoingOffset);\n        }\n    }, {\n        duration,\n        easing: easeInOut,\n        incomingOnTop: false,\n        setup: (_, scenes)=>{\n            const resolvedDistance = resolveSlideDistance(direction, scenes, distance);\n            const directionVectorMap = {\n                left: new _index.Vector(-resolvedDistance, 0),\n                right: new _index.Vector(resolvedDistance, 0),\n                up: new _index.Vector(0, -resolvedDistance),\n                down: new _index.Vector(0, resolvedDistance)\n            };\n            offsetVector = directionVectorMap[direction];\n            const { from } = scenes;\n            from === null || from === void 0 ? void 0 : from.setOpacity(1);\n        },\n        cleanup: (_, { from, to })=>{\n            if (from) {\n                from.setRenderOffset(_index.Vector.zero());\n            }\n        }\n    });\n};\nconst colorFlash = (color = \"white\", duration = 750)=>createSceneTransition((progress, { from, to })=>{\n        const flashAlpha = progress < 0.5 ? progress * 2 : (1 - progress) * 2;\n        if (progress < 0.5) {\n            to.setOpacity(0);\n            from === null || from === void 0 ? void 0 : from.setOpacity(1);\n        } else {\n            to.setOpacity(1);\n            from === null || from === void 0 ? void 0 : from.setOpacity(0);\n        }\n        to.setOverlay(color, flashAlpha);\n        if (from) {\n            from.setOverlay(color, flashAlpha);\n        }\n    }, {\n        duration,\n        easing: easeInOut,\n        setup: (_, { from, to })=>{\n            to.setOpacity(0);\n            from === null || from === void 0 ? void 0 : from.setOpacity(1);\n            to.setOverlay(color, 0);\n            if (from) {\n                from.setOverlay(color, 0);\n            }\n        },\n        cleanup: (_, { from, to })=>{\n            to.setOpacity(1);\n            from === null || from === void 0 ? void 0 : from.setOpacity(1);\n            to.setOverlay(null, 0);\n            if (from) {\n                from.setOverlay(null, 0);\n            }\n        }\n    });",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Scenes/SceneManager/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"SceneManager\", {\n    enumerable: true,\n    get: function() {\n        return SceneManager;\n    }\n});\nconst _Transitions = require(\"./Transitions.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass SceneManager {\n    tick() {\n        var _this_context;\n        (_this_context = this.context) === null || _this_context === void 0 ? void 0 : _this_context.incrementTickCount();\n        this.updateTransition(performance.now());\n        this.activeScenes.forEach((scene)=>scene.tick());\n    }\n    getScene(name) {\n        return this.scenes[name] || null;\n    }\n    setSceneAsCurrent(name) {\n        const scene = this.getScene(name);\n        if (scene) {\n            scene.setContext(this.context);\n            const previousActiveScenes = this.activeScenes;\n            this.activeScenes = [\n                scene\n            ];\n            this.currentScene = scene;\n            scene.activate();\n            previousActiveScenes.forEach((activeScene)=>{\n                if (activeScene !== scene) {\n                    activeScene.deactivate();\n                }\n            });\n        }\n    }\n    getAllScenes() {\n        return Object.values(this.scenes);\n    }\n    getActiveScenes() {\n        return this.activeScenes;\n    }\n    getCurrentScene() {\n        return this.currentScene;\n    }\n    pushSceneToActive(scene) {\n        scene.setContext(this.context);\n        this.activeScenes.push(scene);\n        scene.activate();\n    }\n    popSceneFromActive() {\n        const popped = this.activeScenes.pop();\n        popped === null || popped === void 0 ? void 0 : popped.deactivate();\n        return popped;\n    }\n    bindContext(context) {\n        this.context = context;\n        this.getAllScenes().forEach((scene)=>scene.setContext(context));\n    }\n    async transitionToScene(name, transition = (0, _Transitions.fadeTransition)(), mode = \"replace\") {\n        var _transition_setup;\n        const target = this.getScene(name);\n        if (!target) {\n            return Promise.reject(new Error(`Scene \"${name}\" not found`));\n        }\n        if (this.activeTransition) {\n            return Promise.reject(new Error(\"A transition is already running\"));\n        }\n        target.setContext(this.context);\n        const transitionScenes = {\n            from: this.currentScene,\n            to: target\n        };\n        const fromScene = transitionScenes.from;\n        var _transition_incomingOnTop;\n        const incomingOnTop = (_transition_incomingOnTop = transition.incomingOnTop) !== null && _transition_incomingOnTop !== void 0 ? _transition_incomingOnTop : true;\n        const targetIndex = this.activeScenes.indexOf(target);\n        const fromIndex = fromScene ? this.activeScenes.indexOf(fromScene) : -1;\n        const targetIsActive = targetIndex !== -1;\n        // Ensure the incoming scene renders during the transition, respecting\n        // whether it should be above or below the outgoing scene.\n        if (!targetIsActive) {\n            if (mode === \"push\" || incomingOnTop) {\n                this.activeScenes = [\n                    ...this.activeScenes,\n                    target\n                ];\n            } else if (fromIndex >= 0) {\n                this.activeScenes = [\n                    ...this.activeScenes.slice(0, fromIndex),\n                    target,\n                    ...this.activeScenes.slice(fromIndex)\n                ];\n            } else {\n                this.activeScenes = [\n                    ...this.activeScenes,\n                    target\n                ];\n            }\n        } else if (mode === \"push\" || incomingOnTop) {\n            this.activeScenes = [\n                ...this.activeScenes.filter((scene)=>scene !== target),\n                target\n            ];\n        }\n        this.currentScene = target;\n        target.activate();\n        (_transition_setup = transition.setup) === null || _transition_setup === void 0 ? void 0 : _transition_setup.call(transition, 0, transitionScenes);\n        return new Promise((resolve)=>{\n            this.activeTransition = {\n                ...transitionScenes,\n                startedAt: performance.now(),\n                config: transition,\n                mode,\n                resolve\n            };\n        });\n    }\n    updateTransition(now) {\n        if (!this.activeTransition) {\n            return;\n        }\n        const { from, to, config, startedAt, mode, resolve } = this.activeTransition;\n        const elapsed = now - startedAt;\n        const progress = Math.min(1, elapsed / config.duration);\n        const easedProgress = config.easing ? config.easing(progress) : progress;\n        config.step(easedProgress, {\n            from,\n            to\n        });\n        if (progress >= 1) {\n            var _config_cleanup;\n            (_config_cleanup = config.cleanup) === null || _config_cleanup === void 0 ? void 0 : _config_cleanup.call(config, 1, {\n                from,\n                to\n            });\n            const previousActiveScenes = this.activeScenes;\n            if (mode === \"replace\") {\n                this.activeScenes = [\n                    to\n                ];\n            } else if (mode === \"push\") {\n                // Keep existing stack but make sure \"to\" is on top once\n                this.activeScenes = [\n                    ...this.activeScenes.filter((scene)=>scene !== to),\n                    to\n                ];\n            }\n            const activeSet = new Set(this.activeScenes);\n            previousActiveScenes.forEach((scene)=>{\n                if (!activeSet.has(scene)) {\n                    scene.deactivate();\n                }\n            });\n            this.activeTransition = null;\n            resolve();\n        }\n    }\n    constructor(scenes, currentScene = null){\n        _define_property(this, \"scenes\", void 0);\n        _define_property(this, \"currentScene\", void 0);\n        _define_property(this, \"activeScenes\", []);\n        _define_property(this, \"context\", null);\n        _define_property(this, \"activeTransition\", null);\n        this.scenes = scenes;\n        this.currentScene = currentScene;\n        if (currentScene) {\n            this.pushSceneToActive(currentScene);\n        }\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/Scenes/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"Scene\", {\n    enumerable: true,\n    get: function() {\n        return Scene;\n    }\n});\nconst _index = require(\"../Lib/Vector/index.js\");\nconst _index1 = require(\"../Physics/index.js\");\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass Scene {\n    setup() {\n        console.log(`Setting up scene: ${this.name}`);\n    }\n    onEnter() {}\n    onExit() {}\n    tick(dt) {\n        var _this_context;\n        this.gameObjects.forEach((obj)=>{\n            obj.tick();\n        });\n        this.physicsWorld.step(this.gameObjects, this.gravity, dt !== null && dt !== void 0 ? dt : (0, _index1.resolveSceneTimeStep)((_this_context = this.context) === null || _this_context === void 0 ? void 0 : _this_context.getTickRate()));\n    }\n    getGameObjects() {\n        return this.gameObjects;\n    }\n    removeGameObject(gameObject) {\n        gameObject.notifyRemovedFromScene(this);\n        this.gameObjects = this.gameObjects.filter((go)=>go !== gameObject);\n    }\n    getLastPhysicsStepResult() {\n        return this.physicsWorld.getLastStepResult();\n    }\n    render(canvas) {\n        var _this_context, _this_context1;\n        const ctx = (_this_context = this.context) === null || _this_context === void 0 ? void 0 : _this_context.getCanvas().getContext();\n        if (!ctx) {\n            return;\n        }\n        const previousAlpha = ctx.globalAlpha;\n        ctx.globalAlpha = previousAlpha * this.opacity;\n        if (this.backgroundColor) {\n            var _this_context2;\n            ctx.save();\n            ctx.translate(this.renderOffset.x, this.renderOffset.y);\n            (_this_context2 = this.context) === null || _this_context2 === void 0 ? void 0 : _this_context2.getCanvas().getShapeDrawer().drawBackground(this.backgroundColor);\n            ctx.restore();\n        }\n        const sortedObjects = this.gameObjects.map((obj, index)=>({\n                obj,\n                index\n            })).sort((a, b)=>{\n            if (a.obj.zIndex !== b.obj.zIndex) {\n                return a.obj.zIndex - b.obj.zIndex;\n            }\n            return a.index - b.index;\n        }).map(({ obj })=>obj);\n        sortedObjects.forEach((obj)=>{\n            obj.render(canvas, this);\n        });\n        if (this.overlayColor && this.overlayAlpha > 0) {\n            ctx.save();\n            ctx.setTransform(1, 0, 0, 1, 0, 0);\n            ctx.globalAlpha = previousAlpha * this.overlayAlpha;\n            ctx.fillStyle = this.overlayColor;\n            ctx.fillRect(0, 0, ctx.canvas.width, ctx.canvas.height);\n            ctx.restore();\n        }\n        ctx.globalAlpha = previousAlpha;\n        (_this_context1 = this.context) === null || _this_context1 === void 0 ? void 0 : _this_context1.getCanvas().reset();\n    }\n    addGameObject(_objects) {\n        const gameObjects = Array.isArray(_objects) ? _objects : [\n            _objects\n        ];\n        gameObjects.forEach((go)=>{\n            go.scene = this;\n            go.setContext(this.context);\n            this.gameObjects.push(go);\n            go.notifyAddedToScene(this, this.context);\n        });\n    }\n    handleEvent(event) {\n        this.gameObjects.toReversed().forEach((obj)=>{\n            if (event.stopPropagation) return;\n            obj.handleEvent(event);\n        });\n    }\n    setContext(context) {\n        this.context = context;\n        this.gameObjects.forEach((obj)=>{\n            obj.setContext(context);\n            obj.notifyAddedToScene(this, context);\n        });\n        if (context && this.pendingEnter) {\n            this.pendingEnter = false;\n            this.runSetupIfNeeded();\n            if (this.isActive) {\n                this.onEnter();\n            }\n        }\n    }\n    getContext() {\n        return this.context;\n    }\n    getOffset() {\n        return this.offset;\n    }\n    setOffset(offset) {\n        this.offset = offset;\n    }\n    getRenderOffset() {\n        return this.renderOffset;\n    }\n    setRenderOffset(offset) {\n        this.renderOffset = offset;\n    }\n    getVisualOffset() {\n        return this.offset.toAdded(this.renderOffset);\n    }\n    getGravity() {\n        return this.gravity;\n    }\n    setGravity(gravity) {\n        this.gravity = gravity;\n    }\n    getOpacity() {\n        return this.opacity;\n    }\n    setOpacity(opacity) {\n        this.opacity = opacity;\n    }\n    setOverlay(color, alpha) {\n        this.overlayColor = color;\n        this.overlayAlpha = alpha;\n    }\n    activate() {\n        if (this.isActive) {\n            if (this.pendingEnter && this.context) {\n                this.pendingEnter = false;\n                this.runSetupIfNeeded();\n                this.onEnter();\n            }\n            return;\n        }\n        this.isActive = true;\n        if (!this.context) {\n            this.pendingEnter = true;\n            return;\n        }\n        this.runSetupIfNeeded();\n        this.onEnter();\n    }\n    deactivate() {\n        if (!this.isActive) {\n            return;\n        }\n        this.isActive = false;\n        this.pendingEnter = false;\n        this.onExit();\n    }\n    runSetupIfNeeded() {\n        if (this.didSetup) {\n            return;\n        }\n        this.didSetup = true;\n        this.setup();\n    }\n    constructor(name, backgroundColor){\n        _define_property(this, \"name\", void 0);\n        _define_property(this, \"backgroundColor\", void 0);\n        _define_property(this, \"gameObjects\", []);\n        _define_property(this, \"context\", null);\n        _define_property(this, \"offset\", new _index.Vector(0, 0));\n        _define_property(this, \"renderOffset\", _index.Vector.zero());\n        _define_property(this, \"gravity\", _index.Vector.zero());\n        _define_property(this, \"opacity\", 1);\n        _define_property(this, \"overlayColor\", null);\n        _define_property(this, \"overlayAlpha\", 0);\n        _define_property(this, \"didSetup\", false);\n        _define_property(this, \"isActive\", false);\n        _define_property(this, \"pendingEnter\", false);\n        _define_property(this, \"physicsWorld\", new _index1.ScenePhysicsWorld());\n        this.name = name;\n        this.backgroundColor = backgroundColor;\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/ScriptedEvents/Combinators/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get all () {\n        return all;\n    },\n    get conditional () {\n        return conditional;\n    },\n    get first () {\n        return first;\n    },\n    get parallel () {\n        return parallel;\n    },\n    get repeat () {\n        return repeat;\n    },\n    get repeatWhile () {\n        return repeatWhile;\n    },\n    get sequenceOf () {\n        return sequenceOf;\n    },\n    get waitForKeyPress () {\n        return waitForKeyPress;\n    },\n    get waitTicks () {\n        return waitTicks;\n    },\n    get waitUntil () {\n        return waitUntil;\n    },\n    get withTimeout () {\n        return withTimeout;\n    }\n});\nconst _index = require(\"../index.js\");\nconst sequenceOf = (events, label = null)=>{\n    return (0, _index.scripted)(async (ctx, _state)=>{\n        let state = _state;\n        for (const event of events){\n            state = await (0, _index.runEvent)(event, ctx, state);\n        }\n        return state;\n    }, label);\n};\nconst parallel = (events, label = null)=>{\n    return (0, _index.scripted)(async (ctx, state)=>{\n        const promises = events.map((event)=>(0, _index.runEvent)(event, ctx, state));\n        const newState = await Promise.all(promises).then((res)=>{\n            return res.reduce((acc, curr)=>{\n                if ((0, _index.isAborted)(curr)) return acc;\n                return {\n                    ...acc,\n                    ...curr\n                };\n            }, state);\n        });\n        return newState;\n    }, label);\n};\nconst first = (events, label = null)=>{\n    return (0, _index.scripted)(async (ctx, state)=>{\n        const promises = events.map((event)=>(0, _index.runEvent)(event, ctx, state));\n        const firstResolved = await Promise.race(promises);\n        return firstResolved;\n    }, label);\n};\nconst conditional = (predicate, thenEv, elseEv, label = null)=>{\n    return (0, _index.scripted)(async (ctx, state)=>{\n        if (predicate(state)) {\n            return (0, _index.runEvent)(thenEv, ctx, state);\n        } else {\n            return (0, _index.runEvent)(elseEv, ctx, state);\n        }\n    }, label);\n};\nconst all = (events, label = null)=>{\n    return (0, _index.scripted)(async (ctx, _state)=>{\n        const promises = events.map((event)=>()=>(0, _index.runEvent)(event, ctx, _state));\n        let state = _state;\n        return new Promise((resolve)=>{\n            promises.forEach((promise, index)=>{\n                promise().then((newState)=>{\n                    state = {\n                        ...state,\n                        ...newState\n                    };\n                    if ((0, _index.isAborted)(state)) {\n                        resolve((0, _index.abort)(state, `Aborted on ${label || \"runAll\"}\\n${state.aborted}`));\n                    }\n                    if ((0, _index.isErrored)(state)) {\n                        resolve((0, _index.failed)(state, `Failed on ${label || \"runAll\"}\\n${state.error}`));\n                    }\n                    if (index === promises.length - 1) resolve(state);\n                });\n            });\n        });\n    }, label);\n};\nconst repeatWhile = (predicate, event, label = null)=>{\n    return (0, _index.scripted)(async (ctx, state)=>{\n        let newState = state;\n        while(predicate(ctx, newState)){\n            if ((0, _index.isAborted)(newState)) {\n                return (0, _index.abort)(newState, `Aborted on ${label || \"repeatWhile\"}\\n${newState.aborted}`);\n            }\n            if ((0, _index.isErrored)(newState)) {\n                return (0, _index.failed)(newState, `Failed oon ${label || \"repeatWhile\"}\\n${newState.error}`);\n            }\n            newState = await (0, _index.runEvent)(event, ctx, newState);\n        }\n        return newState;\n    });\n};\nconst repeat = (event, times, label = null)=>{\n    return (0, _index.scripted)(async (ctx, _state)=>{\n        let state = _state;\n        for(let i = 0; i < times; i++){\n            state = await (0, _index.runEvent)(event, ctx, state);\n        }\n        return state;\n    }, label);\n};\nconst withTimeout = (event, timeout, label = null)=>{\n    return (0, _index.scripted)(async (ctx, state)=>{\n        setTimeout(()=>{\n            (0, _index.abort)(state, `Timeout on ${label || \"withTimeout\"}`);\n        }, timeout);\n        return await (0, _index.runEvent)(event, ctx, state);\n    });\n};\nconst waitTicks = (timeInTicks)=>{\n    return (0, _index.scripted)(async (ctx, state)=>{\n        const currTicks = ctx.getTickCount();\n        const tickRate = ctx.getGame().getTickRate();\n        const sleep = (ms)=>new Promise((resolve)=>setTimeout(resolve, ms));\n        while(ctx.getTickCount() < currTicks + timeInTicks){\n            await sleep(1000 / (tickRate * 2));\n        }\n        return state;\n    });\n};\nconst waitUntil = (predicate, opts)=>{\n    var _opts_pollGameTicks;\n    const pollGameTicks = (_opts_pollGameTicks = opts === null || opts === void 0 ? void 0 : opts.pollGameTicks) !== null && _opts_pollGameTicks !== void 0 ? _opts_pollGameTicks : 1;\n    var _opts_label;\n    const label = (_opts_label = opts === null || opts === void 0 ? void 0 : opts.label) !== null && _opts_label !== void 0 ? _opts_label : \"waitUntil\";\n    const sleep = (ms)=>new Promise((resolve)=>setTimeout(resolve, ms));\n    const core = (0, _index.scripted)(async (ctx, state)=>{\n        const ticksPerSecond = ctx.getTickRate();\n        while(true){\n            if (predicate(ctx, state)) return state;\n            await sleep(pollGameTicks * (1000 / ticksPerSecond));\n        }\n    }, label);\n    return core;\n};\nconst waitForKeyPress = (key, label = null)=>{\n    return waitUntil((ctx, state)=>{\n        return ctx.getPressedKeys().includes(key);\n    }, {\n        pollGameTicks: 1,\n        label: label !== null && label !== void 0 ? label : `waitForKeyPress(${key})`\n    });\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/ScriptedEvents/Library/TextBoxSequence.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get DisplayTextBox () {\n        return DisplayTextBox;\n    },\n    get TextBoxSequence () {\n        return TextBoxSequence;\n    }\n});\nconst _index = require(\"../index.js\");\nconst _TextBox = require(\"../../GameObject/Library/TextBox.js\");\nconst DisplayTextBox = (entry)=>{\n    return (0, _index.scripted)(async (ctx, state)=>{\n        return new Promise((resolve)=>{\n            var _ctx_getSceneManager_getCurrentScene;\n            const textBox = new _TextBox.TextBox(\"textBox\", entry.text, entry, ()=>resolve(state));\n            (_ctx_getSceneManager_getCurrentScene = ctx.getSceneManager().getCurrentScene()) === null || _ctx_getSceneManager_getCurrentScene === void 0 ? void 0 : _ctx_getSceneManager_getCurrentScene.addGameObject(textBox);\n        });\n    });\n};\nconst TextBoxSequence = (entries)=>{\n    return (0, _index.sequenceOf)([\n        ...entries.map((entry)=>DisplayTextBox(entry))\n    ]);\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/ScriptedEvents/Library/WaitForKeyPress.js": {
		code: "\"use strict\";",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/ScriptedEvents/Library/WalkCharacter.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"walkCharacter\", {\n    enumerable: true,\n    get: function() {\n        return walkCharacter;\n    }\n});\nconst _index = require(\"../index.js\");\nconst walkCharacter = (character, walker, label = null)=>{\n    return (0, _index.scripted)((ctx, state)=>{\n        character.setWalker(walker);\n        if (!walker.getTargetedWaypoint()) {\n            return Promise.resolve(state);\n        }\n        return new Promise((resolve)=>{\n            walker.setOnComplete(()=>resolve(state));\n            walker.start();\n        });\n    }, label !== null && label !== void 0 ? label : \"walkCharacter\");\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/ScriptedEvents/Library/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./TextBoxSequence.js\"), exports);\n_export_star(require(\"./WalkCharacter.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/ScriptedEvents/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nfunction _export(target, all) {\n    for(var name in all)Object.defineProperty(target, name, {\n        enumerable: true,\n        get: Object.getOwnPropertyDescriptor(all, name).get\n    });\n}\n_export(exports, {\n    get abort () {\n        return abort;\n    },\n    get done () {\n        return done;\n    },\n    get failed () {\n        return failed;\n    },\n    get isAborted () {\n        return isAborted;\n    },\n    get isDone () {\n        return isDone;\n    },\n    get isErrored () {\n        return isErrored;\n    },\n    get runEvent () {\n        return runEvent;\n    },\n    get scripted () {\n        return scripted;\n    }\n});\n_export_star(require(\"./Combinators/index.js\"), exports);\n_export_star(require(\"./Library/index.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}\nconst scripted = (cb, label = null)=>{\n    // This is the state that will be passed to the script event\n    const scriptState = {\n        done: false,\n        aborted: undefined,\n        error: undefined\n    };\n    return {\n        label: label !== null && label !== void 0 ? label : undefined,\n        run: (ctx, state)=>{\n            const newState = {\n                ...scriptState,\n                ...state\n            };\n            return cb(ctx, newState);\n        },\n        abort: (reason)=>abort(scriptState, reason)\n    };\n};\nconst done = (state)=>({\n        ...state,\n        done: true,\n        aborted: undefined,\n        error: undefined\n    });\nconst abort = (state, reason)=>({\n        ...state,\n        aborted: reason,\n        error: undefined,\n        done: true\n    });\nconst failed = (state, error)=>({\n        ...state,\n        aborted: undefined,\n        error: new Error(error),\n        done: true\n    });\nconst isAborted = (state)=>{\n    return state.aborted !== undefined;\n};\nconst isErrored = (state)=>{\n    return state.error !== undefined;\n};\nconst isDone = (state)=>{\n    return state.done;\n};\nconst runEvent = async (event, ctx, state)=>{\n    if (isDone(state)) {\n        return state;\n    }\n    if (isAborted(state)) {\n        return abort(state, `Sequence Aborted: ` + state.aborted);\n    }\n    if (isErrored(state)) {\n        return failed(state, `Sequence Failed: ` + state.error);\n    }\n    return event.run(ctx, state);\n};",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/SoundManager/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\nObject.defineProperty(exports, \"SoundManager\", {\n    enumerable: true,\n    get: function() {\n        return SoundManager;\n    }\n});\nfunction _define_property(obj, key, value) {\n    if (key in obj) {\n        Object.defineProperty(obj, key, {\n            value: value,\n            enumerable: true,\n            configurable: true,\n            writable: true\n        });\n    } else {\n        obj[key] = value;\n    }\n    return obj;\n}\nclass SoundManager {\n    /**\n     * Has to be called whenever the user clicks something or hits a key\n     * to unlock the audio context. Browsers require this to prevent\n     * autoplaying sounds.\n     */ async unlock() {\n        this.masterGain.connect(this.context.destination);\n        if (this.context.state !== \"running\") {\n            await this.context.resume();\n            this.active = true;\n        }\n    }\n    setMasterGain(volume) {\n        // Avoid negative volumes\n        this.masterGain.gain.value = Math.max(0, volume);\n    }\n    getMasterGain() {\n        return this.masterGain.gain.value;\n    }\n    getLoadedSounds() {\n        return this.sounds;\n    }\n    setSoundVolume(sound, volume) {\n        const audioEntry = this.sounds[sound];\n        if (!audioEntry) {\n            throw new Error(`Tried to set volume of sound ${sound} but it was not loaded into the library`);\n        }\n        audioEntry.volume = Math.max(0, volume);\n    }\n    getSoundVolume(sound) {\n        const audioEntry = this.sounds[sound];\n        if (!audioEntry) {\n            throw new Error(`Tried to set volume of sound ${sound} but it was not loaded into the library`);\n        }\n        return audioEntry.volume;\n    }\n    async loadSound(name, url, tags = []) {\n        const res = await fetch(url.toString());\n        if (!res.ok) {\n            throw new Error(`Failed to fetch audio ${name}: ${res.status}-${res.statusText}`);\n        }\n        const arrayBuffer = await res.arrayBuffer();\n        const audioBuffer = await this.context.decodeAudioData(arrayBuffer);\n        this.sounds[name] = {\n            buffer: audioBuffer,\n            volume: 1,\n            tags\n        };\n    }\n    getCurrentSongName() {\n        var _this_currentSong;\n        return (_this_currentSong = this.currentSong) === null || _this_currentSong === void 0 ? void 0 : _this_currentSong.name;\n    }\n    stopSong(options = {}) {\n        if (!this.currentSong) return;\n        var _options_fadeOut, _ref;\n        const fadeOutSeconds = Math.max(0, (_ref = (_options_fadeOut = options.fadeOut) !== null && _options_fadeOut !== void 0 ? _options_fadeOut : options.fade) !== null && _ref !== void 0 ? _ref : 0);\n        this.stopSongPlayback(this.currentSong, fadeOutSeconds);\n    }\n    /**\n     * Plays a single tracked \"song\" (typically background music).\n     * - If the same song is requested, nothing happens.\n     * - If a different song is requested, the previous song is stopped and the new one starts.\n     * - Optional fade transition is supported (in seconds) via `fade`, `fadeIn`, `fadeOut`.\n     */ playSong(name, options = {}) {\n        var _this_currentSong;\n        if (!this.active) {\n            this.unlock();\n        }\n        if (((_this_currentSong = this.currentSong) === null || _this_currentSong === void 0 ? void 0 : _this_currentSong.name) === name) {\n            return this.currentSong.controller;\n        }\n        const audioEntry = this.sounds[name];\n        if (!audioEntry) {\n            throw new Error(`Tried to play song ${name} but it was not found in the library`);\n        }\n        var _options_fadeOut, _ref;\n        const fadeOutSeconds = Math.max(0, (_ref = (_options_fadeOut = options.fadeOut) !== null && _options_fadeOut !== void 0 ? _options_fadeOut : options.fade) !== null && _ref !== void 0 ? _ref : 0);\n        var _options_fadeIn, _ref1;\n        const fadeInSeconds = Math.max(0, (_ref1 = (_options_fadeIn = options.fadeIn) !== null && _options_fadeIn !== void 0 ? _options_fadeIn : options.fade) !== null && _ref1 !== void 0 ? _ref1 : 0);\n        if (this.currentSong) {\n            this.stopSongPlayback(this.currentSong, fadeOutSeconds);\n        }\n        const { buffer, volume: defaultVolume } = audioEntry;\n        const source = this.context.createBufferSource();\n        source.buffer = buffer;\n        var _options_loop;\n        source.loop = (_options_loop = options.loop) !== null && _options_loop !== void 0 ? _options_loop : true;\n        var _options_playbackRate;\n        source.playbackRate.value = (_options_playbackRate = options.playbackRate) !== null && _options_playbackRate !== void 0 ? _options_playbackRate : 1;\n        const gain = this.context.createGain();\n        var _options_volume;\n        const targetVolume = Math.max(0, (_options_volume = options.volume) !== null && _options_volume !== void 0 ? _options_volume : defaultVolume);\n        const now = this.context.currentTime;\n        gain.gain.cancelScheduledValues(now);\n        gain.gain.setValueAtTime(fadeInSeconds > 0 ? 0 : targetVolume, now);\n        if (fadeInSeconds > 0) {\n            gain.gain.linearRampToValueAtTime(targetVolume, now + fadeInSeconds);\n        }\n        source.connect(gain);\n        gain.connect(this.masterGain);\n        const id = ++this.songIdCounter;\n        // one try-catch for each disconnect to avoid one failing to stop the others\n        const onEnd = ()=>{\n            var _this_currentSong;\n            try {\n                source.disconnect();\n            } catch  {}\n            try {\n                gain.disconnect();\n            } catch  {}\n            if (((_this_currentSong = this.currentSong) === null || _this_currentSong === void 0 ? void 0 : _this_currentSong.id) === id) {\n                this.currentSong = undefined;\n            }\n        };\n        source.onended = onEnd;\n        source.start(now);\n        const controller = {\n            name,\n            stop: (stopOptions = {})=>{\n                var _stopOptions_fadeOut, _ref;\n                const fadeOut = Math.max(0, (_ref = (_stopOptions_fadeOut = stopOptions.fadeOut) !== null && _stopOptions_fadeOut !== void 0 ? _stopOptions_fadeOut : stopOptions.fade) !== null && _ref !== void 0 ? _ref : 0);\n                this.stopSongPlayback({\n                    source,\n                    gain\n                }, fadeOut);\n            }\n        };\n        this.currentSong = {\n            id,\n            name,\n            source,\n            gain,\n            controller\n        };\n        return controller;\n    }\n    playSound(name, options = {}) {\n        // This will work only if the sonud was triggered by a\n        // User interaction.\n        if (!this.active) {\n            this.unlock();\n        }\n        const audioEntry = this.sounds[name];\n        if (!audioEntry) {\n            throw new Error(`Tried to play sound ${name} but it was not found in the library`);\n        }\n        const { buffer, volume } = audioEntry;\n        const source = this.context.createBufferSource();\n        source.buffer = buffer;\n        var _options_loop;\n        source.loop = (_options_loop = options.loop) !== null && _options_loop !== void 0 ? _options_loop : false;\n        var _options_playbackRate;\n        source.playbackRate.value = (_options_playbackRate = options.playbackRate) !== null && _options_playbackRate !== void 0 ? _options_playbackRate : 1;\n        const gain = this.context.createGain();\n        var _options_volume;\n        gain.gain.value = Math.max(0, (_options_volume = options.volume) !== null && _options_volume !== void 0 ? _options_volume : volume);\n        source.connect(gain);\n        gain.connect(this.masterGain);\n        var _options_wait;\n        const waitTime = (_options_wait = options.wait) !== null && _options_wait !== void 0 ? _options_wait : 0;\n        var _options_start;\n        const audioStart = (_options_start = options.start) !== null && _options_start !== void 0 ? _options_start : 0;\n        const audioDuration = (()=>{\n            if (options.duration) {\n                return options.duration;\n            } else if (options.end) {\n                var _options_start;\n                return options.end - ((_options_start = options.start) !== null && _options_start !== void 0 ? _options_start : 0);\n            } else {\n                return undefined;\n            }\n        })();\n        // one try-catch for each disconnect to avoid one failing to stop the others\n        const onEnd = ()=>{\n            try {\n                source.disconnect();\n            } catch  {}\n            try {\n                gain.disconnect();\n            } catch  {}\n        };\n        source.onended = onEnd;\n        source.start(this.context.currentTime + waitTime, audioStart, audioDuration);\n        return {\n            stop: ()=>{\n                try {\n                    source.stop();\n                } catch (e) {}\n                onEnd();\n            }\n        };\n    }\n    stopSongPlayback(song, fadeOutSeconds) {\n        const { source, gain } = song;\n        const now = this.context.currentTime;\n        const stopAt = now + Math.max(0, fadeOutSeconds);\n        try {\n            gain.gain.cancelScheduledValues(now);\n            gain.gain.setValueAtTime(gain.gain.value, now);\n            if (fadeOutSeconds > 0) {\n                gain.gain.linearRampToValueAtTime(0, stopAt);\n            } else {\n                gain.gain.setValueAtTime(0, now);\n            }\n        } catch  {}\n        try {\n            source.stop(stopAt);\n        } catch  {}\n    }\n    constructor(){\n        _define_property(this, \"sounds\", {});\n        _define_property(this, \"active\", false);\n        _define_property(this, \"context\", new AudioContext());\n        _define_property(this, \"masterGain\", this.context.createGain());\n        _define_property(this, \"currentSong\", void 0);\n        _define_property(this, \"songIdCounter\", 0);\n    }\n}",
		hidden: true,
		readOnly: true,
	},
	"/node_modules/sliver-engine/index.js": {
		code: "\"use strict\";\nObject.defineProperty(exports, \"__esModule\", {\n    value: true\n});\n_export_star(require(\"./Assets/Shapes/index.js\"), exports);\n_export_star(require(\"./CanvasController/index.js\"), exports);\n_export_star(require(\"./Context/index.js\"), exports);\n_export_star(require(\"./Events/index.js\"), exports);\n_export_star(require(\"./Events/decorators.js\"), exports);\n_export_star(require(\"./Game/index.js\"), exports);\n_export_star(require(\"./GameObject/index.js\"), exports);\n_export_star(require(\"./GameObject/Decorators/index.js\"), exports);\n_export_star(require(\"./GameObject/Hitboxes/index.js\"), exports);\n_export_star(require(\"./GameObject/Library/index.js\"), exports);\n_export_star(require(\"./GameObject/Walker.js\"), exports);\n_export_star(require(\"./Lib/index.js\"), exports);\n_export_star(require(\"./Physics/index.js\"), exports);\n_export_star(require(\"./Scenes/index.js\"), exports);\n_export_star(require(\"./Scenes/SceneManager/index.js\"), exports);\n_export_star(require(\"./Scenes/SceneManager/Transitions.js\"), exports);\n_export_star(require(\"./ScriptedEvents/index.js\"), exports);\n_export_star(require(\"./SoundManager/index.js\"), exports);\nfunction _export_star(from, to) {\n    Object.keys(from).forEach(function(k) {\n        if (k !== \"default\" && !Object.prototype.hasOwnProperty.call(to, k)) {\n            Object.defineProperty(to, k, {\n                enumerable: true,\n                get: function() {\n                    return from[k];\n                }\n            });\n        }\n    });\n    return from;\n}",
		hidden: true,
		readOnly: true,
	},
} as const;

