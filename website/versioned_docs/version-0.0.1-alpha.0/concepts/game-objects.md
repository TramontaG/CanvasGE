---
title: Game objects
---

`GameObject` is the base building block. Objects can:
- Have `tick()` / `render()` hooks.
- Own children (relative positioning and hierarchy).
- React to input/events via decorators.
- Define hitboxes and participate in collisions.

