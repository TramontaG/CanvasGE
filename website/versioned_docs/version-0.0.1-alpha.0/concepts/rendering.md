---
title: Rendering
---

Rendering is built around `CanvasController` and `ShapeDrawer` (primitives/text, clipping and rotation helpers, font loading).

Sprites and sprite-sheets are supported via helpers like `renderSprite` and `renderSpriteAnimation`.

