{
  "name": "sliver-engine",
  "version": "0.0.1-alpha.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "clean": "bun scripts/clean.js",
    "build": "bun run clean && bunx tsc -p tsconfig.build.json",
    "typecheck": "bunx tsc -p tsconfig.json",
    "prepack": "bun run build",
    "docs:install": "cd website && bun install",
    "docs:start": "cd website && bun run start",
    "docs:build": "cd website && bun run build",
    "docs:serve": "cd website && bun run serve",
    "docs:clear": "cd website && bun run clear",
    "docs:version": "cd website && bun run docs:version"
  }
}
